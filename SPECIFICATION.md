# Техническое задание (ТЗ) — Prompt Library для ComfyUI

**Версия:** v1.44 (2026-09-22: **мультивывод — базовая структура готова, UX доработки в процессе**); v1.43 (2026-09-21: `category_out` сохраняет пробелы — `_sanitize_folder_path` больше не заменяет пробел на `_` (по желанию пользователя: в Windows папки с пробелами): `"Мои Пейзажи"` → `"Мои Пейзажи/"`; спецсимволы `&`, `!` и т.п. и служебные ветки `__*` по-прежнему чистятся; JS-экспорт на диск не тронут (§37.11, запись 35)), v1.42 (2026-09-21: замена превью у записи с legacy JPG больше не оставляет старый `.jpg` сиротой — `_drop_legacy_preview_file` вызывается после успешной записи PNG в `_save_preview_upload` и `_save_thumbnail`; тест 305/305), v1.41 (2026-09-21: `IS_CHANGED` больше НЕ смотрит на `mode` — проверка из v1.39 делала `float("nan")` безусловным (`mode` непустой всегда), поэтому ComfyUI перестал кэшировать ноду и она на каждом Queue заново разбирала `library.json` (замер 0.165 с); смена режима и без этой проверки даёт перепрогон — `mode` входит в ключ кэша виджетом; живая проба `tests/_probe_live_cache.py` (5 прогонов: кэш, смена режима, подхват) — ЗЕЛЁНАЯ на живом ComfyUI 0.36.0, §33.6); v1.40 (2026-09-21: снимок графа вынесен ИЗ базы в `workflows/{id}.json` — база перестала пухнуть (5 новых карточек: +2.5 КБ вместо ~1700 КБ), функция открытия воркфлоу сохранена целиком, старые записи с inline-графом не переделывались (§24.3, §25.3.1); v1.39 (2026-09-21: аудит нашёл и закрыл четыре дефекта — массовый экспорт терял записи с одинаковыми названиями (теперь имена файлов уникальны в пределах папки, а в `.md` есть `№` записи, §39.2), `move_many` заводил пустую категорию-фантом по устаревшему id (§24 роутов), «Избранное» по категории теперь берёт поддерево (как перемещение/удаление/экспорт), поиск больше не бьёт по базе на каждую букву (дебаунс 250 мс); v1.38 (2026-09-21: подхват подчиняется режиму — в «📤 Выдача» он больше ничего не сохраняет, запись финального текста — режимом «📤📥 Выдача + запись» (§29.3, §30.4); v1.37 (2026-09-20: закрепление папок в проводнике дерева — `POST /prompt_library/folder_pin`, `pinned_folders` в `library.json`, кнопка 📌/📍 в `folderRow`, закреплённые папки сортируются вверх; v1.36 массовый drag-and-drop favorite через `POST /prompt_library/favorite_many` и `POST /prompt_library/move_many`; в v1.35 счётчик использований `use_count` + сортировка «Частые» — по убыванию выдач, при равенстве по `last_used`; отображается в мета-строке карточки как `×N` и в формуляре панели книги (§40); в v1.34 массовый экспорт на диск — одна УМНАЯ кнопка «📤 Экспорт» в шапке проводника рядом с «+ Категория»: при активных метках (Ctrl/Shift) экспортирует отмеченные записи и категории, без меток — текущую выбранную категорию (и «Всё»/«Избранное»/«Без категории» — вся база); на диске — зеркало иерархии (папка категории → файлы + подпапки), обложки рядом с `.md`, полные тексты догружаются из `/entry`, резиновый прогресс-бар на время экспорта (скрывая подсказку) (§39); в v1.33 экспорт записи на диск — кнопка «💾 Экспортировать» в панели книги, системный выбор папки; с обложкой — подпапка `<title>/` с `.md` и превью, без обложки — только `.md` (§38); в v1.32 панель свойств больше не зажимает контент — корень найден живьём: панель писала ширину в `widget.width` через `WidgetLegacy`, лечение — `hideInPanel` на виджетах ноды (§37); страж `unstickWidth` (v1.31) остался страховкой; в v1.30 закреп карточек (§36); в v1.29 название в ручном вводе (§35); в v1.28 подхват перестал молча теряться — `IS_CHANGED` (§33); в v1.27 глубокий аудит (§32); в v1.26 обложка из видеофайлов прогона (§31); в v1.25 подхват финального текста (§30), в v1.24 три режима и обложка из прогона без IMAGE-провода (§29) — тесты 304/304, смоук 83/83, аудит чист; + probe-инструменты живого замера DOM и кэша (§34, §33.6))
**Связь:** продолжение серии Prompt Keeper (v1.0 — «последний промпт», Library — «база промптов»)
**Важно (§37):** в записи v1.31 диагноз «писателя нет, значение — ископаемое» БЫЛ
ОШИБОЧНЫМ — прогон-ловушка в канвас-режиме не выделял ноду и потому ничего не
рендерил в панели. Настоящий писатель найден живым аудитом (`--audit-writer`) и
устранён в v1.32.

## Модель «как в реальной библиотеке»

Запись = книга в категории. Каталожная карточка: **название** (title), **аннотация** (полный текст промпта),
**категория** (внутреннее поле folder, путь `Фото/Портреты`), **обложка** (превью), формуляр: создана / выдана (`created_at` / `last_used`),
★ «список чтения» (favorite). Поиск — по названию и тексту. Порядок категории: сначала новые / по названию А–Я /
сначала старые / недавно выданные. Вид — как в проводнике Windows: 🖼 Крупные (163px сетка, по умолчанию),
🎞 Средние (109px сетка), 📋 Список (82px строки); выбор запоминается в localStorage. Ничего не выдумываем —
типовые библиотечные практики.

---

# 1. Цель проекта

Разработать кастомную ноду для ComfyUI **Prompt Library** — библиотеку промптов с категориями, поиском и превью изображений, которая:

- автоматически сохраняет промпты и превью в процессе генерации (если опция включена);
- позволяет всегда вернуться к ранее созданным промптам;
- по необходимости выдаёт нужный промпт в рабочий процесс.

---

# 2. Зафиксированные решения (опрос 2026-09-13)

| Вопрос | Решение |
|--------|---------|
| Архитектура | **Одна универсальная нода** (и сохраняет, и выдаёт). Отдельные Saver/Browser не делаем |
| Категории | Эволюция: сначала **свободная строка + избранное**, затем (по решению пользователя) **дерево категорий**: категории/подкатегории `Фото/Портреты` с созданием/переименованием/удалением + **имена записей**. Слово «полки» из интерфейса убрано — везде «категории» |
| Превью | **Файл прогона** (v1.24): обложка берётся после Queue из событий `executed`, провод не нужен (§29). Вход `image` (`IMAGE,VIDEO`) остался опцией — для картинки НЕ из этого прогона. Прежнее решение (2026-09-13: только провод) отменено в v1.24 |
| Хранение | **Внешний JSON + папка превью**. Не внутри workflow/PNG (как Keeper), а общая база для всех воркфлоу |

---

# 3. Назначение и схема работы

Одна нода на всю работу: принимает текст проводом (`source`) или ручным вводом,
выдаёт текст в поток и сохраняет записи. Обложка больше НЕ требует провода —
она берётся из файлов этого же прогона после Queue (v1.24, §29).

```text
LLM / Keeper / ручной ввод ──source──▶ [Prompt Library] ──prompt_out──▶ CLIP Encode
                                            ▲
   LLM-цепочка ▶ финальный текст ▶ CLIP     └── 📎 Текст в базу: узел-источник (v1.25, §30)
   (провод назад не нужен: он замкнул бы граф в кольцо, а ComfyUI исполняет только DAG)
```

Три режима в одном проходе `execute()` (§29.3):
1. **📥 Запись** — сохраняет входящий текст; выход пуст (сквозь — только если выход уже подключён проводом: совместимость старых графов).
2. **📤 Выдача** — на выход идёт текст выбранной записи (если записи нет — входящий); в базу ничего не пишется.
3. **📤📥 Выдача + запись** — выдаёт и сохраняет за один прогон.

Опциональный вход `image` (`IMAGE,VIDEO`) — обложка для картинки НЕ из этого
прогона (с диска, из другого воркфлоу); сам прогон провода не требует (§29).

---

# 4. Хранение данных (внешнее)

## 4.1. Расположение

```text
ComfyUI/user/prompt_library/
├── library.json
└── previews/
    └── {id}.png (legacy: {id}.jpg без метаданных)
```

- `user/` переживает обновление ноды и ComfyUI — выбрано сознательно.
- Путь резолвится от рабочей копии ComfyUI в рантайме (через `folder_paths` / `__file__`-относительно, уточнить на реализации).
- Удаление ноды базу не трёт.

## 4.2. Схема записи

`library.json` — объект `{"entries": [...], "folders": [...], "pinned_folders": [...]}` (legacy: голый список — мигрирует автоматически).

```json
{
  "id": "a3f9c1e2",
  "title": "Дайвер в латунном шлеме",
  "prompt": "portrait, soft light...",
  "folder": "Фото/Портреты",
  "favorite": false,
  "pinned": false,
  "created_at": "2026-09-13T12:00:00",
  "last_used": "2026-09-14T09:00:00",
  "use_count": 5,
  "preview": "previews/a3f9c1e2.png",
  "media": "video"
}
```

- `pinned_folders` — список закреплённых папок (v1.37, §37.10): пути к папкам, которые отображаются вверху дерева категорий. Служебные ветки (`__all`, `__fav`, `__root`) нельзя закреплять. При удалении/переименовании/перемещении папки список автоматически обновляется.

- Старые записи мигрируют: `category` → `folder`, `title` = первые 60 символов текста.
- Пустые категории хранятся в `folders` (иначе их было бы не показать в дереве).
- Удаление категории не теряет книги: записи переезжают в корень. Переименование двигает подкатегории и записи (префикс).

- `id` — 10 hex от MD5(`dedup_hash + timestamp`).
- `media` — `video` / `image` / `null` (старые записи). Описывает ТЕКУЩУЮ обложку, поэтому обновляется вместе с ней: из провода (`_media_of()`, v1.17), из расширения файла прогона (`_is_video_file`, v1.26) или из подсказки браузера при ручном превью/замене (§31). В дедупликацию не входит; у записей без метки — `null` (без перезаписи файла).
- `pinned` — закреп вверху папки (v1.30, §36): новые записи `false`, у старых
  отсутствие поля трактуется как `false` (`setdefault`, файл не переписывается зря).
- `use_count` — счётчик выдач (v1.35): увеличивается при каждом попадании записи в
  режим «Выдача» / «Выдача + запись». Новые записи `0`, у старых отсутствие = 0
  (`setdefault`). Используется для сортировки «Частые» (по убыванию, при равенстве —
  по `last_used` desc).
- Дедупликация двухслойная: тот же текст уже есть в базе (**в любой папке**, `_find_text_match`) → не сохраняем вообще, предупреждаем где лежит (консоль + тост + `skipped_duplicate` в ui / `duplicate` в `/add`); иначе хеш MD5(`prompt + \n + folder`) уже есть → не сохраняем повторно (возвращаем существующий id).
- `title` — имя книги; авто: первая строка текста до 60 символов; правится в панели книги.
- Лимит: 500 последних записей, новые вставляются в начало.
- Превью: PNG-тамбнейл, длинная сторона 512px (с запасом под крупный показ; в карточках даунскейлит браузер), ресемплинг LANCZOS, со встроенным чанком `workflow` (PngInfo — как у SaveImage; compact-JSON). Если провода нет, обложка подтягивается из прогона (§29) — и для видео тоже: первый кадр снимает PyAV (v1.26, §31). `preview: null` остаётся только когда в прогоне не было ни картинок, ни видео (кэш, текстовый прогон, ручное добавление без файла). Старые JPG остаются как есть, при backfill'е workflow one-time апгрейдятся в PNG (§24.3).
- `category` оставлен дублем `folder` в записи — legacy-совместимость.

---

# 5. Входы

| Имя | Тип | Обяз. | Описание |
|-----|-----|-------|----------|
| `source` | `*` (ANY) | optional | Подписанный вход «Промт (вход)» — провод от LLM / Keeper / др. нод |
| `image` | IMAGE,VIDEO | optional | Двухцветный сокет превью (синий IMAGE-тензор или зелёный VIDEO-объект ядра `VideoInput`; мульти-тип через запятую — штатный механизм, как `FLOAT,INT`). Не создаёт виджета, только коннектор. VIDEO разворачивается в первый кадр через `_extract_frame()` (`get_components().images[0]`) |
| `save_folder` | STRING (скрытый виджет, схлопнут) | required | Выбор дерева как есть: `""` = «Всё», `__fav`/`__root` = служебные ветки, иначе путь папки (пишет JS при каждом клике; персистится через `extra_pnginfo`; `onConfigure` восстанавливает). Python режет `__*` в корень — записи в служебные имена не сохраняются (§23.4) |
| `mode` | COMBO `📥 Запись` / `📤 Выдача` / `📤📥 Выдача + запись` | required | Запись = автосохранение (выход пуст, сквозь — только если выход уже подключён проводом); Выдача = выход текстом выбранной книги; Выдача + запись = оба дела за один прогон (v1.24, §29) |
| `selected` | STRING (hidden-виджет) | hidden/display | id выбранной записи. Пишется из JS, персистится через `extra_pnginfo` |
| `pickup` | STRING (скрытый виджет) | hidden | id узла-источника для подхвата текста после прогона (`""` = как раньше, сохранять входящий; v1.25, §30). Видимый контрол — DOM-строка `📎 Текст в базу` |
| `extra_pnginfo` | EXTRA_PNGINFO | hidden | Для патча workflow (как в Keeper) |
| `unique_id` | UNIQUE_ID | hidden | Для патча workflow (как в Keeper) |

**Ввод промпта:** в виджет `prompt` удалён. Ручной ввод через DOM-кнопку `➕ Добавить промпт` → поле названия (необязательно) + textarea + `💾 Сохранить промпт` (POST `/prompt_library/add` с `title`). Вход `source` — провод от LLM / Keeper.

Нативную кнопку не используем: связка «DOM-окно + нативная кнопка» даёт щель при ресайзе (апстрим issue #7942).

---

# 6. Выходы

| Имя | Тип | Описание |
|-----|-----|----------|
 | `prompt_out` | STRING | Итоговый текст: входящий (сквозной) или текст выбранной записи (режимы `📤 Выдача` / `📤📥 Выдача + запись`); в режиме `📥 Запись` — пусто (сквозь только при подключённом выходе, §29) |
 | `category_out` | STRING | Санитизированный путь выбранной категории для использования по проводу (например, `Пейзажи/Аляска/`). Пробелы сохраняются, нечитаемые символы заменены на `_`. Служебные ветки (`__all`, `__fav`, `__root`) → пустая строка. v1.37 (§37.11), v1.43 (пробелы больше не заменяются на `_`) |

Изменение текста на выходе запрещено (кроме выбора источника) — прозрачная нода, как Keeper. Третий выход `count` не делаем — счётчик виден в hint (`Записей в категории: N`).

---

# 7. Логика `execute()` (Python, `prompt_library_node.py`)

```
1. mode → issue / both / save_on (по умолчанию «Запись»; legacy `use_selected` → «Выдача»)
   out_linked = _output_linked(extra_pnginfo, unique_id) — у выхода уже есть провод?
   incoming = str(source), если source — непустая строка (IMAGE/LATENT отбрасываются); display = incoming; dirty = False
   out_text = incoming; в режиме «Запись» без провода на выходе — "" (старый граф с проводом — сквозь, §29)
   если (issue или both) и selected-id найден:
       out_text = запись.prompt; запись.last_used = now; dirty = True
2. fld = _storage_folder(save_folder || kwargs.folder || kwargs.category) — legacy-фолбэк, `__*` → корень
   если save_on и incoming непустой и pickup НЕ выбран:
       wf = _snapshot_workflow(extra_pnginfo)  (deep-copy только в этой ветке)
       dup = _find_text_match(entries, incoming)  (глобально — любая папка)
       если dup: skipped = {folder, id}, новой записи нет
       иначе insert {id, hash, title=auto, prompt, folder=fld, workflow=wf, media, preview=thumbnail(image)}
       added → preview_target = id (обложку берёт JS из прогона, §29);
       существующая → бэкфилл пустых workflow/media/preview, preview_target если обложки нет
       entries = entries[:500]; добавить fld + родителей в folders
   если dirty: write library.json (атомарно: tmp + rename); _broadcast_refresh() — только при added
3. если pickup выбран (v1.25): входящий текст НЕ сохраняется (шаг 2 пропущен),
   pickup_token = _pickup_stash(pickup, fld, _snapshot_workflow(extra_pnginfo)) → токен в _PICKUP (лимит 20)
4. если extra_pnginfo + unique_id:
       патч workflow.nodes[id].widgets_values = [mode, selected, save_folder, pickup]
5. notice (mode_notice): «Запись» с подключённым выходом · подхват включён при непустом входящем тексте
6. return {"ui": {"entries": до 200 × (id, title, head≤120, folder, favorite, created_at, last_used,
                                  has_preview, has_workflow, media),
                  "folders": [...], "selected": sel, "text": [display], "skipped_duplicate": {},
                  "saved_id": [id] | [], "pickup": [токен] | [], "pickup_node": [id] | [],
                  "mode_notice": [notice]},
           "result": (out_text,)}
```

Роуты продолжают логику снаружи: `/attach_preview` (§29) доводит обложку прогона,
`/save_pickup` (§30) создаёт запись из подхваченного текста по одноразовому токену.

Требования:
- `OUTPUT_NODE = True` (нужно для `{"ui": ...}` → `onExecuted` и для `extra_pnginfo`-патча).
- `FUNCTION = "execute"`, `CATEGORY = "My_custom_nodes/Prompts"`.
- `RETURN_TYPES = ("STRING", "STRING")`, `RETURN_NAMES = ("prompt_out", "category_out")`. v1.37 (§37.11)
- Перезапись JSON целиком под каждый сейв допустима (база маленькая).
- Пустой `folder` = корень (`""`), без `"Без категории"`.
- `**kwargs["category"]` — совместимость со старыми workflow (виджет раньше назывался `category`).

Endpoints (`server.PromptServer`): GET `list` (`{entries` без `workflow`, но с
`has_workflow`, `folders}`) / `entry?id` (полная запись, включая `workflow`) /
`preview?id` (файл из записи, guard `previews/` + jpg|png, legacy-фолбэк) /
`search?q` (v1.27 §32: полнотекстовый поиск по названию/тексту/папке —
возвращает ТОЛЬКО `ids`, потому что `list` отдаёт лишь `head` ≤120 символов);
POST `add` (ручное сохранение с кнопки, без workflow; поля `title` (пусто = авто из
начала текста, v1.29 §35), `preview_data` — PNG/JPEG dataURL с диска → `previews/{id}.png`) / `favorite` / `favorite_many` (`{ids}` или `{folder_paths}` — массовая отметка избранного, v1.36 §drag→★; с v1.39 папка — это ПОДДЕРЕВО: отметка категории берёт и вложенные подпапки, как перемещение/удаление/экспорт) / `pin` (`{id, pinned}` — закреп/откреп, v1.30 §36) / `folder_pin` (`{path, pinned}` — закреп/откреп папки в проводнике, v1.37 §37.10; валидация: `__` префикс запрещён, папка должна существовать) / `update`
(title+prompt+folder, пересчёт hash) / `delete` (+ файл превью из записи) /
`folder_create` / `folder_rename` (движет префикс) / `folder_delete`
(записи переезжают в корень, не теряются);
`delete_many` (`{ids}` — массовое удаление записей, мусор игнорируется,
превью чистятся) / `folder_delete_many` (`{paths}` — массовое удаление категорий,
семантика как у одиночного) /
`attach_preview` (`{id}` + либо `{filename, subfolder, type}` — файл прогона, §29,
либо `{preview_data}` — PNG-dataURL кадра от браузера, v1.26 §31; `media` уточняет
метку типа, `force` разрешает замену готового превью; файл ищется в output/temp/
input с защитой от traversal, видео декодируется первым кадром) /
`save_pickup` (`{token, text}` — запись текста, подхваченного из узла-источника
ПОСЛЕ прогона, §30; токен одноразовый, выдаётся `execute()` в `ui.pickup`,
пустой текст → `skipped: "empty"`, дубль → `duplicate: true`).

Все POST-роуты и читающие `list`/`entry`/`search` работают под общим `_DB_LOCK`
(v1.27 §32.2), а `attach_preview` проверяет `id` на `[a-zA-Z0-9]+`, прежде чем
подставить его в имя файла превью (§32.3).

`_pl_list` возвращает `{"entries": ..., "folders": ..., "pinned_folders": [...]}`
(v1.37). `_save_db` всегда сохраняет `pinned_folders` в `library.json`.
При удалении/переименовании/перемещении папки `pinned_folders` автоматически
очищается/обновляется, чтобы не оставались «призраки» закреплённых путей.

---

# 8. JS-интерфейс (`web/js/prompt_library.js`)

Обычными виджетами не обойтись — нужен **DOM-виджет** внутри ноды (скил `comfyui-js-extension`).

## 8.1. Компоновка (сверху вниз)

Нативные виджеты ComfyUI (сверху вниз): `mode` (📥 Запись / 📤 Выдача / 📤📥 Выдача + запись),
скрытые `selected` + `save_folder` + `pickup` (v1.25, §30). Нет виджета `prompt` — ручной ввод через DOM-кнопку. Кнопка и автосейв пишут в открытую в дереве категорию (корень — если «Всё»).

DOM-библиотека (`addDOMWidget`, сериализация выключена):
- **Строка подхвата** `pickupRow` (22px, `flex-shrink:0`, первый ребёнок `root`): `📎 Текст в базу:` + `<select>` со списком узлов живого графа (STRING-выход или строковый виджет; своя нода, другие `PromptLibrary` и неактивные узлы не предлагаются (mute = `mode 2`, bypass = `mode 4` — как `isMuted` во фронтенде; v1.27 §32.4); «— из входа (провод) —» = как раньше) — v1.25, §30
- **Тогл ручного ввода**: `➕ Добавить промпт` → показывает/скрывает поле названия `inputTitle` (однострочное, плейсхолдер «Название (необязательно)»; v1.29, §35) + textarea (4 строки) + `📷 Прикрепить превью` (**картинка ИЛИ видео** с диска: картинка — даунскейл 512px через canvas, видео — первый кадр через `<video>`+canvas; оба → PNG dataURL; v1.26 §31) + `💾 Сохранить промпт` (POST `/prompt_library/add` с `preview_data` + `media`; нечитаемый файл — запись без превью + стойкая подсказка). При переключении вызывает `syncNodeSize()` для подстройки высоты
- тулбар: вид (🖼 Крупные / 🎞 Средние / 📋 Список) → порядок категории (новые / А–Я / старые / недавно выданные) → тип (Всё / 🖼 Фото / 🎬 Видео, запоминается в localStorage; записи без метки видны только во «Все») → поиск (название + текст, клиентский);
- ряд: слева список книг (`list`, `flex:1`) | справа дерево категорий (`treeBox`, `width:34%`):
  - `list` = flex column: `listHead`(22px, пустой спейсер) + `listContent`(скролл, карточки)
  - `treeBox` = flex column: `treeHead`(22px, "Категории" + "+ Категория") + `tree`(скролл)
  - Оба контейнера начинаются и заканчиваются на одном уровне (одинаковая структура)
  - `list` растягивается при ресайзе ноды (`flex:1`), `treeBox` — фиксированная доля (`34%`)
  - Обе панели — `flex:1 1 0` + `min-height:0`: заполняют отведённое место и СЖИМАЮТСЯ
    под него (свой скролл), переполнения наружу нет — соседи не перекрываются (§28)
  - Категории: 📚 Всё, ★ Избранное, 📥 Без категории + вложенные 📁 с отступом; ✏️ переименовать, 🗑 удалить с переездом книг в корень; 📤 экспортировать всю категорию (включая подкатегории) на диск (v1.34 §39); `+ Категория` создаёт категорию, в текущей — подкатегорию);
- drag & drop (нативный HTML5, без библиотек): книга тащится на строку категории / на фон списка открытой категории (переезд через POST `update`); категория тащится в другую категорию или на 📚 Всё/📥 Без категории (переезд в корень через POST `folder_rename`); дроп на себя/внутрь себя и на текущее место — игнор; дроп-зона подсвечивается пунктиром;
- карточка: обложка, название жирным (с бейджем 🎬/📷 по `media`, префиксом 📌 у закреплённых (v1.30, §36), текст-only через `plBadge()` — раскладку не трогает), начало текста (в списке), категория · даты, 📌/★/🗑;
- мультивыделение на удаление: Ctrl+клик — поштучно, Shift+клик — диапазон (карточки — в порядке списка, папки — в порядке дерева; служебные ветки не помечаются; якорь диапазона ставит и обычный клик, как в проводнике); помеченные — левая акцент-полоса (`box-shadow: inset`, layout не трогает) + тёплая подложка + ☑; метки живут только в сессии, текущую папку и выбранную запись не меняют → цель сохранения (`save_folder`) всегда однозначна; режим эксклюзивный — разметка чужого типа с зажатыми Ctrl/Shift игнорируется, сброс чужих меток — только обычным кликом (затем обычное действие); bulk-бар в строке-подсказке («Помечено — … · 🗑 Удалить выбранное · ✖ Снять выделение»), шпаргалка по умолчанию, тултипы на строках; 📤 экспорта в bulk-баре НЕТ (v1.34 — одна умная кнопка «📤 Экспорт» в шапке проводника: метки → отмеченное, без меток → категория, §39); метки снимаются кнопкой, обычным кликом по любой карточке/папке, кликом по пустому месту списка/дерева (`ev.target === zone`) или Esc (слушатель на корне ноды); bulk-бар живёт в НИЖНЕЙ строке ноды (`hintRow`, фиксированные 22px, `flex-shrink:0`: подсказка и бар сменяют друг друга на одном месте — раскладка не двигается), счётчик `flex:1 1 auto` + `min-width:0` (многоточие), кнопки `flex-shrink:0` — из строки не выдавливаются (§28); удаление — через `confirm` + `delete_many`/`folder_delete_many` + один `reload()`;
- панель книги: название, категория, полный текст, формуляр (`№ id · создана · выдана` + `· 🎬 видео` / `· 📷 фото` по `media`, у старых записей без метки — без хвоста), 📋 Копировать (clipboard + fallback), ✏️/💾/✖ редактирование (POST `update`; ✖ Отмена возвращает значения из базы — с подтверждением, только если правки реально есть) — в режиме правки рядом с 💾 живут ✖ и 🖼/🎬, 🖼/🎬 **Заменить превью** (v1.26 §31: файл с диска — картинка или видео, у видео первый кадр; `attach_preview` с `preview_data`+`media`+`force`; подпись кнопки показывает текущий тип обложки; кнопка живёт ТОЛЬКО в режиме ✏️ Редактировать и спрашивает подтверждение — замена необратима), 💾 **Экспортировать** (v1.33 §38: экспорт записи на диск — системный выбор папки (Chrome/Edge); **с обложкой** — создаётся **подпапка `<title>/`** и в неё пишутся `<title>.md` (текст + метаданные) + обложка `<title>.png/.jpg`; **без обложки** — только `<title>.md` прямо в выбранную папку, без подпапки; байты обложки — тем же роутом `/prompt_library/preview`, что и карточка; новых роутов/данных серверу не нужно).

## 8.2. Хуки LiteGraph

- `onNodeCreated` — DOM-виджет (с `computeSize` с фиксированными константами), схлопывание технических `selected`/`save_folder`, первичная загрузка через `reload()` (GET `list`). Регистрирует `app.api.addEventListener("prompt_library/refresh", plListener)` для автообновления (§26).
- `onExecuted(message)` — пришедшие `entries` → полный `reload()`; `mode_notice`/`skipped_duplicate` → нижняя стойкая строка (`hintSticky`, дубль — ещё и тост). **Не пишет** в виджет `prompt` (его больше нет).
- Слушатели `app.api` `executed` / `execution_success` — обложка из прогона (`saved_id` → `/attach_preview`, §29) и подхват текста (`pickup`/`pickup_node` → `/save_pickup`, §30); `pendingCover`/`pendingPickup`/`runTexts` чистятся на успехе и на `execution_error`, сами слушатели снимаются в `onRemoved`.
- `onConfigure(info)` — синхронный restore папки из данных воркфлоу
  (`info.widgets_values_named.save_folder`, фолбэк — `info.widgets_values[2]`
  по порядку INPUT_TYPES) + одноразовый reconcile после `reload()` (виджет
  побеждает, иначе sync-значение, мусор → корень). Ноль таймеров (§23).
  Список кандидатов подхвата пересобирается после загрузки (`refreshPickupOptions`, §30).
- `onConnectionsChange` — только сторож цикла.
- Поиск/фильтр/сортировка — чистый JS по `entries`.
- Клик по карточке → `selected = id` + панель книги (GET `entry` с кэшем). Режим **не** переключается автоматически.
- Защита от кольца (JS, без участия Python — цикл виден только по проводам): переход в выдающий режим («📤 Выдача» / «📤📥 Выдача + запись») при подключённом `image` показывает confirm «отключить IMAGE-провод?»; при отмене `mode` возвращается на «📥 Запись» (`ensureIssueSafe()` возвращает bool). Сторож `checkCycle` (`onConnectionsChange`/`onConfigure`/`onNodeCreated`) красит ноду в красный + toast, если `image` подключён И выход куда-то ведёт; снимается сам при исправлении. Переключение папки сбрасывает `detailId`, `selWidget.value` и скрывает detail-панель.
- Автосокеты (фронтенд 1.52 даёт сокет каждому виджету, `getWidgetConfig`, тип `*` — проверено в установленном бандле): у `selected`/`save_folder`/`pickup` автосокеты удаляются (`dropAutoSockets` через `removeInput`, только неподключённые). Остаются ровно два подписанных входа: `source`, `image`. Подключённые не трогаем.
 - ★/🗑 записи и ✏️/🗑 папок — прямые POST на endpoints + `reload()`; удаления через `confirm()`; 📌/📍 кнопка закрепления папок в проводнике (v1.37 §37.10) — `POST /prompt_library/folder_pin`, закреплённые папки сортируются вверху дерева;

## 8.3. Стиль

Тёмная тема под ComfyUI. Дерево и список со своим скроллом (max-height ~300–336px). Превью отдаёт endpoint `GET /prompt_library/preview?id=` напрямую файлом (без чтения базы: `png → jpg`; `Last-Modified` → дешёвые 304). В JS стабильный `t=created_at` (не `Date.now()` — иначе перезакачка всех превью при каждом рендере) + `loading="lazy"`.

---

# 9. Поиск, категории, порядок (правила)

- Поиск — по подстроке в названии + тексте + категории, case-insensitive, клиентский.
- Категория — путь через `/` (`Фото/Портреты`); нормализация режет пробелы и пустые сегменты; пусто = корень.
- Родительские категории дерева выводятся автоматически из путей записей.
- Избранное — boolean-флаг, ★-тоггл, фильтр `★ Избранное`.
- Порядок категории: сначала новые (default) / по названию А–Я (`localeCompare ru`) / сначала старые / недавно выданные (`last_used`) / **частые** (`use_count` desc, при равенстве `last_used` desc).
- Закреп (v1.30, §36): в открытой папке закреплённые — поверх выбранного порядка (внутри групп порядок сохраняется); во «Всё» / «Избранном» / «Без категории» закреп не действует.
- Переименование категории двигает префикс у подкатегорий и записей; удаление категории переносит её книги в корень.

---

# 10. Персистентность (наследие Keeper, скил `comfyui-png-persistence`)

Проблема Keeper сохраняется: workflow JSON захватывается до выполнения → виджеты пусты в PNG.

- Решение то же: патч `extra_pnginfo['workflow'].nodes[unique_id].widgets_values` на стороне Python.
- Персистим: `[mode, selected, save_folder, pickup]` (порядок = порядок INPUT_TYPES; старые массивы из 3 значений читаются — `pickup` = `""`).
- Сама база (`library.json`) в PNG не дублируется — только выбор и настройки.
- Билингва виджетов — через `locales/{lang}/nodeDefs.json` (скилы `comfyui-localization`, `comfyui-bilingual-node`), паттерн — из Keeper.

---

# 11. Архитектура файлов

```text
Prompt_Library/
├── __init__.py                 # экспорт маппингов, WEB_DIRECTORY = "web/js" (как в Keeper — "web")
├── prompt_library_node.py      # класс PromptLibrary
├── locales/
│   └── ru/
│       └── nodeDefs.json       # переводы входов/выходов
├── web/
│   └── js/
│       └── prompt_library.js   # DOM-виджет библиотеки
├── tests/                      # ТОЛЬКО здесь тесты проекта (AGENTS.md §1.1)
│   ├── _test_prompt_library.py # Python-песочница (304 проверки)
│   ├── _smoke_prompt_library.mjs # JS-смоук (73 фазы)
│   ├── _audit_prompt_library.mjs # статический аудит
│   ├── _probe_live_dom.py      # живой замер DOM + аудит писателя width (--audit-writer, §34/§37.9)
│   │                           #   требует запущенный ComfyUI + Chrome
│   ├── _probe_live_cache.py    # живой замер кэша ComfyUI (v1.41, §33.6) — требует запущенный ComfyUI
│   ├── _probe_live_library.py  # живой аудит данных базы, с защитой от правки во время замера (v1.41, §33.7)
│   └── _probe_snippet.js       # сниппет для F12 → Console: диагностика залипшей раскладки живой сессии (§34)
├── README.md                   # пользовательская дока
├── SPECIFICATION.md            # этот файл
└── .gitignore                  # __pycache__/, *.pyc, .DS_Store
```

Каждый проект — отдельный git-репозиторий (см. AGENTS.md §6). Корень бандла — не репозиторий.

---

# 12. Ограничения (не делаем)

- Превью для записи, созданной вручную (`➕ Добавить промпт`) — только загрузкой файла с диска: обложки прогона у неё нет.
- Экспорт/импорт базы (TXT/JSON), облачная синхронизация.
- Пагинация/виртуализация больше 500 записей (только первые 200 в UI-пакет + поиск).
- Множественный выбор / пакетная выдача.
- Теги отдельно от папок (пока хватает полок + избранного).
- Endpoint-авторизация / многопользовательность — локальный ComfyUI, один пользователь.

---

# 13. Совместимость

- Актуальный ComfyUI (LiteGraph, `/i18n`, `addDOMWidget`, Nodes 2.0: `const { app } = window.comfyAPI.app`).
- Провод Keeper → Library (STRING → STRING) — штатный апгрейд-путь.
- PIL/Pillow для тамбнейлов (уже зависимость ComfyUI).
- Windows-пути (`F:/...`, `D:/...`) — только `os.path.join`, без хардкода слешей.

---

# 14. Критерии приёмки (статус: принято пользователем в ручном тесте)

| Критерий | Статус |
|----------|--------|
| Нода в меню `My_custom_nodes/Prompts` | ✅ |
| Поток Library → CLIP не ломает генерацию | ✅ |
| Режим `📥 Запись` → запись в `library.json` после Queue (выход пуст) | ✅ |
| Режим `📤📥 Выдача + запись` — выдаёт и сохраняет в один прогон (v1.24) | ✅ |
| Обложка сохранённой записи подтягивается из прогона без IMAGE-провода (v1.24, §29) | ✅ |
| Дубликат не плодит записи | ✅ |
| Превью из IMAGE-провода в карточке | ✅ |
| Прогон без файлов-превью (кэш, текстовый прогон) — запись без превью (`preview: null`) | ✅ |
| Обложка из видеофайла прогона: первый кадр через PyAV (v1.26, §31) | ✅ тесты 225/225 |
| Видео как превью ручного промпта (кадр снимает браузер) (v1.26, §31) | ✅ тесты 225/225 |
| Замена обложки существующей записи картинкой или видео (v1.26, §31) | ✅ тесты 225/225 |
| Подхват финального текста из выбранного узла после прогона (v1.25, §30) | ✅ тесты 233/233; живой тест ожидается |
| Мутации базы не теряют чужие изменения (замок `_DB_LOCK`) (v1.27, §32.2) | ✅ тесты 225/225 |
| `id` не может выйти за `previews/` (guard в `attach_preview`) (v1.27, §32.3) | ✅ тесты 225/225 |
| Поиск находит слово из СЕРЕДИНЫ длинного текста (`/search`) (v1.27, §32.5) | ✅ тесты 225/225 |
| Mute/bypass-узлы не предлагаются в подхвате (v1.27, §32.4) | ✅ смоук 70/70 |
| Двойной клик `💾` не плодит запросы (v1.27, §32.6) | ✅ смоук 70/70 |
| Подхват срабатывает в КАЖДОМ прогоне, а не только после смены виджета (v1.28, §33) | ✅ Python 233/233 + смоук 70/70 |
| Пропущенный подхват (нет токена / отказ сервера) виден пользователю (v1.28, §33.3) | ✅ смоук 70/70 |
| Кнопка `➕ Добавить промпт` (POST без Queue) | ✅ |
| Закреп карточек в папке (📌, вверху папки) (v1.30, §36) | ✅ Python 244/244 + смоук 72/72 |
| Закреп папок в проводнике (📌/📍, вверху дерева) (v1.37, §37.10) | ✅ Python 244/244 + аудит 19 роутов |
| Панель свойств не зажимает контент (v1.32, §37): панель не рендерит виджеты ноды (`hideInPanel`) | ✅ смоук 74/74 + живой аудит `--audit-writer`: hits 0 в канвасе и Vue |
| Заражённая сессия (чужое `widget.width` уже на виджете) лечится стражем `unstickWidth` (v1.31, §37.4) | ✅ смоук 74/74 (фаза «v1.31») |
| Экспорт записи в папку на диске (v1.33, §38): кнопка, `.md` с метаданными, обложка `.png/.jpg`, отмена диалога, браузер без поддержки | ✅ смоук 76/76 (фаза «v1.33») |
| Массовый экспорт (v1.34, §39): 📤 в шапке проводника (одна умная кнопка: метки → отмеченное, без меток → категория/база), зеркало иерархии, обложка рядом с `.md`, полные тексты из `/entry`, __all/__fav/__root, отмена/без поддержки | ✅ смоук 80/80 (фаза «v1.34») |
| Название в ручном вводе (пусто = из начала текста) (v1.29, §35) | ✅ Python 236/236 + смоук 71/71 |
| Поиск по названию/тексту/категории | ✅ |
| Дерево полок: создать/переименовать/удалить (книги в корень) | ✅ |
| Имена записей + редактирование (название/категория/текст) | ✅ |
| Сортировка: новые / А–Я / старые / недавно выданные / **частые (use_count)** | ✅ |
| Счётчик использований `use_count` в мета-строке карточки (`×N`) и панели книги | ✅ |
| Виды: крупные 163 / средние 109 / список 82 | ✅ |
| ★ избранное переживает рестарт | ✅ |
| Режим `📤 Выдача` выдаёт текст записи в CLIP, `last_used` обновляется | ✅ |
| Выбор и настройки переживают reopen workflow/PNG | ✅ |
| RU-локализация | ✅ |
| Вертикальный stretch: контент тянется с нодой вниз, без пустоты (v1.8) | ✅ проверено живьём 2026-09-17 |
| Папка восстанавливается при reopen/переключении вкладок без мигания (v1.9) | ✅ проверено живьём 2026-09-17 |
| Drag карточки на канвас / 📥 открывает сохранённый граф через confirm (v1.10) | ✅ проверено живьём 2026-09-17 |
| Lazy-превью: без базы, кэш + 304, без мелькания при рендерах (v1.10) | ✅ проверено живьём 2026-09-17 |

---

# 15. Хроника реализации

1. ✅ Python-каркас + JSON + автосейв + превью + `extra_pnginfo`-патч.
2. ✅ JS: список + выбор + выдача + поиск + ★/🗑.
3. ✅ Вход `source` (ANY) возвращён как подписанный «Промт (вход)» — единственный ясный путь провода (был удалён по ошибке, возвращён по требованию).
4. ✅ Нативная кнопка `💾 Сохранить промпт`; `prompt_height`, плашка, CSS-инъекции, таймеры и наблюдатели высоты — удалены. Окно держится контентом (голова 300 символов при проводе), списки фиксированы 480px.
5. ✅ Категории/подкатегории (создать/переименовать/удалить, DnD), имена записей, сортировка, виды как в Windows, `last_used`. Термин «полки» убран — везде «категории».
6. ✅ `git init` + `gh repo create` + push (2026-09-14).
7. ✅ Виджет `prompt` удалён из INPUT_TYPES. Ручной ввод через DOM-кнопку `➕ Добавить промпт` → textarea + save (POST `/prompt_library/add`). Вход `source` — единственный способ передать промпт проводом.
8. ✅ v1.16: вход `image` → `IMAGE,VIDEO` (двухцветный сокет, принимает синий и зелёный провода); `_extract_frame()` разворачивает `VideoInput` в первый кадр; `_save_thumbnail()` закалён (батч/список/uint8/grayscale). Скилл `comfyui-video-socket` (в корне бандла, 3 папки).
9. ✅ v1.17: авто-метка `media` (`_media_of()` в `execute()` → `_add_entry(media=)` + backfill в пустое); бейдж 🎬/📷 в заголовке карточки (`plBadge()`); фильтр типа в тулбаре (+ localStorage); метка в формуляре деталки (`· 🎬 видео` / `· 📷 фото`); `media` в ui-пакете и `/list` (через `_strip_entry`); локаль `image` → «Изображение / видео»; иконка 📷 вместо 🖼 везде (рендер Windows).
10. ✅ v1.18: мультивыделение Ctrl/Shift (карточки + папки) с bulk-баром и массовым удалением (`delete_many`/`folder_delete_many`); метки session-only (+ чистка протухших в `reload()`), цель сохранения не меняется; эксклюзив — чужой тип с модификаторами игнорируется, сброс только обычным кликом/пустым местом/Esc; акцент-полоса `box-shadow: inset` + подложка; переименование на месте (`st.inlineEdit`: Enter/blur — применить, Esc/пусто — отмена; ошибки через toast, не alert); `PL_JS_VERSION = "1.18-multiselect"` + видимая версия в тулбаре; общий helper `_req_body()`; коэрсия битого `prompt` в `_load_db`; 15 новых тестов (90/90); смоук-фазы меток и inlineEdit (47/47), аудит чист (12 роутов); глубокая ревизия без колхоза (helper `_req_body`, коэрсия `prompt`, backfill `media`, чистка протухших меток, протухшие комментарии вычищены).
11. ✅ v1.19: ручное превью с диска (`_save_preview_upload` + кнопка `📷 Прикрепить превью` с даунскейлом в браузере; `INPUT_H` 130→170); §11 тестов (4 проверки).
12. ✅ v1.20: глобальный дубль по тексту (`_find_text_match` в execute и `/add`: та же строка в другой папке — пропуск + предупреждение с папкой оригинала, backfill по workflow/media — на найденную запись); тост и `⚠️ Уже есть в «…»` на ручной кнопке; фикс краша Queue (`skipped_duplicate: {}`, а не None — ComfyUI сливает ui перебором; JS проверяет `sd.id`; регресс-тест `_ui_merge_ok`); возврат высоты после закрытия панелей (`panelOpened`/`shrinkBack`: запомнили при открытии — вернули при закрытии, только canvas, ручной ресайз не срезаем); `PL_JS_VERSION = "1.20-dup-warn"`; §12 тестов.
13. ✅ v1.21: автообновление Library-нод (§26) — `_broadcast_refresh()` → `send_sync("prompt_library/refresh")` → JS `app.api.addEventListener("prompt_library/refresh", ...)` → `reload()`. Флаг `need_broadcast` (только при `added=True`). Исправлен мёртвый `window.addEventListener` fallback в JS. Тесты 101/101 + смоук 48/48.
14. ✅ v1.22 (аудит 2026-09-18, §27): broadcast из ВСЕХ мутирующих роутов (не только Queue); синхронизация Library-нод одной страницы (`plLiveStates` + `st.apiPost`, §26.9); снятие слушателей `app.api`/settings в `onRemoved`; стойкая подсказка о дубле (`hintSticky`); серверная защита служебного префикса `__`; бэкфилл превью у существующей записи; `_snapshot_workflow` — только в ветке записи; вычищен мёртвый код (`HEAD_CHARS`/`lastFullText`); `PL_JS_VERSION = "1.22-broadcast-all"`; тесты 133/133 + смоук 51/51.
15. ✅ v1.23 (§28): панели списка/дерева сжимаются (пол `min-height:480px` снят, пол один — `computeLayoutSize`) — панель книги больше не перекрывает низ списка с кнопками карточек; кнопки массового удаления вернулись вниз (нижняя строка 22px, подсказка и бар сменяют друг друга), счётчик не выдавливает кнопки за край; `main` — `overflow:hidden` страховкой; `PL_JS_VERSION = "1.23-panes-fit"`; смоук 53/53.
16. ✅ v1.24 (§29): **одна нода вместо двух** — третий режим «📤📥 Выдача + запись»,
    автоподхват обложки из прогона (JS-слушатели `executed`/`execution_success` →
    роут `attach_preview`, без IMAGE-провода и без кольца); совместимость «Записи»
    по подключённому выходу (`_output_linked` + `mode_notice`);
    `PL_JS_VERSION = "1.24-one-node"` → `1.24b` (живой тест: id в `onNodeCreated`
    ещё `-1` — свой id читаем в момент события; запас `st.runImages` для позиции
    после узлов вывода); тесты 173/173 + смоук 57/57.
17. ✅ v1.25 (§30): **подхват финального текста** — скрытый виджет `pickup` +
    DOM-строка `📎 Текст в базу` (первый ребёнок `root`), текст узла-источника
    забирается из `executed` на `execution_success` и сохраняется по одноразовому
    токену (`/save_pickup`), обложка — из того же прогона; входящий текст при
    включённом подхвате не сохраняется (`mode_notice`);
    `PL_JS_VERSION = "1.25-pickup"`, `BASE_H = 596 → 624`;
    тесты 194/194 + смоук 61/61.
18. ✅ v1.26 (§31): **медиа-обложка без провода во всех трёх точках** — видео из
    прогона (`_load_video_frame` через PyAV: PIL медиафайл не открывает, отсюда и
    было «видео-превью не сохраняется»), видео в ручном превью и замена обложки
    существующей записи (`attach_preview` принимает `preview_data` + `media` +
    `force`); `media` теперь описывает текущую обложку;
    `PL_JS_VERSION = "1.26-media"` (BASE_H без изменений);
    тесты 213/213 + смоук 65/65.
19. ✅ v1.27 (§32): **аудит 2026-09-19** — `_DB_LOCK` на read-modify-write
    (все мутирующие роуты + `execute()`), guard `[a-zA-Z0-9]+` на `id` в
    `attach_preview` (единственная точка записи превью без него), полнотекстовый
    `GET /search?q=` (список отдаёт только `head` ≤120 символов), фильтр
    mute/bypass в подхвате (было только `mode === 4`), защита от двойного клика на
    `💾` (ручное сохранение и панель книги), проверка дескриптора
    `LiteGraph.vueNodesMode`, гигиена `st.previewStamp`/`st.deepIds`;
    `PL_JS_VERSION = "1.27-audit"`; тесты 225/225 + смоук 68/68.
20. ✅ v1.28 (§33): **`IS_CHANGED` против кэша** — жалоба «подхват то пишет, то
    нет»: нода без проводов кэшировалась по виджетам, `execute()` не вызывался,
    токен не выдавался. Непустой `pickup` → `float("nan")` (прогон каждый Queue),
    пусто → `None` (обычный кэш); пропуски видны в нижней строке ноды, а не молчат;
    `PL_JS_VERSION = "1.28-pickup"`; тесты 233/233 + смоук 70/70.
21. ✅ §34 (без бампа версии): **probe-инструменты живого замера DOM** —
    стабильные классы `pl-root`/`pl-main`/`pl-scroll` на ключевых контейнерах
    (только якоря для замера, CSS по ним не строится, поведение не меняется) +
    `tests/_probe_live_dom.py` (headless Chrome + CDP: цепочка ширин ноды в живом
    ComfyUI) и `tests/_probe_snippet.js` (сниппет в F12 для залипшей сессии).
22. ✅ v1.29 (§35): **название в ручном вводе** — поле `inputTitle` над textarea
    («Название (необязательно)»), уходит в POST `/add` как `title`; пусто —
    сервер берёт начало текста (`_auto_title`, как раньше). Бэкенд менять не
    пришлось: `title` в `/add` и `_add_entry(title=)` были с самого начала.
    `INPUT_H = 170 → 202` (+поле ~28px + gap 4px); `PL_JS_VERSION = "1.29-manual-title"`;
    тесты 236/236 + смоук 71/71.
23. ✅ v1.30 (§36): **закреп карточек** — поле `pinned` + роут `/pin` + кнопка 📌
    (только в папке); закреплённые всплывают вверху папки поверх порядка;
    `PL_JS_VERSION = "1.30-pin"`; тесты 244/244 + смоук 72/72.
24. ⚠️ v1.31 (§37): **панель свойств не зажимает контент** — страж `unstickWidth`
    сносит чужое `widget.width`. Симптом лечил, но диагноз был неверным
    («писателя в стеке нет, значение — ископаемое»): живой прогон-ловушка
    в канвас-режиме не выделял ноду, панель ничего не рендерила и хитов не
    давала. Страж остался как страховка, фикс корня — v1.32.
25. ✅ v1.32 (§37.2–§37.9): **настоящий корень** — панель свойств рендерит
    виджеты узла, для типа `custom` монтируется `WidgetLegacy.vue`, а он пишет
    `widgetInstance.width = ширина панели` прямо в живой объект виджета (замер:
    262/235 при ноде 1000; у пользователя 213 при ноде 1137). Лечение —
    `options.hideInPanel = true` у `pl_browser` и технических полей (панель
    их не рендерит; DOM-оверлей/Nodes 2.0/сериализация не затронуты);
    `PL_JS_VERSION = "1.32-hide-in-panel"`; тесты 244/244 + смоук 74/74 + аудит
    чист; живой аудит `--audit-writer`: hits 0 в обоих режимах.
26. ✅ v1.33 (§38): **экспорт записи на диск** — кнопка «💾 Экспортировать»
    в ряду действий панели книги; по клику системный выбор папки
    (`showDirectoryPicker`, File System Access API — Chrome/Edge). Раскладка
    зависит от содержимого: **с обложкой** — создаётся подпапка `<title>/` и в
    неё пишутся `<title>.md` (текст + метаданные: категория, дата, тип,
    избранное) + обложка `<title>.png/.jpg` (байты — роутом
    `/prompt_library/preview`, расширение по `blob.type`); **без обложки** —
    только `<title>.md` прямо в выбранную папку (подпапку не плодим). Имя
    файла/папки задаёт `st.sanitizeFileName()` (вырезает недопустимые символы,
    точки в начале, фолбэк «запись»). Чистый JS, новых Python-роутов/данных не
    нужно; отмена диалога — тихий выход; браузер без поддержки — стойкая
    подсказка. `PL_JS_VERSION = "1.33-export-to-folder"`; тесты 244/244 + смоук
    76/76 + аудит чист.
27. ✅ v1.34 (§39): **массовый экспорт на диск** — одна УМНАЯ кнопка «📤 Экспорт»
    в шапке проводника (рядом с «+ Категория»): при активных метках (Ctrl/Shift)
    экспортирует отмеченные записи и категории, без меток — текущую выбранную
    ветку (категорию с подкатегориями / «Всё» / «Избранное» / «Без категории»);
    отдельных 📤 на строках и в bulk-баре НЕТ (переработано); резиновый
    прогресс-бар в нижней строке на время экспорта (подсказка скрывается);
    на диске — зеркало иерархии: папка
    категории → `.md` (полный текст + метаданные) + обложка РЯДОМ
    (`name.png/.jpg`, не в подпапке `<title>/`), подкатегории — вложенными
    папками. Методы `exportFolder`,
    `exportMarked`, `writeEntryFlat`, `ensureDirPath`, `loadFulls` (пул `/entry`),
    `relFolder`; новое имя метода `exportMarked` вместо `st.bulkExport` — имя
    DOM-элемента затирало метод (позже и элемент удалён — одна кнопка).
    Чистый JS; `PL_JS_VERSION = "1.34-folder-export"`;
    тесты 244/244 + смоук 80/80 + аудит чист.
28. ✅ v1.36 (§drag→★): **массовый drag-and-drop** — `POST /prompt_library/move_many` + `POST /prompt_library/favorite_many`;
     `card.ondragstart` отправляет `d.ids` массив, `row.ondragstart` — `d.paths`;
     `plDrop` обрабатывает массивы, drop на ★ Избранное (`__fav`) вызывает
     `favorite_many`; `list.ondrop` разрешает drop на `__fav`;
     `PL_JS_VERSION = "1.36-bulk-move"`; тесты 244/244 + аудит 18 роутов.
29. ✅ v1.37 (§37.10): **закрепление папок в проводнике** — `POST /prompt_library/folder_pin`;
     `pinned_folders` в `library.json` через `_load_pinned_folders()`/`_save_pinned_folders()`;
     `_save_db` сохраняет `pinned_folders`; `_pl_list` возвращает `pinned_folders`;
     кнопка 📌/📍 в `folderRow`, `st.pinnedFolders` Set, закреплённые вверху дерева;
     `_pl_folder_delete`, `_pl_folder_delete_many`, `_pl_folder_rename`, `_pl_move_many`
     автоматически обновляют `pinned_folders` (нет «призраков»);
     валидация: `__` префикс запрещён, папка должна существовать;
     `PL_JS_VERSION = "1.36-bulk-move"`; тесты 244/244 + аудит 19 роутов.
30. ✅ v1.37 (§37.11): **выход `category_out`** — `RETURN_TYPES = ("STRING", "STRING")`, `RETURN_NAMES = ("prompt_out", "category_out")`;
     `_sanitize_folder_path(folder)` санитизирует путь: пробелы → `_`, спецсимволы → `_`, служебные ветки → `""`;
     тесты обновлены: 9 проверок переписаны на `res["result"][0]`;
     `PL_JS_VERSION = "1.36-bulk-move"`; тесты 244/244 + аудит 19 роутов.
31. ✅ v1.39 (2026-09-21, аудит):
     **(а) экспорт терял записи с одинаковыми названиями** — имена файлов теперь
     уникальны в пределах папки (`st.uniqueName` + `used` на каждую папку),
     в `.md` пишется `№` записи; на живой базе было 6 групп коллизий = 9 записей;
     **(б) `move_many` заводил категорию-фантом** — условие `if want:` (непустой
     список id) заменено на `if moved_entries:`: устаревший id (запись удалена в
     другой вкладке) больше не создаёт пустую папку навсегда;
     **(в) `favorite_many` по `folder_paths`** берёт поддерево (было точное
     сравнение — бросок папки на ★ Избранное не отмечал записи подпапок);
     **(г) поиск** — серверный запрос с паузой 250 мс (`st.SEARCH_DEBOUNCE`),
     локальный фильтр мгновенный: каждый `/search` читает всю библиотеку под общим
     замком базы (замер: 0.165 с на 18.6 МБ), а `oninput` бил на каждую букву;
     **тесты закрыли слепую зону**: три роута v1.36–v1.37 (`move_many`,
     `favorite_many`, `folder_pin`) не проверял НИ ОДИН набор — добавлены
     песочница (§24) и две фазы смоука; все новые проверки проверены
     фальсификаторами (снятый фикс → красное).
     `PL_JS_VERSION = "1.39-unique-export"`; тесты 272/272 + смоук 83/83 + аудит чист.
32. ✅ v1.40 (2026-09-21): **снимок графа — отдельным файлом** `workflows/{id}.json`,
     в записи только `workflow_file`; `_entry_workflow()` читает сначала inline
     (старые 36 записей не переделываются), потом файл; `/entry` отдаёт то же
     поле `workflow` — браузерная часть не менялась вовсе (кнопка «📥 Воркфлоу»
     и перетаскивание карточки работают как раньше); удаление и обрезка
     `MAX_ENTRIES` убирают файл графа; замена превью переносит граф
     (`_entry_workflow`, иначе карточка теряла бы воркфлоу); если файл записать
     нельзя (правленный вручную id, права) — граф остаётся inline, без потерь.
     Замер на КОПИИ живой базы: 5 новых карточек → база +2.5 КБ (было ~1700 КБ),
     графы — файлами рядом. Тесты 292/292 на момент v1.40 (после v1.41 — 304/304),
     три фальсификатора (inline-запись, `/entry` без досыпки, замена превью без
     графа) — каждый краснит свою проверку.
     `PL_JS_VERSION` не менялся (JS не тронут).
33. ✅ v1.41 (2026-09-21): **`IS_CHANGED` перестал смотреть на `mode`** — проверка,
     добавленная в v1.39 («любое изменение mode требует перевыполнения»), была
     ошибкой: `mode` непустой ВСЕГДА, поэтому метод возвращал `float("nan")` при
     каждом Queue, кэш ComfyUI для ноды не работал вовсе и нода на каждом прогоне
     заново разбирала `library.json` (замер 0.165 с из 18.6 МБ) под общим
     `_DB_LOCK`. Докстрока и §33.3 при этом обещали `None` без подхвата.
     Отдельная проверка не нужна: `mode` — виджет, его значение входит в ключ
     кэша, поэтому переключение режима и так даёт перепрогон. Проверено в
     песочнице (непустой `mode` без подхвата → `None`; `mode` списком → `None`;
     `mode` не мешает NaN при подхвате) и живьём новой пробой (§33.6).
     Там же — живой аудит данных базы (§33.7): он снимает отпечаток базы до и
     после проверок и подтверждает каждую находку повторным чтением, чтобы аудит
     во время правки базы не выдавал артефакты за дефекты (урок v1.40).
      Тесты: 304/304, смоук 83/83, аудит чист; обе живые пробы зелёные.
34. ✅ v1.42 (2026-09-21): **замена превью не оставляет legacy `.jpg` сиротой** — решено: в `_save_preview_upload` и `_save_thumbnail` при успешной записи PNG вызывается `_drop_legacy_preview_file(entry_id)`, удаляющий `previews/{id}.jpg` того же id. Найдено в аудите-двойнике (`tests/_tmp_ghost_probe.py`, сейчас удалён — логика встраивается в постоянный тест секции 19.5). Тесты: 305/305, смоук 83/83, аудит чист. Затем — полный повторный аудит проекта (check.py --strict: провалов 0; живой аудит базы 8/8 — сирот и мусора нет; живая проба кэша зелёная; DOM-замер --panel — ширина стабильна, панель не зажимает). ⚠️ Пустые категории дерева — НЕ мусор: у «Пейзажи» есть подпапка «Аляска» (2 записи), «System Prompt» закреплена вручную («пустая категория» как признак мусора — ложная находка).
35. ✅ v1.43 (2026-09-21): **`category_out` сохраняет пробелы** — `_sanitize_folder_path` перестал заменять пробелы на `_` (по желанию пользователя: в Windows папки с пробелами). Пробел теперь сохраняется и сжимается до одного; спецсимволы (`&`, `!` и т.п.), служебные ветки `__*` и нечитаемое по-прежнему → `_`/`""` на выходе. Пример: `"Мои Пейзажи"` → `"Мои Пейзажи/"` (было `"Мои_Пейзажи/"`). JS-экспорт на диск тронут НЕ был (`st.sanitizeFileName` — отдельная функция для имён файлов). Тесты песочницы (§19.5 + новые на санитизацию).
36. ⏳ v1.44 (2026-09-22): **мультивывод — частично реализован** (базовая структура готова, требуются доработки по UX).
   - **Порядок выходов**: `category_out` (путь категории, индекс 0) → `prompt_out` (стандартный промпт, индекс 1) → `out_2..out_11` (дополнительные, 2..11). `RETURN_NAMES` обновлён, `_output_linked` проверяет output 1.
   - **Скрытые сокеты**: доп. сокеты 2..11 скрыты (`o.hide=true`) по умолчанию, `applyOutSockets()` в конце `onNodeCreated`. **Проблема**: в живом ComfyUI сокеты могут не скрываться до перезагрузки (требуется проверка).
   - **Дроп в «Выходы»**: карточка/папка → слот на ближайшем свободном индексе (`nextOutSlot`); дубли игнорируются; все 10 занято → toast предупреждение. Запрет режимов убран — работает в ЛЮБОМ режиме.
   - **Папка-слот**: дроп папки создаёт слот с `active_id` = текущее выделение. В дереве под «🔌 Выходы» папка-слот показана с иконкой 🔌📁 и кнопкой ✖. Клик по папке в дереве → открывает её карточки в категории «Выходы». Клик по карточке → `active_id` перезаписывается, cache-key меняется → перепрогон. Отключение провода только крестиком ✖ у папки.
   - **Имена проводов**: имя сокета `o.name` НЕ меняется (оставляем `RETURN_NAMES`: `out_2`..`out_11`). Название подключения в `o.title` (tooltip, обрезка 32 символа).
   - **Запрет режимов убран** — подключение работает в ЛЮБОМ режиме.
   - **Проблемы UX (не сделано)**:
     1. Имена проводов в UI не отображаются как название подключения (только tooltip).
     2. Сокеты могут не скрываться сразу при создании ноды (требуется живая проверка).
     3. Выбор в категории «Выходы» не подсвечивает запись в проводнике, не показывает превью, не открывает панель промпта — переходит в папку категории. Требуется: при клике в «Выходы» → подсветка в проводнике, превью слева, панель промпта снизу, БЕЗ смены `selFolder`.
   - **PNG-персистентность**: 5-й элемент `widgets_values` + `widgets_values_named.slots_out` (JSON `{i,kind,id|path,active_id,name}`); гидрация в `onConfigure` из named/позиционного фолбэка; `applyOutSockets` показывает/прячет сокеты, задаёт tooltip.
   - Тесты: Python 330/330 ✅, статический аудит ✅, `check.py` (Python) ✅, sync ✅, push ✅. JS смоук: известная проблема с релоад-штормом в тестовом окружении.


 # 16. Решения по открытым вопросам (приняты в коде)

1. Путь: `folder_paths.get_user_directory()` с фолбэком на `__file__`-родственников. ✅
2. Превью: endpoint `GET /prompt_library/preview?id=`. ✅
3. ★/🗑/папки: прямые POST-endpoints + `reload()`. ✅
4. `widgets_values` = `[mode, selected, save_folder, pickup]` (v1.25; `source` — сокет, в патч не входит; виджет `prompt` удалён). ✅
5. Фронтенд 1.52 игнорирует `computeSize`/`hidden` у multiline-виджетов (проверено на машине пользователя) — больше не боремся, только штатные механизмы. ✅
6. Второй выход не нужен — счётчик в hint. ✅

---

# 17. Текущее состояние и следующий шаг (обновлено 2026-09-22)

Состояние: **v1.44 — мультивывод частично реализован** (Python 330/330 ✅, статический аудит ✅, sync ✅, push ✅).
- Базовая структура мультивывода готова: 12 выходов, категория «🔌 Выходы», папка-слот, скрытые сокеты, PNG-персистентность.
- **Проблемы UX (не сделано, требуют доработки)**:
  1. Имена проводов в UI не обновляются (только tooltip).
  2. Сокеты могут не скрываться сразу при создании ноды.
  3. Выбор в категории «Выходы» не подсвечивает запись в проводнике, не показывает превью, не открывает панель промпта — переходит в папку категории. Требуется: при клике в «Выходы» → подсветка в проводнике, превью слева, панель промпта снизу, БЕЗ смены `selFolder`.
- Python: 330/330 ✅, статический аудит ✅, `check.py` (Python) ✅, sync ✅, push ✅.
- JS смоук: известная проблема с релоад-штормом в тестовом окружении (plLiveStates накапливает ноды). Код работает корректно в живом ComfyUI.

**Следующий шаг**: передача другой модели для доработки UX (§40.2 — 40.4).
- v1.34: **массовый экспорт на диск** (§39) — одна УМНАЯ кнопка «📤 Экспорт»
  в шапке проводника (рядом с «+ Категория»): при активных метках (Ctrl/Shift)
  — отмеченные записи и категории, без меток — текущая выбранная категория
  (и «Всё»/«Избранное»/«Без категории» — вся база); отдельных 📤 на строках
  и в bulk-баре НЕТ; на диске —
  зеркало иерархии (папка категории → файлы + подпапки), обложки рядом
  с `.md`, полные тексты догружаются из `/entry`; резиновый прогресс-бар на
  время экспорта; `PL_JS_VERSION = "1.34-folder-export"`.
- v1.33: **экспорт записи на диск** (§38) — кнопка «💾 Экспортировать»
  в панели книги: системный выбор папки (Chrome/Edge); с обложкой —
  подпапка `<title>/` с `.md` + превью, без обложки — только `.md` в выбранную
  папку (роут `/preview`); `PL_JS_VERSION = "1.33-export-to-folder"`.
- v1.32: **панель свойств не трогает наши виджеты** (§37) — `hideInPanel`
  у `pl_browser` и технических полей; страж `unstickWidth` — страховка;
  `PL_JS_VERSION = "1.32-hide-in-panel"`; в probe — флаг `--audit-writer`.
- v1.30: **закреп карточек** (§36) — поле `pinned`, роут `/pin`, кнопка 📌 только
  в папке, закреплённые вверху папки; `PL_JS_VERSION = "1.30-pin"`.
- v1.29: **название в ручном вводе** (§35) — поле `inputTitle`, `title` в `/add`,
  пусто = авто из текста; `INPUT_H = 202`; `PL_JS_VERSION = "1.29-manual-title"`.
- §34: **probe-инструменты живого замера DOM** — классы-якоря `pl-root`/
  `pl-main`/`pl-scroll`, `tests/_probe_live_dom.py`, `tests/_probe_snippet.js`.
- v1.28: **подхват в каждом прогоне** (§33) — `IS_CHANGED` возвращает NaN при
  непустом `pickup`; `PL_JS_VERSION = "1.28-pickup"`.
- v1.27: **аудит 2026-09-19** (§32) — `_DB_LOCK`, guard `id`, `/search`,
  mute/bypass, двойной клик, гигиена состояния; `PL_JS_VERSION = "1.27-audit"`.
- v1.26: **обложка из видео** (§31) — первый кадр видеофайла прогона (PyAV),
  видео в ручном превью (кадр снимает браузер) и замена обложки существующей
  записи картинкой или видео; `media` следует за текущей обложкой;
  `PL_JS_VERSION = "1.26-media"`.
- v1.24: **одна нода вместо двух** — режимы «📥 Запись / 📤 Выдача / 📤📥 Выдача + запись»
  и автоподхват обложки из прогона (`ui.saved_id` + `executed` → `/attach_preview`)
  без IMAGE-провода (§29). Две ловушки на живой: `this.id` в `onNodeCreated` ещё
  не назначен (`-1`), нода внутри subgraph приходит с префиксом (`"5:12"`).
- v1.25: **подхват финального текста** (§30) — нода-«выдача» отдаёт карточку в
  LLM-цепочку, а её результат сохраняется без провода назад (провод назад = кольцо:
  `pickup` + токен в `ui` + роут `/save_pickup` + текст из `executed.ui.text` или
  виджета узла). `PL_JS_VERSION = "1.25-pickup"`, `BASE_H = 624` (строка подхвата).
- v1.8: вертикальный stretch через `computeLayoutSize` (§22) — контент тянется
  с нодой вниз; `widgets_values`-баг исправлен (был мёртвый персистентность-патч).
- v1.9: restore папки без polling — sync из `onConfigure(info)` + одноразовый
  reconcile (§23); промежуточный retry удалён.
- v1.9.1: служебные ветки `__fav`/`__root` персистятся как есть (§23.4);
  `execute()` режет `__*` в корень.
- v1.10: воркфлоу в карточке (§24) — PNG-превью с чанком workflow + снапшот
  в записи (compact-JSON, кап 2МБ), drag на канвас и кнопка 📥 через confirm;
  lazy-превью (отдача без базы, стабильный кэш-ключ, 304).
- v1.11: Vue stretch — контент тянется за нодой в Nodes 2.0 (§22.9).
- v1.14: live mode switch — ширина ноды при переключении canvas ↔ Vue без F5
  (§22.11, `settleLayout` с одним rAF).
- v1.15: Vue floor 480px — единый `PANES_MIN_H=480` для обоих режимов;
  фикс «ноду можно сжать в ноль» и «новая нода открывается сжатой» (§22.10);
  исправлен вечный repaint (`checkCycle` в `onDrawForeground`, §25.2);
  устойчивость `_load_db` к битому `library.json` (§25.2).
- `computeLayoutSize`: `BASE_H=624` (v1.25), `DETAIL_H=280`, `INPUT_H=202` (v1.29: было 170, +поле названия) как минимумы.
- Detail-панель: `max-height:320px`, `overflow-y:auto`.
- v1.16: двухцветный сокет `image: IMAGE,VIDEO` (§5) — синий тензор и зелёный
  `VideoInput` в один вход; `_extract_frame()` берёт первый кадр видео;
  `_save_thumbnail()` принимает батч/список/uint8/grayscale; тест IMAGE,VIDEO;
  скилл `comfyui-video-socket` в корне бандла.
- v1.20: глобальный дубль по тексту (`_find_text_match`); тост + `⚠️ Уже есть в «…»`;
  фикс краша Queue (`skipped_duplicate: {}`); возврат высоты после закрытия панелей
  (`panelOpened`/`shrinkBack`); bulk-бар в `listHead`; `PL_JS_VERSION = "1.20-dup-warn"`.
- v1.21: **автообновление Library-нод** при записи через Saver (§26) —
  `_broadcast_refresh()` → `PromptServer.instance.send_sync("prompt_library/refresh")` →
  JS `app.api.addEventListener("prompt_library/refresh", ...)` → `reload()`.
  Исправлено: без broadcast Library-нода не обновлялась, пока пользователь не нажмёт
  «новая генерация». Флаг `need_broadcast` гарантирует пересылку ТОЛЬКО при новой записи
  (не при backfill или смене папки).
- v1.22: **аудит 2026-09-18 (§27)** — broadcast из всех мутирующих роутов, стойкая
  подсказка о дубле (`hintSticky`), защита служебного префикса `__`, бэкфилл превью
  у существующей записи, чистка мёртвого кода (`HEAD_CHARS`/`lastFullText`);
  **синхронизация нод одной страницы** (§26.9, реестр `plLiveStates` + `st.apiPost`) и
  снятие слушателей в `onRemoved`; `PL_JS_VERSION = "1.22-page-sync"`.
- v1.23: **панели сжимаются (§28)** — пол `min-height:480px` снят с `tree`/`listContent`
  (в обоих режимах одна и та же flex-раскладка), `main` — `overflow:hidden`; кнопки
  массового удаления перенесены из `listHead` в нижнюю строку `hintRow` (22px,
  `flex-shrink:0`), счётчик сжимается — кнопки не режутся; `PL_JS_VERSION = "1.23-panes-fit"`.

**Закрыто:** проблема «низ не примыкает» (§21.5) решена в §22 — было ограничением
legacy `computeSize`, а не фундаментальным.

**Исправлено в v1.21:** `_broadcast_refresh()` вызывался при любом `dirty=True`
(backfill, создание папки) — пересылал ненужные WebSocket-события всем Library-нодам.
Теперь широковещание происходит ТОЛЬКО при `added=True` (новая запись).
Также убран мёртвый `window.addEventListener` fallback в JS — `window` не получает
ComfyUI WebSocket-события; `app.api` (ComfyApi extends EventTarget) корректно
рассылает `CustomEvent` для зарегистрированных типов через `_registered` set.

Следующий шаг (по решению пользователя):
1. ~~Разбить на две ноды: **Prompt Library** (дерево/поиск/выдача) + **Prompt Saver** (пассивная)~~ — ОТМЕНЕНО в v1.24: нода одна, разделение на Library+Saver больше не нужно (§29).
2. Постраничность списка (по ~20 записей, «дальше/назад»).
3. ~~Воркфлоу inline в `library.json`~~ — СДЕЛАНО в v1.40: снимок вынесен в
   `workflows/{id}.json`, база перестала пухнуть (5 карточек: +2.5 КБ вместо
   ~1700 КБ), старые записи не переделывались (§24.3, §25.3.1).
4. Живая проверка v1.25 (§30): рестарт ComfyUI + Ctrl+F5 (кэш JS). В тулбаре
   должно быть `v1.25-pickup`. Сценарий подхвата: нода в режиме «📤 Выдача» с
   выбранной карточкой, поле «📎 Текст в базу» = узел с финальным текстом
   (`Итоговый Prompt`), Queue — запись в базе с обложкой прогона, кольца нет
   (финальный текст на вход ноды НЕ подаётся).
5. Живая проверка v1.24 (§29, если ещё не проходила): режимы «📥 Запись» /
   «📤📥 Выдача + запись» / «📤 Выдача», обложка без IMAGE-провода, старый граф
   с Saver — как раньше; панель книги, ресайз и массовое удаление — как в §28.

# 18. Sizing DOM-виджета: анализ и решения (2026-09-15)

## 18.1. Три найденные проблемы и их корни

### Проблема 1: Бесконечное вытягивание вниз
**Симптом:** нода растёт без остановки при создании/загрузке.
**Корень:** `computeSize` использовал `root.scrollHeight` — полную высоту DOM-дерева.
У `list` и `tree` стоит `overflow:auto` (визуально 480px), но `scrollHeight`
считает **все** строки: 100 карточек × 200px = 20,000px → нода растягивается.
**Решение:** `computeSize` возвращает фиксированные константы из стейта виджета.

### Проблема 2: Нода «пляшет» при зуме
**Симптом:** при изменении масштаба канваса нода прыгает вверх-вниз.
**Корень:** `computeSize` делил высоту на `plScale()` (зум канваса). При зуме 0.5×
фронтенд получал высоту ×2 → растягивал ноду → `computeSize` вызывался снова →
обратная связь. Дополнительно: `offsetHeight` нестабилен во время layout-пересчётов.
**Решение:** убрано деление на `plScale()`, убран `offsetHeight` — фиксированные константы.

### Проблема 3: Пустое пространство снизу
**Симптом:** после удаления `computeSize` фронтенд даёт DOM-виджету дефолтную
высоту (~420px), которая не совпадает с реальным контентом.
**Корень:** фронтенд 1.52 не может точно вычислить высоту кастомного DOM без подсказки.
**Решение:** `computeSize` с фиксированными константами, вычисленными из реального контента.

## 18.2. Почему другие подходы не работали

| Подход | Почему не работает |
|--------|-------------------|
| `root.scrollHeight` | Считает скрытый контент `overflow:auto` → бесконечный рост |
| `offsetHeight` элементов | Нестабилен при layout-пересчётах (zoom, resize) → обратная связь |
| Деление на `plScale()` | При зуме фронтенд получает разную высоту → «танцы» |
| `fitNode` + `Math.max` | Нода может только расти,never сжиматься → застряет на максимуме |
| CSS-инъекции / таймеры / наблюдатели | Колхоз, ломается при обновлениях фронтенда |
| Override `this.computeSize` на ноде | Фронтенд использует для определения размера ноды → mismatch |

## 18.3. Текущее решение: фиксированные константы + detail max-height

```js
const DETAIL_H = 280; // detail-панель: title + folder + textarea(10rows) + meta + buttons
const BASE_H = 624;   // 596 (v1.24) + pickupRow(22) + gap(6) — см. §18.4
const INPUT_H = 170;  // toggle input area (textarea 4 rows + attach row + save button)

browserWidget.computeSize = (w) => {
    const showDetail = detail && detail.style.display !== "none";
    const showInput = inputVisible;
    return [w || this.size[0], BASE_H + (showDetail ? DETAIL_H : 0) + (showInput ? INPUT_H : 0)];
};
```

**Почему это работает:**
- Константы вычислены один раз из реального контента (см. раздел 18.4)
- `computeSize` детерминирован: одинаковый вход → одинаковый выход
- Нет обратной связи с DOM (ни `offsetHeight`, ни `scrollHeight`)
- Нет зависимости от зума (`plScale` удалён)
- Фронтенд доверяет результату и не пересчитывает

## 18.4. Вычисление констант

Контент `root` (flex column, gap:6px):

| Элемент | Высота | Как вычислено |
|---------|--------|---------------|
| `pickupRow` | 22px | строка подхвата (label + select), `flex-shrink:0` (§30) |
| `inputToggle` | 30px | padding:4px × 2 + font:12px + border:1px × 2 ≈ 30px |
| `toolbar` | 30px | padding:4px × 2 + font:12px + border:1px × 2 ≈ 30px |
| `main` | ≈480px | остаток высоты (flex:1); панели сжимаются (§28) |
| `detail` | 280px | title(28) + folder(28) + textarea(170) + meta(14) + buttons(28) + gaps |
| `hintRow` | 22px | фиксированная строка (подсказка или bulk-бар) |
| gaps (6 шт) | 36px | 6 × 6px = 36px |

**BASE_H** = 596 (v1.24) + 22 (ряд подхвата) + 6 (ещё один gap) = **624px** (без detail и input area)
**С detail** = 624 + 280 = **904px**
**С input area** = 624 + 170 = **794px**
**С detail + input area** = 624 + 280 + 170 = **1074px**

> Актуально на v1.22: `INPUT_H = 170` (v1.19 добавил ряд «📷 Прикрепить превью»);
> v1.29: `INPUT_H = 202` (поле названия ~28px + gap 4px поверх старых 170);
> отдаются эти константы через `computeLayoutSize` (минимум, см. §22), а не `computeSize`.
>
> v1.23 (§28): это МИНИМУМ ноды, а не точная высота панелей. Панели — `flex:1 1 0`
> и берут остаток (`H − chrome`), сколько бы нода ни была: при ноде минимального
> размера остаток ≈ 480px (отсюда и старое значение пола). Раньше пол стоял и на
> панелях — если реального остатка было меньше (номинальный `DETAIL_H=280` <
> фактической высоты панели книги, другой шрифт/зум), контент вылезал за `root`
> и его перекрывал `detail` (рисуется позже). Теперь пол ОДИН — `computeLayoutSize`.

## 18.5. Методология: как решать проблемы с фронтендом

**Правило: не гадать, а читать исходники.**

Источник знаний — минифицированный JS фронтенда:
```
D:\ComfyUI_windows_portable\python_embeded\Lib\site-packages\
comfyui_frontend_package\static\assets\core-*.js
```

Алгоритм:
1. Найти ключевой метод через grep (`computeSize`, `_finalizeWidget`, `addDOMWidget`)
2. Найти штатные примеры (`RerouteNode.computeSize`, CLIP Text Encode, LoadImage)
3. Понять механику ДО написания кода (что возвращает, когда вызывается, как влияет CSS)
4. Применить базовые знания CSS (`flex:1` vs `height`, `overflow:auto` vs `scrollHeight`)
5. Проверить прогоном (Python headless + JS syntax check), а не визуально

## 18.6. Паттерн для других DOM-виджетов ComfyUI

При создании DOM-виджета с `addDOMWidget`:

1. **Определи фиксированные высоты** каждого видимого элемента (padding + font + border)
2. **Запиши константы** в `computeSize` — без `offsetHeight`, `scrollHeight`, `plScale`
3. **Не используй** `fitNode`, таймеры, наблюдатели, CSS-инъекции для sizing
4. **Вызывай `setSize`** только при изменении visible состояния (показ/скрытие панелей)
5. **Не дели на `plScale()`** — canvas-трансформация применяется фронтендом к container div
6. **Не перезаписывай `this.computeSize` на ноде** — ломает border/resize (проверено)

Если контент динамический (список растёт) — используй `overflow:auto` с фиксированной
высотой на контейнере, `computeSize` считай по контейнеру, а не по содержимому.

### Дополнительные паттерны (из Prompt Library)

**Две колонки (list + tree):** одинаковая структура (header 22px + scrollable 480px),
`st.list = listContent` (скроллируемая область, а не обёртка).

**Flex stretch:** `flex:1` на основном контенте + `width:34%; flex-shrink:0` на sidebar.
При пустом main он всё ещё занимает место → sidebar не «улетает».

**Textarea в detail:** `resize:none; rows=10; overflow-y:auto`. Уголок ресайза — нет.
Ограничение высоты через `max-height` на родителе (detail), а не на textarea.

---

# 19. Аудит ошибок (deep audit, 2026-09-15)

## 19.1. Python: isinstance(source, str) — защита от IMAGE-тензора

**Проблема:** `source` объявлен как `("*", {})` — ANY-тип. Если подключить IMAGE
(тензор), `str(source)` вернёт `"<torch.Tensor object at 0x...>"` → мусор в промпте.

**Решение:**
```python
if source is not None and isinstance(source, str):
    incoming = source.strip()
else:
    incoming = ""
```

## 19.2. Python: дедупликация через _add_entry вместо ручного кода

**Проблема:** `execute()` содержал копию логики `_add_entry()` (dedup + insert),
дублируя код. Пропущен параметр `title`.

**Решение:** `execute()` вызывает `_add_entry(entries, incoming, fld)` —
единая точка дедупликации и вставки.

## 19.3. Python: preview сохраняется ПОСЛЕ _add_entry

**Проблема:** `_save_thumbnail(image, entry_id)` требует `entry_id`, но `entry_id`
создаётся внутри `_add_entry()`. Нельзя вызвать `_save_thumbnail` до `_add_entry`.

**Решение:** сначала `_add_entry` возвращает `entry_id`, потом `_save_thumbnail`
сохраняет превью, потом patch обновляет поле `preview` в записи.

## 19.4. JS: sort comparator возвращает 0 при равенстве

**Проблема:** Компараторы `old`, `used`, `new` возвращали только `-1` или `1`,
никогда `0` при равных таймстемпах → недетерминированный порядок (мерцание списка).

**Решение:**
```js
ts(a.created_at) < ts(b.created_at) ? -1 : ts(a.created_at) > ts(b.created_at) ? 1 : 0
```

## 19.5. JS: console.log → console.debug

**Проблема:** Debug-лог `[PromptLibrary] build 20260915-audit` в production-коде
засорял консоль.

**Решение:** заменён на `console.debug`.

## 19.6. Оставлено без изменений (осознанно)

- **Race condition `_load_db()`** — нет file-locking, но `os.replace` атомарен.
  При параллельных HTTP-запросах возможна потеря данных. Приоритет: низкий
  (один пользователь, последовательные запросы).
- **`reload()` молча глотает ошибки** — по design: не спамить toast при старте.
- **Double render в `onConfigure`** — `reload()` + `.then()` вызывают render
  дважды, но это корректно (данные обновляются между вызовами).
- **`BASE_H=436`** — константа, пересчитывается при визуальных изменениях.
- **`_pl_list` без кэша** — полное чтение JSON при каждом запросе. Для <500
  записей приемлемо.

---

# 20. Detail layout: откат и текущее состояние

## 20.1. История попыток

В ходе работы было сделано 10+ попыток исправить перекрытие detail-панели:

| # | Попытка | Результат |
|---|---------|-----------|
| 1 | `syncNodeSize` → `this.computeSize` | Не помогло |
| 2 | Override `browserWidget.computeSize` с `BASE_H=436` | Не помогло |
| 3 | Override `this.computeSize` на ноде | Хуже (border отвалился) |
| 4 | ResizeObserver на `this.element` | Не помогло |
| 5 | CSS: root height:100%, main flex:1, detail flex-shrink:0 | Всё ещё перекрывает |
| 6 | Fixed node 480×520, убрал computeSize хаки | «Низ резиновый» |
| 7 | Root 530×480, resizable=false, hidden prompt | Prompt исчез (нужен!) |
| 8 | Вернул prompt, detail после main | Detail маленький, кнопки не видны |
| 9 | Node 540×680, requestAnimationFrame textarea | Textarea перекрывает save |
| 10 | Node 540×780, убрал textarea хак | Пользователь: «подход не работает» |

## 20.2. Причина провала

Все попытки были **слепыми** — CSS-хаки без понимания того, как фронтенд
реально вызывает `computeSize` для DOM-виджетов. Не было прочитано
соответствующее место в исходниках фронтенда (`core-*.js`).

## 20.3. Решение того момента (откат к 68c5ea8 + фиксы) — ИСТОРИЯ

> Ниже — состояние на v1.8; «фиксированные 480px» с тех пор заменены на
> растяжение со сжимаемыми панелями (§22.3, §28), а виджет `prompt` удалён
> в v1.7 — строки про него оставлены как история.

Откат к коммиту `68c5ea8` (SPECIFICATION.md v1.2: §19 аудит) + фиксы:

- **detail**: `max-height:320px`, `overflow-y:auto`, `flex-shrink:0`
- **dText**: `rows=10`, `resize:none` (убран уголок ресайза), `overflow-y:auto`
- **computeSize**: `BASE_H=596`, `DETAIL_H=280`
- **list/tree выравнивание**: одинаковая структура (header + scrollable 480px)
  - `list` = `listHead`(22px) + `listContent`(480px, `flex:1`)
  - `treeBox` = `treeHead`(22px) + `tree`(480px, `width:34%`)
- **list растяжение**: `flex:1` вместо фиксированной ширины (растягивается при ресайзе)
- **treeBox**: `width:34%; min-width:110px; flex-shrink:0` (не сжимается)
- **mode revert**: при отмене IMAGE-wire disconnect диалога режим возвращается на «📥 Запись»
- **save priority**: виджет `prompt` приоритетнее провода `source` (fallback только если виджет пуст)
- **mode before prompt**: `mode` перед `prompt` в `INPUT_TYPES` (виджет режима над окном промпта)
- **folder switch**: сброс `detailId`, `selWidget.value` и скрытие detail-панели при переключении папки

## 20.4. Что НЕ работает (для будущих агентов)

| Попытка | Почему не работает |
|---------|-------------------|
| `this.computeSize = ...` (override на ноде) | Фронтенд использует `node.computeSize()` для определения размера ноды → mismatch |
| `ResizeObserver` на `this.element` | Нода и виджет — разные DOM-элементы. ResizeObserver на ноде не влияет на виджет |
| `root.style.height = this.size[1] + "px"` | `this.size` = canvas units, не CSS pixels. Прямое присвоение создаёт mismatch |
| `offsetHeight` в `computeSize` | Создаёт обратную связь при zoom/resize (подробно — §18) |
| `scrollHeight` в `computeSize` | Считает скрытый overflow → бесконечный рост |
| Увеличение нативного textarea через rAF | Перекрывает нативные кнопки |

## 20.5. Паттерн для detail-панелей в DOM-виджетах

1. Detail **внутри root** (flex column), после основного контента
2. Основной контейнер = `flex:1; overflow:hidden` (растягивается при ресайзе)
3. List/tree внутри основного = `height:Npx; overflow-y:auto` (фиксированная высота)
4. Detail = `flex-shrink:0; max-height:320px; overflow-y:auto` (не сжимается, прокручивается)
5. `computeSize` = фиксированные константы (BASE_H + DETAIL_H)
6. **Никогда** не перезаписывайте `this.computeSize` на ноде — это ломает border/resize
7. **Никогда** не используй `root height:100%` — только `flex:1` на дочерних элементах

---

# 21. Эксперименты с flex stretch (2026-09-16) — НЕ УДАЛОСЬ

## 21.1. Цель

Сделать так, чтобы контент (list/tree) растягивался вместе с нодой при ресайзе
(как нормальное окно), а не был фиксированных 480px. Низ ноды должен
«примыкать» к контенту.

## 21.2. Попытки и результаты

| # | Попытка | Результат |
|---|---------|-----------|
| 1 | `flex:1` на list/tree вместо `height:480px` | **Feedback loop**: `computeSize` читает `scrollHeight` → DOM заполняет ноду → `scrollHeight` = высота ноды → бесконечный цикл. Нода растёт бесконечно |
| 2 | Offscreen measurement (cloneNode) | **Сломало всё**: clone с `flex:1` создавал бесконечную высоту |
| 3 | Убрал `computeSize` override | Нода失去了 фиксированный размер, content залипает |
| 4 | `resizable=true` + `onResize` clamps | Низ ноды не сжимается — `computeSize` возвращает min height, `onResize` не может уменьшить |
| 5 | Убрал `resizable=true` | `resizable=false` запрещает ресайс вообще — пользователь не может тянуть |
| 6 | Убрал `computeSize` override | Фронтенд даёт дефолтную высоту (~420px), не совпадающую с реальным контентом |

## 21.3. Корневая причина

`computeSize` — единственный способ фронтенду узнать высоту DOM-виджета.
Он читает `scrollHeight` (или должен возвращать фиксированную константу).
Если DOM заполняет ноду через `flex:1` → `scrollHeight` = высота ноды →
`computeSize` возвращает эту высоту → фронтенд устанавливает эту высоту → loop.

**Нельзя одновременно:**
- чтобы контент растягивался с нодой (flex:1)
- и чтобы `computeSize` возвращал детерминированную высоту

Это фундаментальное ограничение архитектуры DOM-виджетов ComfyUI.

## 21.4. Вывод (для будущих агентов)

**НЕ ПОВТОРЯТЬ:**
- `flex:1` на list/tree/content — создаёт feedback loop с `computeSize`
- Offscreen measurement с cloneNode — clone наследует `flex:1`, ломает всё
- Убирание `computeSize` — фронтенд даёт неправильную дефолтную высоту
- `resizable=true` + `onResize` — не может уменьшить ноду меньше `computeSize`
- Любые CSS-хаки для «прилипания» низа — всё создаёт обратную связь

**Единственное стабильное решение:** фиксированные константы в `computeSize`
(`BASE_H=596`, `DETAIL_H=280`, `INPUT_H=130`). Контент прокручивается
внутри фиксированных контейнеров. Нода не «прилипает» к контенту — это
компромисс, но стабильный.

## 21.5. Текущее состояние (архив: решено в §22)

- ~~Низ ноды не примыкает к контенту~~ — решено переходом на `computeLayoutSize`
  (§22): ограничение было у legacy `computeSize`, а не фундаментальным.
- Деталь истории неудачных попыток выше (§21.1–21.4) — не повторять подходы
  с чтением размеров DOM в sizing-методе.

---

# 22. Вертикальный stretch через computeLayoutSize (2026-09-17) — РЕШЕНО

## 22.1. Проблема

При растягивании ноды в сторону контент тянулся, при растягивании вниз —
нет: внутри оставалось пустое пространство, низ не примыкал к контенту.

## 22.2. Корень (найден в исходниках фронтенда, `settingStore-*.js`)

`_arrangeWidgets` имеет две ветки:

- виджет с legacy **`computeSize`** → `computedHeight = computeSize()[1] + 4`
  — **точная** высота, свободное место ноды ему не достаётся;
- виджет с **`computeLayoutSize`** → `{minHeight, maxHeight}` →
  `distributeSpace` отдаёт ему **всё свободное место** (лимит по умолчанию —
  бесконечность). Так работают штатные video/image-превью.

Наш `browserWidget` задавал legacy `computeSize` → 596/876px намертво.
Vue-обёртка `WidgetDOM` (`flex flex-col *:flex-1`) и так растягивает наш
`root` — растягивать было нечего, виджету всегда давали ровно 596.

## 22.3. Решение (не повтор §21)

- `browserWidget.computeSize` **убран**, вместо него
  `browserWidget.computeLayoutSize = () => ({minHeight, minWidth})`,
  где `minHeight = BASE_H + DETAIL_H + INPUT_H` по boolean-стейту
  (попутно починен пропуск `INPUT_H` в старом `computeSize`).
- CSS-цепочка: `root height:100%` → `main flex:1 1 0` →
  `listContent`/`tree`: `flex:1 1 0 + min-height:0` (заполняют окно и сжимаются;
  floor — только `computeLayoutSize`-минимум, см. §28 — панели обязаны
  сжиматься, иначе переполнение перекрывает соседей).
- Почему это не §21: `computeLayoutSize` читает только boolean-стейт
  (`display`-флаги), а не размеры DOM → детерминирован, feedback loop
  невозможен по построению.

## 22.4. Статус

Проверено живьём пользователем 2026-09-17: drag вниз тянет контент, пустоты
нет, зум без «плясок». Принято (canvas-режим).

## 22.6. Двухрежимный layout: Vue (Nodes 2.0) vs canvas (механика из исходников)

- Min width в Vue = `node.style.min-width` инлайн или 225 (`useNodeResize`);
  наш MIN_W через `setSize` туда не доходит → ставим напрямую на `[data-node-id]`
  (в канвасе элемента нет → no-op). Лечит вываливание контента вправо.
- Min height Vue меряет сам (`measureMinContentHeight`: обнуляет высоту, меряет
  контент) — низ примыкает руками: потянуть вниз до упора + сохранить workflow.
  Высокий размер при открытии = сохранённый (эпоха canvas-stretch), не баг.

Факт из исходников: Vue-обёртка `WidgetDOM` (`flex flex-col *:flex-1`) без
высоты — content-sized. Flex-цепочка (§22.3) там нечем ограничить: все карточки
наружу, скролла нет, нода растягивается и не сжимается.

Решение (★ упразднено 2026-09-17, см. §22.9): `st.applyPaneLayout()` по флагу
`Comfy.VueNodes.Enabled` (чтение по документированному Settings API:
`app.extensionManager.setting.get(id)`, фолбэк — `app.ui.settings.getSettingValue`):
~~Vue — фиксированные 480px у `tree`/`listContent` (скролл всегда, как v1.7)~~,
canvas — flex stretch. NB: чтение через `app.ui.settings` напрямую НЕ работает
(молча false) — только через `extensionManager.setting`.
`computeLayoutSize`-минимум общий для обоих режимов.

> Фиксированные 480px в Vue отменены: цепочка высоты в Vue существует (§22.9),
> поэтому там теперь та же flex-раскладка, что в канвасе. Ошибка была в том, что
> решение принималось до находки цепочки. Подписка на смену режима (`.change`)
> висит на `app.ui.settings`, а детект читает `extensionManager.setting` —
> §22.9 «Открытые проблемы» п.2.

## 22.7. Vue-режим: auto-fit через прокси размера (без борьбы с layout)

Исправление прошлой ошибки: `setSize` в Vue РАБОТАЕТ — `node.size` это Proxy,
запись коммитит в layout store (`resizeNode`, source=Canvas), а платформа не
перекоммичивает без изменений (`isSizeEqual`, сходимость встроена). Вывод
«setSize мёртв» был неверен: код hug ни разу не запустился (флаг `_vuePanes`
не присваивался), проверять было нечего. Дёргавшая проба (−120/+120) удалена.

Механика `st.autoFitHeight()` (только Vue; в канвасе stretch): панели
фиксированы → контент не зависит от размера → таргет стабилен. Chrome
калибруется минимумом, зум делится (`ds.scale`), пол из `st.minH()`, deadband
4px, антиспам при несдвинувшейся ноде и том же контенте. Вызывается на
дискретных сменах контента (конец `render()`, тоглы, `onResize` — в Vue он
редок, вреда нет). Таймеров, наблюдателей, скрапинга DOM нет. Смена коннектов
сбрасывает калибровку chrome.

## 22.8. Статус Vue-фита: НЕ работает живьём, изъян найден (2026-09-17)

Пользователь: после рестарта ComfyUI и замены ноды — абсолютно без изменений.
Круговое хождение признано, работа передана другой модели.

Честный разбор изъяна (мой): калибровка `chromeMin` самоблокируется на высокой
ноде. Первая же выборка: `chrome = nodeH − content` включает ВЕСЬ surplus
(нода 2000, контент 900 → chrome=1100) → `target = content + chrome = nodeH` →
deadband «сошлось» → больше никогда не двигается. Минимум, который может только
уменьшаться, никогда не уменьшится: чтобы узнать истинный chrome, нужен момент
примыкания, которого нет. Замкнутый круг.

Факты, установленные достоверно (не терять):
- `node.size` — Proxy, запись коммитит в layout store (`resizeNode`);
  `isSizeEqual` даёт сходимость; `measureMinContentHeight` меряет минимум по
  контенту; min-width = инлайн или 225 (`useNodeResize`).
- Детект Vue через `app.extensionManager.setting.get('Comfy.VueNodes.Enabled')`
  (напрямую `app.ui.settings` — молча false). Панели 480 + скролл в Vue работают.
- Правильное направление: chrome НЕ выводить из (высокая нода − контент);
  нужен независимый замер рамки (шапка ноды напрямую) или константа.
  Канвас-режим при этом не трогать (там stretch работает, проверено). Пол `_vueFloor` из замера оставлен как фолбэк
для минимумов.

Урок: телеметрию проверять на присвоение, прежде чем верить ей.

## 22.9. Vue-режим: растяжение контента — РЕШЕНО фактами из фронтенда (2026-09-17)

Симптом: в Nodes 2.0 при растягивании ноды вниз контент оставался 480px → пустота снизу.

Факты из живого фронтенда (`comfyui_frontend_package/static/assets/settingStore-*.js`
и его **source map** — читать исходники можно прямо из `.map`, `sourcesContent`):

| Что | Код | Следствие |
|-----|-----|-----------|
| `_arrangeWidgets` | `computeLayoutSize()` → `{minHeight, maxHeight}`; `minHeight→minSize`, `maxHeight→prefHeight→maxSize` (`?? Infinity`); `distributeSpace` отдаёт свободное место, пишет `widget.computedHeight` | виджет тянется, если у него есть `computeLayoutSize` |
| то же | `!vueNodesMode && l > t && this.setSize(...)` | в Vue нода НЕ авторастится под контент |
| `useGraphNodeManager.ts` | `hasLayoutSize: typeof widget.computeLayoutSize === 'function'` | наш виджет квалифицируется |
| `useProcessedWidgets.ts` | `gridTemplateRows = shouldExpand(type) \|\| hasLayoutSize ? 'auto' : 'min-content'` | наша строка = `auto` |
| `NodeWidgets.vue` | `flex: gridTemplateRows.includes('auto') ? 1 : undefined` | контейнер виджетов получает `flex:1` |
| `WidgetDOM.vue` | `<div class="flex flex-col *:flex-1">`, своей высоты нет | наш `root` получает `flex:1` → **реальная высота** |
| `NodeContent.vue` | `flex flex-auto grow flex-col` | тело ноды реальной высоты |

Вывод: цепочка высоты в Vue существует. Тянуть контент можно тем же приёмом, что
в канвасе (§22.3) — мешал только `applyPaneLayout`, который пинил `main flex:none`
и панели на 480px (§22.6).

Решение (Vue-ветка `applyPaneLayout`):
- `main`: `flex:1 1 0` + `min-height:0`; `tree`/`listContent`: `flex:1 1 0` +
  `min-height:0` + `height:""` (тянутся по высоте ноды, скролл внутри);
- `main`+`detail` переезжают в `scrollArea` (`flex:1 1 0`, `min-height:0`,
  `overflow-y:auto`), `root` — `overflow:hidden`: переполнение уходит в скролл;
- канвас-ветка: та же flex-раскладка панелей (main/detail — прямые дети root,
  `overflow` уводить некуда, поэтому панели сжимаются — v1.23, §28).

Удалено как тупиковое (не возвращать):
- `autoFitHeight` + `_chromeMin`/`_fitLast` — калибровка chrome самоблокируется (§22.8);
- `calibrateFloor` + `_vueFloor` — в Vue `root` растянут до высоты ноды, поэтому
  `offsetHeight` = текущая высота → минимальная высота запиралась на текущей
  (нода никогда не сжималась). Та же болезнь, что `chromeMin`.

Минимум высоты в обоих режимах — константы `st.minH()` (только boolean-стейт,
без замеров DOM). `maxHeight` НЕ задаём: он уходит в `prefHeight`, а `maxSize`
по умолчанию `Infinity` → `distributeSpace` отдаёт виджету всё свободное место.

Проверка без ComfyUI: смоук-тест `tests/_smoke_prompt_library.mjs` (в папке проекта, §25.1)
исполняет файл в Node с заглушками DOM/LiteGraph и прогоняет жизненный цикл в
ОБОИХ режимах, проверяя flex-цепочку. Ловит `ReferenceError`/`TypeError` в
`onNodeCreated` за секунды — именно так были найдены потерянные `const st` и
`dropAutoSockets`.

## 22.10. Vue: «сжатие в ноль» и смена режима без перезагрузки — РЕШЕНО (2026-09-17)

Оба симптома воспроизведены пользователем живьём и исправлены.

1. **Ноду можно было сжать «в ноль»** (панели исчезали, оставались только
   шапка/тулбар/подвал). Причина: панели в Vue получили `min-height:0` +
   `flex:1 1 0` — это было нужно для скролла, но одновременно убрало ПОЛ.
   Решение: раскладка панелей теперь ОДНА для обоих режимов —
   `flex:1 1 auto` + пол `min-height:480px` (как в канвасе). Пол работает дважды:
   контент тянется при росте ноды И нода не сжимается ниже контента.
   (v1.23, §28: пол `min-height:480px` снят с панелей — он же и перекрывал низ
   списка; минимум ноды остался в `computeLayoutSize`, а панели сжимаются.)
   Отличие Vue-ветки осталось ровно одно: `main`+`detail` лежат в `scrollArea`,
   а `root` их обрезает (`overflow:hidden`) → переполнение скроллится, а не
   вылезает за ноду.

3. **В Vue новая нода открывалась «сжатой»** (видны только шапка, виджеты,
   тулбар и подвал; списка и дерева нет). Механика (подтверждена исходниками):
   - в канвасе фронтенд сам увеличивает ноду под контент —
     `!Z.vueNodesMode && l > t && (this.setSize(...))` в `_arrangeWidgets`;
     в Vue этой ветки нет, поэтому высоту ноды задаёт только **минимальная
     высота контента** (`measureMinContentHeight`);
   - пол `min-height` на панелях в этот замер НЕ попадает: панели лежат внутри
     `scrollArea` с `overflow-y:auto`, а содержимое скролл-контейнера не
     расширяет родителя → минимальная высота = только фиксированные части (~330px).
   Решение: пол перенесён с панелей на сам `scrollArea`
   (`PANES_MIN_H` в `applyPaneLayout`): он попадает в замер и становится
   минимальной высотой ноды — нода открывается с видимой частью списка.
   В Vue панели при этом `flex:1 1 0` + `min-height:0` (заселяют видимое окно,
   свой скролл рассчитан на переполнение карточками). С v1.23 (§28) так же
   и в канвасе (scrollArea там не нужен — сжимающихся панелей достаточно).
   **21:15 (по замечанию живьём):** исходное значение было 280px — позволяло сжать
   ноду заметно сильнее, чем в старых нодах (видны ~1.5 карточки). Теперь пол —
   одна константа `PANES_MIN_H = 480` для ОБОИХ режимов (канвас: панели, Vue:
   scrollArea), т.е. минимальный размер ноды в 2.0 = как в канвасе.

2. **Смена режима canvas ↔ Vue без перезагрузки не применяла раскладку** —
   нода, созданная в канвасе, оставалась с canvas-раскладкой (`overflow` не
   обрезал, пол не совпадал с новой высотой виджета) → контент вылезал наружу.
   Причина: единственным сигналом было событие настроек. Решение — три
   независимых сигнала, все без таймеров и наблюдателей:
   - **основной:** перехват `window.LiteGraph.vueNodesMode` — это тот самый флаг,
     которым фронтенд включает Vue-рендер (`useVueFeatureFlags`:
     `watch(...) → LiteGraph.vueNodesMode`), а `window.LiteGraph` ставит
     `useGlobalLitegraph` (чанк GraphView). Обёртка — один раз на страницу
     (`plHookVueMode`), реестр нод `plModeWatchers`, отписка в `onRemoved`;
   - **страховка:** событие настроек `Comfy.VueNodes.Enabled.change`
     (`scripts/ui/settings.ts`: `dispatchChange` шлёт CustomEvent на
     `app.ui.settings`; новое значение берём из `detail.value` — стор
     обновляется ДО события, см. `applySettingLocally`);
   - **догоняющая сверка:** `render()` сравнивает `isVueNodes()` с `_vuePanes`
     и переприменяет раскладку при расхождении (одно чтение свойства).

Детект режима (`st.isVueNodes`): сначала `window.LiteGraph.vueNodesMode`
(авторитетный флаг), затем `app.extensionManager.setting.get(...)`, затем
`app.ui.settings.getSettingValue(...)`.

Проверено смоук-тестом без ComfyUI: оба режима + живая смена обоими сигналами
+ фолбэк без `window.LiteGraph`.

## 22.11. Vue: живое переключение давало УЗКУЮ ноду — РЕШЕНО (2026-09-17)

**Симптом (живьём, только при переключении в настройках canvas → Nodes 2.0
без перезагрузки):** нода появлялась «сжатой» — узкая рамка, из которой контент
вылезал (список/дерево обрезаны, ширина меньше нормальной). После F5 — нормально.

**Причина.** Ширина Vue-ноды задаётся в `LGraphNode.vue` как
`min-width: var(--min-node-width)` (дефолт 350px). Наши 470px (`MIN_W`) ставит
`st.applyNodeMinWidth` — инлайн-стилем на узел `[data-node-id]`, потому что
`setSize` в Vue ширину контейнера не определяет. Но Vue **монтирует ноду
асинхронно**: в момент смены флага `LiteGraph.vueNodesMode` элемента
`[data-node-id]` в DOM ещё нет → `root.closest("[data-node-id]")` возвращает
`null` → применение в пустоту, нода остаётся на 350px. В reload-пути то же
действие выполняет `requestAnimationFrame` в `onNodeCreated` — уже после
монтирования, поэтому «после F5 всё хорошо» (и это же объясняло прошлый
симптом «нода узкая только при живом переключении»).

**Решение** (только в `st.onModeChange`; без таймеров, наблюдателей и циклов):
`settle(again)` — `applyNodeMinWidth?.()` + `enforceMinWidth?.()` сейчас и
**один** повторный кадр через `requestAnimationFrame` (два кадра: первый — после
монтирования Vue, второй — страховка на случай кадра между флагом и рендером).

**Маркер сборки:** `1.14-vue-switch-width` (`PL_JS_VERSION`, печатается в
консоль при загрузке) — удобно отличать закешированный JS от нового.

Проверено смоук-тестом: после живого переключения флага `nodeEl.style.minWidth
=== "470px"` (заглушка `[data-node-id]` + `HTMLElement`).

## 22.5. Аудит 2026-09-17: баг персистентности (v1.7-регрессия)

Найдено тщательным ревью: в `execute()` остался хвост удалённого виджета —
`widgets_values = [mode, selected, save_folder, prompt]`, где `prompt`
не определён (NameError). `except Exception: pass` глушил ошибку →
`extra_pnginfo`-патч **никогда не применялся**: режим/выбор/папка не
переживали reopen workflow и не сохранялись в PNG (и `onConfigure`-restore
нечего было восстанавливать). Исправлено на `[mode, selected, save_folder]`
(порядок = INPUT_TYPES required), доказано headless-тестом
(`_library_root` → temp, два кейса: patch в write-режиме, выдача + `last_used`
в issue-режиме). Урок: голый `except: pass` вокруг патча прячет регрессии —
при удалении виджетов grep'ать все упоминания имени.

---

# 23. Восстановление папки при открытии: гонка со store hydration (2026-09-17)

## 23.1. Факты (проверены, не гипотезы)

- **Save-сторона в порядке**: в workflow-файле `widgets_values_named.save_folder`
  корректен (проверено в `Krea2_MY2.json`: `"save_folder": "Fs/FAS"`), папка
  есть в `library.json`. Потеря на вкладках без рестарта — тот же load-путь.
- **Load-сторона**: одноразовый restore в `onConfigure` (даже через `rAF` + `fetch`)
  не срабатывал — в момент проверки `save_folder`-виджет ещё пуст.
- **Вывод**: новый фронтенд применяет `widgets_values` к виджетам **асинхронно**
  (widget store hydration) после создания ноды; синхронного уведомления об
  окончании на момент исследования не найдено.
- Режим при этом восстанавливался сам (фронтенд доприменяет позже), а кастомный
  JS-стейт (`selFolder`) — нет: наш restore уже отработал и пропустил.

## 23.2. Решение (без polling): sync-из-info + одноразовый reconcile

Исследование ядра (`LGraphNode.configure`): значения виджетов применяются
синхронно, затем вызывается `onConfigure(info)` — а `info` это сами данные
воркфлоу (`widgets_values` + `widgets_values_named`). Вывод: ждать hydration
виджетов не нужно вообще.

- **Sync-fase** в `onConfigure(info)`: `save_folder` из
  `info.widgets_values_named.save_folder`, фолбэк — `info.widgets_values[2]`
  (порядок INPUT_TYPES required `[mode, selected, save_folder]`, SPEC §10;
  совпадает и со старыми 4-элементными массивами). Сразу `st.selFolder = v` —
  первый рендер после fetch уже правильная папка, кадра «Всё» нет.
- **Reconcile-fase** (один `.then` после `reload()`, без таймеров): виджет
  (если гидрация донесла) побеждает, иначе держим sync-значение, мусор
  (папка удалена из базы) → `"__all"`. Клики всегда синхронизируют виджет
  и `selFolder`, поэтому сверка никогда не спорит с пользователем.
- Попутно: `syncSaveFolder` ищет виджет заново при каждом вызове
  (не кэширует ссылку); `browserWidget.serialize = false` выставлен
  **свойством** (`options.serialize` не пробрасывается — в файле был лишний
  4-й value `pl_browser:""`).
- Промежуточный retry (`st.restoreFolder`, 5×400мс) **удалён полностью**:
  в пути restore ноль `setTimeout`. Оставшиеся в файле `setTimeout` — только
  сбросы подписей кнопок (UX-фидбэк, к restore не относятся).

## 23.3. Критерий профессионального решения (выполнен)

Ни одного `setTimeout`-retry в пути restore; restore не зависит от таймингов
фронтенда (sync из данных воркфлоу); мигание сведено к задержке fetch записей
(неустранимый минимум — список нечем рисовать, пока нет данных).

## 23.4. Служебные ветки тоже персистятся (v1.9.1)

Баг: на «Избранном» (`__fav`) / «Без категории» (`__root`) выбор сбрасывался
на «Всё» — `syncSaveFolder` маппил любой `__*` в `""`, и восстановить было
не из чего (пробел с самого начала, не регрессия).

Решение: виджет несёт выбор как есть (`""` только для `__all`), reconcile
считает валидными `__all`/`__fav`/`__root` + папки из базы. `execute()` на
входе режет `str(folder).startswith("__")` в `""` — новые записи из этих
режимов сохраняются в корень, папка `__fav` в базе невозможна. Старые файлы
(там `""`) открываются как «Всё» — что не было сохранено, не восстановить.

Проверено живьём пользователем 2026-09-17: Избранное и Без категории держатся
при переключении вкладок; Queue из этих режимов пишет в корень. Принято.

---

# 24. Воркфлоу в карточке: drag на канвас открывает граф (v1.10, проверено живьём)

## 24.1. Замысел

ComfyUI сохраняет изображение вместе с воркфлоу и настройками — превью в Library
несёт то же самое: карточка = промпт + превью + воркфлоу, который её создал.
Потянул карточку на канвас (или «📥 Воркфлоу» в панели книги) — открылся граф,
как при дропе PNG с workflow. Искать файлы не нужно: тут и база промптов, и воркфлоу.

**Это ЗАДУМАНО, а не побочный эффект** (подтверждено 2026-09-21): снимок
графа в карточке — и есть смысл карточки, его НЕЛЬЗЯ убирать ради экономии
места или скорости. Тяжелеющая база — цена этой возможности, и решается она
СПОСОБОМ ХРАНЕНИЯ снимка (§25.3.1), а не отказом от него.

## 24.2. Два слоя (оба)

1. **Превью-файл самодостаточен**: PNG 512px с чанком `workflow` (PngInfo) —
   как у SaveImage. Drag из проводника открывает воркфлоу нативно, без нашего кода.
2. **Воркфлоу за записью** (`workflows/{id}.json`, с v1.40 — ФАЙЛОМ, а не inline):
   для drag'а с карточки и кнопки — без файлового round-trip. В `/list` и UI-пакете
   вырезан (тяжёлый), отдаётся флаг `has_workflow`; полный — только в `/entry`
   по клику, и уже оттуда клиент его берёт (форма ответа не изменилась —
   браузерная часть при выносе в файл НЕ правилас).

## 24.3. Захват (Python, `prompt_library_node.py`)

- `execute()`: `_snapshot_workflow(extra_pnginfo)` — deep-copy (json round-trip)
  + кап 2МБ от патологических графов. Снапшот — pre-patch (как было на Queue).
- Минификация (вариант 1+3): `separators=(",", ":")` в снапшоте и чанке —
  данные идентичны (round-trip проверен), экономия ~4% на большом реальном
  графе (352→339 КБ), на pretty-JSON до ~20%. Старые записи — как есть.
- Новая запись: `_attach_workflow()` — граф в `workflows/{id}.json` (компактный
  JSON), в записи — только отметка `workflow_file`; отдельно
  `_save_thumbnail(..., workflow)` → `previews/{id}.png` с чанком.
  Если файл положить не удалось (правленный вручную id вида `bf-broadcast` или
  нет прав на диск) — граф остаётся inline, как до v1.40: лучше старое хранение,
  чем молчаливая потеря воркфлоу.
- **Совместимость (старые записи не переделываем):** `_entry_workflow()` читает
  сначала inline (`entry["workflow"]`), потом файл; `_entry_has_workflow()` —
  дешёвый флаг для `/list` (файлы не открывает). Старые карточки открываются
  как раньше, миграции нет.
- **Удаление/обрезка:** `/delete`, `/delete_many` и `_trim_entries()`
  (`MAX_ENTRIES`) убирают файл графа — сирот в `workflows/` не остаётся.
- **Замена превью** (`/attach_preview`) обязана перенести граф в новую картинку:
  берёт его через `_entry_workflow()` (иначе у карточки пропадал бы воркфлоу —
  ловушка §24.2, закрыта тестом с фальсификатором).
- Дедуп-хит без workflow: backfill + one-time апгрейд JPG→PNG с метаданными
  (`_upgrade_preview_to_png`; старые JPG без workflow остаются как есть —
  не из чего восстановить).
- Ручное сохранение (`/add`) — без workflow (`has_workflow=false`).

## 24.4. Открытие (JS, `prompt_library.js`)

- `app.loadGraphData(workflow)` — штатный API замены графа (проверен в
  `settingStore-*.js`), всегда через confirm (деструктивно).
- Drag: `dragstart` ставит `application/x-pl-entry` (+ `text/plain` для папок);
  capture-хуки `dragover`/`drop` на `app.canvas.canvas`, один раз
  (`window.__plCanvasDropHooked`); чужое не трогаем (проверка типа, иначе return;
  свой — `stopImmediatePropagation`, чтобы файловый хендлер не сработал).
- Кнопка «📥 Воркфлоу» в панели книги (видна при `full.workflow`);
  title-подсказки на карточках; без workflow — warn-toast с объяснением.

---

# 25. Глубокий аудит 2026-09-17: инструменты и найденные дефекты

### 25.1. Чем проверялось (без ComfyUI, воспроизводимо)

| Инструмент | Что ловит |
|---|---|
| `tests/_test_prompt_library.py` (225 проверок) | функциональные дефекты Python: миграция базы, `execute` в трёх режимах, PNG-патч (4 значения), все 15 HTTP-роутов, замок `_DB_LOCK` на мутациях и в `execute()`, guard `id`, полнотекстовый `/search`, одноразовый токен подхвата, видео-обложка и замена превью, traversal, лимиты, broadcast, служебный префикс `__`. Песочница: `folder_paths` → temp, `server`/`aiohttp` — заглушки (роуты регистрируются и вызываются напрямую; `send_sync` — счётчик вызовов). Лежит в `tests/` (AGENTS.md §1.1): код, доки и git — в корне, проверки — в своей папке |
| `tests/_audit_prompt_library.mjs` | связность `st.*` (91 обращение / 8 `this._pl.*`), мёртвые остатки удалённых механизмов, **сверка 15 роутов JS ↔ Python**, покрытие локалей, порядок `widgets_values` |
| `tests/_smoke_prompt_library.mjs` (65 фаз) | падения в жизненном цикле JS, CSS-раскладка canvas/Vue, живая смена режима, подхват текста и обложки, видео-прогон (пулы images/gifs/video), замена обложки, broadcast→reload со снятием слушателя, синхронизация двух нод страницы, отсутствие утечек rAF |

Все три лежат в `Prompt_Library/tests/` (AGENTS.md §1.1) и запускаются из папки проекта:
```bash
cd Prompt_Library
python tests/_test_prompt_library.py
node tests/_smoke_prompt_library.mjs
node tests/_audit_prompt_library.mjs
```
Пути внутри тестов — от самого файла (`Path(__file__).parent.parent`, `new URL("..", import.meta.url)`), поэтому они работают из любой cwd. `sync.py` папку `tests/` в рабочую копию не копирует.

### 25.2. Исправлено по итогам аудита

1. **Битый `library.json` ронял ВСЁ** (Python). Если в `entries` попадал
   не-словарь (правка файла вручную, чужой/старый формат, мусор),
   `_load_db()` падал на `e.get(...)` → 500 на `/list`, `/entry` и ошибка в
   `execute` — нода выглядела полностью нерабочей без шанса самовосстановиться.
   Теперь не-словари и не-строки отбрасываются, база перезаписывается очищенной
   (лог ASCII-only — кириллица в Windows-консоли могла уронить сам лог).
   Проверка: `tests/_test_prompt_library.py`, кейсы “битые записи” и “битый JSON”.
2. **Вечный repaint канваса** (JS). `checkCycle` вызывается из
   `onDrawForeground`, а внутри был безусловный
   `this.graph.setDirtyCanvas(true, true)` → `draw → dirty → draw`: пока нода
   видна, канвас перерисовывается каждый кадр (постоянная нагрузка CPU).
   Теперь перерисовка запрашивается ТОЛЬКО при смене состояния (кольцо
   появилось/снялось); проверки в `onDrawForeground` остались читающими.
   Проверка: новый блок фаз в `tests/_smoke_prompt_library.mjs` (4 assertions).

### 25.3. Найдено, но НЕ исправлено (требует решения)

1. **Масштабирование базы (главный риск) — ЗАКРЫТО в v1.40.** Воркфлоу лежали
   inline в записи: замер 2026-09-21 — `library.json` = **18.6 МБ / 95 записей**,
   из них **36 со снимком — 12.1 МБ** (≈339 КБ на снимок: min 305, max 457 КБ при
   графе в 57–98 узлов, тогда как текст самого промпта — **0.7 КБ**). При
   `MAX_ENTRIES = 500` файл вырос бы до ~180 МБ, а `_load_db()` парсит его
   ЦЕЛИКОМ на каждом запросе (`/list`, `/entry`, `/favorite`, `/update`,
   `/delete`, `/folder_*`, `execute`) — замер: 0.165 с только на `json.loads`,
   около 0.29 с на одну мутацию, и всё это под общим `_DB_LOCK`, под которым
   висит и `execute()`.
   **Решение (2026-09-21):** снимок — ОТДЕЛЬНЫМ файлом `workflows/{id}.json`
   (в записи только `workflow_file`), старые 36 записей с inline-графом НЕ
   переделываются — читаются как есть (§24.3). Замер после правки на КОПИИ
   живой базы: 5 новых карточек дали в базу **+2.5 КБ** (раньше ~1700 КБ на тех
   же графах), файлы графов — рядом, вне базы. Возможность открытия воркфлоу не
   потеряна: `/entry` отдаёт граф из файла, форма ответа и вся браузерная часть
   не менялись (проверено на копии живой базы: старые записи открываются,
   новые — тоже). Что осталось тем же: старые 12 МБ inline в базе — они уйдут
   только вместе с самими записями (миграцию пользователь просил не делать),
   и база всё ещё читается целиком на каждую операцию.
   Альтернатива на будущее: перевод базы на SQLite (при 500+ записях).
2. **Гонки read-modify-write — ЗАКРЫТО в v1.27 (§32.2).** Было: мутации —
   `_load_db()` → правка → `_save_db()` без блокировки, два одновременных цикла
   могли потерять чужое изменение (файл цел — запись атомарна, но изменение
   пропадало молча). Стало: общий `_DB_LOCK` (`threading.RLock`) вокруг цикла:
   POST-роуты через декоратор `_locked`, читающие `list`/`entry`/`search` — через
   `_locked_get` (их `_load_db` тоже пишет при миграции формата), `execute()` —
   обёрткой `execute → _execute`.
3. **Сироты превью при переполнении.** Обрезка `entries[:MAX_ENTRIES]` не удаляет
   PNG выброшенных записей (сейчас сирот 0 из 90 — проявляется только на лимите).
4. **Restore «пустой» папки — снято (v1.9.1/v1.22).** Виджет хранит выбранную
   ветку как есть: `""` = «Всё», `__root` = «Без категории», `__fav` = «Избранное».
   `if (v) st.selFolder = v` восстанавливаети `__root`/`__fav`, а для `""` ветка
   уже `__all` по умолчанию — подсветка верна, править нечего.
5. **Отмена confirm при смене режима в Vue.** `modeW.callback` возвращает
   `modeW.value = "📥 Запись"` при отказе отключить IMAGE-провод. В canvas это
   проверено, в Nodes 2.0 виджет рендерится из стора — нужно живое подтверждение,
   что select визуально возвращается (иначе через API стор-виджета).

---

# 26. Автообновление Library-нод (v1.21)

## 26.1. Проблема

При двух нодах (Saver — запись, Library — выдача) в одном workflow,
создание записи через Saver-нод не обновляло Library-нод автоматически.
Library-нод перечитывал `library.json` только при собственном `execute()`
(который вызывается при входящем сигнале графа). Без связи «Saver → Library»
через граф, Library молчал до нажатия «новая генерация».

## 26.2. Решение: broadcast через WebSocket

```text
Saver execute() → _save_db() → _broadcast_refresh()
  → PromptServer.instance.send_sync("prompt_library/refresh", {})
    → WebSocket → все подключённые клиенты
      → ComfyApi (EventTarget) рассылает CustomEvent(type, {detail})
        → JS: app.api.addEventListener("prompt_library/refresh", ...)
          → this._pl.reload() → GET /prompt_library/list → обновлённый UI
```

## 26.3. Python: `_broadcast_refresh()`

```python
def _broadcast_refresh():
    try:
        from server import PromptServer
        PromptServer.instance.send_sync("prompt_library/refresh", {})
    except Exception:
        pass  # ComfyUI не запущен / тесты — молча игнорируем
```

Вызывается ТОЛЬКО при `need_broadcast=True` (новая запись создана).
Не вызывается при backfill workflow/media или смене папки —
эти изменения не требуют пересоздания записей в Library-нодах.

## 26.4. JS: слушатель в `onNodeCreated`

```js
const plListener = (ev) => {
    try { if (this._pl) this._pl.reload(); } catch (e) { /* silent */ }
};
app.api.addEventListener("prompt_library/refresh", plListener);
```

`app.api` — экземпляр `ComfyApi extends EventTarget`. При получении
WebSocket-сообщения с `type="prompt_library/refresh"`:
- `this._registered.has("prompt_library/refresh")` → `true` (потому что
  `addEventListener` добавляет в `_registered`)
- `super.dispatchEvent(new CustomEvent("prompt_library/refresh", {detail}))`
- JS-слушатель получает `ev` (CustomEvent), вызывает `reload()`

**Почему не `window.addEventListener`:** ComfyUI WebSocket-сообщения
рассылаются через `ComfyApi` (EventTarget), а не через `window.dispatchEvent`.
`window.addEventListener` ловит только DOM-события и никогда не получит
WebSocket-сообщение от ComfyUI. Мёртвый код — убран.

## 26.5. Флаг `need_broadcast`

```python
need_broadcast = False
if not issue and incoming:
    dup = _find_text_match(entries, incoming)
    if dup is not None:
        entry_id, added = dup.get("id"), False
    else:
        entry_id, added = _add_entry(...)
    if added:
        need_broadcast = True   # ← новая запись!
        ...
    elif wf_copy:
        dirty = True   # backfill — broadcast НЕ нужен
```

Гарантирует, что broadcast идёт только при реальном добавлении записи.

## 26.6. Потокобезопасность

`send_sync()` вызывает `self.loop.call_soon_threadsafe(messages.put_nowait, ...)`.
`execute()` работает в worker thread ComfyUI — `call_soon_threadsafe`
потокобезопасен. Если `PromptServer.instance` недоступен (тесты, ранняя
загрузка) — `except Exception: pass` молча проглатывает.

## 26.7. Роуты тоже рассылают (v1.22)

До v1.22 broadcast шёл только из `execute()` — ручные действия (`/add` кнопкой,
`/favorite`, `/update`, `/delete*`, `/folder_*`) обновляли лишь ту ноду, что
инициировала запрос (`await reload()` у вызывающего), а соседние Library-ноды и
вторые вкладки оставались со старым списком. Теперь `_broadcast_refresh()`
вызывается после каждого изменяющего сохранения:

| Роут | Условие рассылки |
|---|---|
| `/add` | только при создании записи (дубль ничего не меняет) |
| `/favorite`, `/update` | запись найдена |
| `/delete`, `/delete_many` | реально что-то удалено |
| `/folder_create`, `/folder_rename`, `/folder_delete`, `/folder_delete_many` | всегда (дерево изменилось) |

`execute()` по-прежнему рассылает ТОЛЬКО при `added=True` (не при backfill и не
при смене папки) — флаг `need_broadcast`.

## 26.8. Снятие слушателя (v1.22)

`app.api.addEventListener("prompt_library/refresh", plListener)` вешается на
глобальный объект (`ComfyApi` живёт всю сессию), поэтому в `onRemoved` слушатель
снимается (`app.api.removeEventListener(..., st.apiListener)`) вместе со
слушателем `Comfy.VueNodes.Enabled.change`. Без снятия удалённая нода не
собиралась GC и продолжала тянуть `/prompt_library/list` на каждый broadcast —
по запросу на каждую выгрузку воркфлоу за сессию.
`ComfyApi.removeEventListener` разворачивает обёртку через `WeakMap`
(`getWrappedListener`), поэтому достаточно передать исходную функцию.

## 26.9. Синхронизация нод ОДНОЙ страницы (v1.22)

WS-сигнал (§26.2) ходит кругом через сервер: между нажатием «🗑» и обновлением
второго экземпляра ноды проходит round-trip, а если сигнал потеряется (старый кэш
JS, переподключение сокета), соседняя нода продолжает показывать удалённые
карточки и папки. Поэтому в JS есть реестр живых нод страницы:

```js
const plLiveStates = new Set();    // каждая нода добавляет сюда свой st
function plRefreshLocal(except) {  // перечитать базу у всех, КРОМЕ инициатора
    for (const s of [...plLiveStates]) if (s !== except) s.reload?.();
}
```

Все изменяющие POST идут через один помощник — забыть уведомление невозможно:

```js
st.apiPost = async (path, payload) => {
    const r = await fetch(path, { method: "POST", ... });
    if (r.ok) plRefreshLocal(st);   // соседи обновляются сразу, не ждут WS
    return r;
};
```

`onRemoved` убирает ноду из реестра (`plLiveStates.delete(st)`) вместе со снятием
слушателей (§26.8). Итог: две ноды в одном графе синхронны мгновенно (удаление
карточек и папок, ★, переименование, DnD, ручное сохранение), а вкладки — через WS.
Проверка: смоук-фазы «удаление карточки обновляет соседнюю ноду» и
«onRemoved убирает ноду из реестра» (реальная кнопка 🗑 карточки + бэкенд-стаб).

---

# 27. Аудит 2026-09-18 (v1.22): найденное и исправленное

## 27.1. Что проверялось

Глубокое ревью Python + JS по коду и по исходникам фронтенда
(`api-DclbNWWy.js`, `comfyui_frontend_package` 1.52.7), плюс прогон
`python tests/_test_prompt_library.py` (101), `node tests/_smoke_prompt_library.mjs` (48),
`node tests/_audit_prompt_library.mjs` (чист).

Подтверждено фактами (не гипотезами):

- `ComfyApi.addEventListener` добавляет тип в `_registered`, а WS-сообщение
  рассылается как `CustomEvent` → подход §26 верный;
- `PromptServer.send_sync` → `loop.call_soon_threadsafe` (потокобезопасно);
- `removeEventListener` разворачивает обёртку через `WeakMap` → снятие работает;
- `graph.clear()`/удаление ноды вызывают `node.onRemoved` → утечка слушателей
  была реальной, а не теоретической;
- `web.FileResponse` отдаёт `Last-Modified`/304 → кэш превью (§8.3) корректен.

## 27.2. Исправлено

| # | Что было | Что стало |
|---|----------|-----------|
| 1 | Ручные сохранения не рассылали broadcast — соседние ноды и вторые вкладки не обновлялись | `_broadcast_refresh()` в `/add`, `/favorite`, `/update`, `/delete*`, `/folder_*` (§26.7) |
| 2 | Слушатели `app.api` и настроек не снимались: утечка + лишние `/list` на каждый broadcast | снятие в `onRemoved` (§26.8) |
| 3 | Предупреждение о дубле после Queue не показывалось (писалось в `hintMsg`, видимый только с открытой панелью; hint мог сказать «Пусто») | стойкое поле `hintSticky`, приоритетное в `renderHint`; сбрасывается кликом по папке/карточке |
| 4 | Категорию можно было назвать `__fav`/`__all`/`__root`: призрак — записи уходят в корень, клик перехватывает служебная ветка, из UI не удалить | серверная защита в `/folder_create` и `/folder_rename` (400); `/add`, `/update`, `execute` режут `__*` в корень через `_storage_folder()` (в т.ч. `" __fav "` с пробелами); в JS — тост вместо запроса |
| 5 | Дубль по тексту не получал превью, даже если пришёл с IMAGE | backfill превью у существующей записи (только если `preview` пуст) |
| 6 | `_snapshot_workflow()` (deep-copy графа) вызывался на каждом `execute()` | только в ветке записи с непустым входящим текстом |
| 7 | Мёртвый код от удалённого виджета `prompt` (`HEAD_CHARS`, `lastFullText`) | удалён |
| 8 | `PL_JS_VERSION` не поднимался, хотя JS менялся в v1.21 → маркер врал | `1.22-broadcast-all` (+ ассерт в смоуке) |
| 9 | Фича v1.21 не покрыта тестами (в Python-стабе не было `send_sync`, в JS-стабе — `addEventListener`) | Python-стаб `send_sync` со счётчиком вызовов (§13), JS-фазы broadcast и снятия слушателя |
| 10 | Две Library-ноды в одном графе не синхронизировали удаления: во второй удалённые карточки/папки оставались на месте | реестр живых нод + единый `st.apiPost` (§26.9): любой изменяющий POST сразу перечитывает базу у соседей; `onRemoved` чистит реестр |

## 27.3. Осталось на решение (не код)

- лимит `MAX_ENTRIES` + inline-воркфлоу в `library.json` (§25.3 п.1);
- ~~гонки read-modify-write (§25.3 п.2)~~ — закрыто в v1.27 (`_DB_LOCK`, §32.2);
- сироты превью при обрезке (§25.3 п.3);
- порядок подразделов §22.11 / §22.5 — хронологический, не числовой;
- сознательное: `/update` не проверяет глобальный дубль по тексту (правка книги
  может свести два текста); записи без метки `media` видны только во «Всё».

# 28. Ловушка: пол на панелях перекрывал низ списка (v1.23, 2026-09-18)

## 28.1. Симптом (живьём)

«При каких-то обстоятельствах удалил часть — и область с кнопками стала
перекрыта»: нижние карточки списка оказывались под панелью книги (detail),
т.е. у них не было видно ни текста/даты, ни ряда кнопок ★/✏️/🗑.
Воспроизводимость «по обстоятельствам» — потому что перекрытие было маргинальным:
на пару десятков пикселей.

## 28.2. Причина (механика из исходников фронтенда)

- Высота DOM-виджета = `computedHeight` (`_arrangeWidgets`): `max(minHeight,
  allocation)`, где `minHeight` — наш `computeLayoutSize()`. Свободное место
  `distributeSpace` отдаёт единственному растягиваемому виджету → `root`
  ВСЕГДА точно равен высоте виджета (`root height:100%`).
- `root` — flex column с `overflow:hidden`; дети: `inputToggle`, `inputArea`,
  `toolbar`, `main`, `detail` (рисуется ПОСЛЕ main), `hintRow`.
- Канвас-ветка `applyPaneLayout` ставила на `tree`/`listContent` пол
  `min-height:480px` (+ `flex-shrink:0` у `list`). Итого минимальная высота
  панелей ≈ 506px, а места при открытой панели книги:
  `minH(876) − chrome(≈87) − detail(реально до 320px)` ≈ 470–490px.
- Дефицит ~20–40px никуда не уходил: серые панели НЕ сжимались, `root` обрезал
  по границе (`overflow:hidden`), но внутри `main` контент выходил за его бокс
  и его перекрывал `detail` — следующий сиблинг, рисуемый поверх.
- Почему `DETAIL_H=280` не спасал: это оценка, а реальная панель книги — до
  `max-height:320px`; плюс высота chrome зависит от шрифта/зума/масштаба
  интерфейса. Ошибка в любую сторону = снова перекрытие (вот откуда
  «при каких-то обстоятельствах»).

## 28.3. Решение

1. **Пол ОДИН — `computeLayoutSize().minHeight`.** С CSS-панелей пол снят:
   `tree`/`listContent` → `flex:1 1 0` + `min-height:0` (как в Vue-ветке),
   `main` → `flex:1 1 0` + `min-height:0` + `overflow:hidden` (страховка на
   случай будущих правок). Нода не может быть меньше `BASE_H` — это гарантирует
   фронтенд (минимум из `computeLayoutSize`), а панели занимают остаток
   (при ноде минимального размера — те же ≈480px, что и раньше).
2. **Обе ветки режима теперь отличает ровно одно:** в Vue `main`+`detail` живут
   в `scrollArea` (переполнение уходит в его скролл), в канвасе — прямые дети
   `root`. Раскладка панелей одинаковая — меньше шансов на расхождение.
3. **Кнопки массового удаления — внизу (как было до v1.20).** Из `listHead`
   переехали в нижнюю строку `hintRow`: подсказка и bulk-бар сменяют друг друга
   на ОДНОМ месте (строка фиксированной высоты 22px, `flex-shrink:0`), поэтому
   появление кнопок не отжимает место у списка (ради этого их и уносили наверх
   в v1.20) и они видны там, где их ждёт пользователь.
4. **Кнопки больше не режутся.** Счётчик — `flex:1 1 auto` + `min-width:0`
   (многоточие), кнопки — `flex-shrink:0`. Раньше счётчик в `listHead`
   (`white-space:nowrap` без `flex`/`min-width`) имел `min-width:auto` = ширину
   текста, поэтому при метке многих записей выдавливал `🗑 Удалить`/`✖` за правый
   край строки, и `overflow:hidden` их обрезал.

## 28.4. Чему это учит (правило)

> Внутри `root` (он всегда ровно высоты виджета + `overflow:hidden`) ни один
> ребёнок не имеет права ставить CSS-пол (min-height/max-height-оценку),
> не помещающуюся в бюджет: дефицит не уходит в скролл, а перекрывается
> следующим сиблингом. Пол — только в `computeLayoutSize`, а панели — сжимаемые.

Строка `hintRow` с фиксированной высотой — часть того же правила: она не может
вырасти при появлении кнопок (v1.20 уже обжигался на этом с подсказкой).

# 29. Одна нода вместо двух: три режима + обложка из прогона (v1.24, 2026-09-18)

## 29.1. Задача

Раньше для работы нужны были ДВЕ ноды: «выдача» перед CLIP (текст в поток) и
«сейвер» после VAE Decode (получает картинку проводом и сохраняет запись с
обложкой). Причина — **кольцо в графе**:

```text
[Library] ──prompt_out──▶ CLIP ──▶ KSampler ──▶ VAE Decode ──image──▶ [Library]
     ▲                                                                     │
     └─────────────── цикл, ComfyUI валидирует граф как DAG ◀─────────────┘
```

Хочется: одна нода и переключатель — сохранять или выдавать.

## 29.2. Что снимает кольцо

Картинка нужна была только как обложка. Провод — не единственный способ её
передать: ComfyUI сам рассылает клиенту список созданных файлов в событии
`executed` (`apiSchema.ts`: `{node, display_node, prompt_id, output}`, где
`output.images = [{filename, subfolder, type}]`, есть также `video/audio/text`).

Поэтому обложка теперь **подтягивается после прогона** (deferred preview):

1. `execute()` сохраняет запись и кладёт в `ui` поле `saved_id` (список: id
   новой записи или существующей, которой не хватает обложки).
2. JS слушает `executed`: от своей ноды — запоминает `prompt_id → saved_id`
   (`st.pendingPreview`), от любого другого узла — первую картинку этого прогона.
3. На `execution_success` — `st.apiPost("/prompt_library/attach_preview",
   {id, filename, subfolder, type})` (упавший/прерванный прогон — ожидание снимается,
   запросов нет).
4. Сервер берёт файл из output/temp (`folder_paths.get_directory_by_type`),
   декодирует (`_load_image_file`) и пишет обложку тем же `_save_thumbnail`
   (512px PNG + встроенный workflow в записи), затем рассылает refresh.

Ожидания чистятся по количеству (≤20), без таймеров. Слушатели снимаются в
`onRemoved` (как §26.8), иначе удалённые ноды продолжали бы дёргать сервер.

**Две ловушки (обе кусались на живой проверке):**

1. **id в `onNodeCreated` НЕ назначен.** Конструктор `LGraphNode` ставит
   `UNASSIGNED_NODE_ID = -1` (LiteGraphGlobal: `node.onNodeCreated?.()` вызывается
   в `createNode` ДО `graph.add`, где присваивается реальный id). Первая версия
   кэшировала `st.nodeId = this.id` → всегда `-1` → рукопожатие не сходилось и
   обложка не прикреплялась. Решение: `st.ownId()` читает `this.id` В МОМЕНТ
   события и отсекает `-1`.
2. **Нода внутри subgraph** приходит с префиксным id (`"5:12"`): сверяем и
   `display_node`, и последний сегмент id.

**Позиция в графе не важна.** Если нода стоит ПОСЛЕ узлов вывода (позиция старого
сейвера), её `saved_id` приходит позже картинок — поэтому первая картинка прогона
сразу кладётся в запас `st.runImages` (`prompt_id → файл`) и подхватывается в
момент прихода `saved_id`. Чистится на `execution_success`/`execution_error`.

**Диагностика на живой (F12, без домыслов):** не найден `saved_id` / в прогоне нет картинок /
сервер отверг файл — в консоль идёт `[PromptLibrary] cover: …` или
`attach_preview failed: <status>`.

## 29.3. Режимы (одна нода)

| Режим | Выход `prompt_out` | Что с базой |
|---|---|---|
| `📥 Запись` | пусто | сохраняет входящий `source` |
| `📤 Выдача` | текст выбранной записи (иначе входящий) | ничего не сохраняет |
| `📤📥 Выдача + запись` | как в выдаче | выдаёт И сохраняет входящий |

`mode` — виджет-строка: старые значения остались валидными. С v1.25 `widgets_values`
= `[mode, selected, save_folder, pickup]` (старые массивы из 3 значений читаются:
`pickup` = `""`), см. §30.

**Подхват тоже подчиняется режиму (v1.38, 2026-09-21).** `pickup` — это ЗАПИСЬ:
сервер отдаёт токен, а клиент по нему создаёт запись и вешает обложку
(`/save_pickup` + `/attach_preview`). Поэтому в «📤 Выдача» токена нет (в `ui` идёт
флаг `pickup_blocked`), а в нижней строке ноды видна причина: «режим ничего не
сохраняет: подхват из узла №N выключен». Сохранять финальный текст в выдающем
прогоне = «📤📥 Выдача + запись». До v1.38 подхват режим игнорировал: каждый Queue в
выдаче плодил запись из узла-источника вместе с превью (живая база: 4 записи со
снапшотом режима «📤 Выдача» и `pickup` = 1622, см. §30.4).

**Совместимость (авто-обход).** До v1.24 «Запись» была сквозной: ноду ставили в
разрыв перед CLIP и архивировали каждый прогон. Поэтому если у `prompt_out` уже
есть провод (проверяем сериализованный граф в `extra_pnginfo` — `_output_linked`),
«Запись» продолжает пропускать текст сквозь и возвращает `mode_notice` — JS
показывает подсказку в нижней строке. Отключить провод — выход станет пустым.

## 29.4. Ограничения и что осталось

- Превью не появится, если прогон не создал ни картинок, ни видео, или если
  запись сохранялась без генерации — тогда ручное `📷 Прикрепить превью` (с v1.26
  и видео: кадр снимает браузер) или IMAGE-провод (остался как опция — он нужен,
  когда картинка не из этого прогона).
- Видео-прогон обложку ДАЁТ: первый кадр снимает PyAV (§31). До v1.26 файл
  попадал в `_load_image_file`, PIL его не открывал — обложка молча не появлялась.
- IMAGE-провод и выход вместе по-прежнему дают кольцо — `checkCycle` подсвечивает
  ноду и тостит, `ensureIssueSafe` предлагает отключить провод при переходе в
  выдающий режим.
- Берётся ПЕРВАЯ картинка прогона (батч/несколько узлов вывода — все равно первая).
- Вторая нода (Saver) больше не нужна, но старые графы с ней работают как есть.

---

# 30. Подхват финального текста из другого узла (v1.25, 2026-09-18)

## 30.1. Симптом и настоящая причина

Живой граф пользователя: нода в режиме `📤 Выдача` отдаёт выбранную карточку в
LLM-цепочку (`PreviewAny → Promt в LLM → Text String (User Prompt) → MASTER STYLE →
Переключатель → Итоговый Prompt`), а финальный текст надо было положить обратно
на вход той же ноды, чтобы он сохранился. ComfyUI отвечает:

> Обнаружены ошибки — Недопустимый рабочий процесс: в рабочем процессе обнаружено
> циклическое соединение узлов

и перечисляет узлы ровно этой цепочки. **Это не баг ноды**: ComfyUI исполняет
только ацикличный граф, а одна нода не может быть одновременно началом и концом
одной цепочки. У IMAGE-провода (§29) была та же природа — именно поэтому раньше
требовались две ноды.

## 30.2. Решение: отложенный подхват (deferred pickup)

1. **Настройка.** `pickup` — скрытый виджет (id узла-источника, `STRING`),
   видимый контрол — DOM-строка `pickupRow` «📎 Текст в базу:» (первая в `root`).
   Список собирается из живого графа: узлы верхнего уровня со `STRING`-выходом или
   строковым виджетом (`text`/`value`/`string`/`prompt`); своя нода, другие
   `PromptLibrary` и mute-узлы не предлагаются.
2. **execute() при включённом подхвате НЕ сохраняет входящий текст** (иначе на каждый
   Queue плодилась бы запись из провода, которого для этого случая и не подключают)
   и возвращает `mode_notice` — «входящий текст не сохраняется, запись берётся из
   узла №N после прогона» (ничего не происходит молча).
   Вместо сохранения — `_pickup_stash()`: снапшот воркфлоу и папка под токеном
   (`_PICKUP`, лимит 20, без таймеров — чистим по количеству), токен уходит в
   `ui.pickup` + `ui.pickup_node`.
3. **JS.** В `executed` ЛЮБОГО узла запоминается `output.text`,
   если он есть (`st.runTexts`; ключ — и полный id, и последний сегмент: нода
   внутри subgraph приходит как `"5:12"`). От своей ноды — `st.pendingPickup`
   (`prompt_id → {token, node, text}`). На `execution_success` (только успех):
   текст = текст прогона → фолбэк на виджет живого графа
   (`st.pickupText`) → `POST /prompt_library/save_pickup {token, text}` →
   обложка этого прогона через тот же `/attach_preview` (§29).
4. **Роут `/prompt_library/save_pickup`.** Токен одноразовый (`_PICKUP.pop`):
   неизвестный/повторный → 400; пустой текст → `skipped: "empty"` (не ошибка);
   дубль по тексту → `duplicate: true`. Запись создаётся с тем же воркфлоу-снапшотом,
   что и автосейв (карточка самодостаточна, §24).

**Почему не проводом:** граф должен остаться DAG, а финальный текст уже есть в
событиях прогона — `PromptKeeper` возвращает `{"ui": {"text": [text]}}`, то есть
любой OUTPUT-узел со строкой виден клиенту в `executed`.

## 30.3. Почему дубль не тостит

Подхват срабатывает на КАЖДЫЙ Queue. Если тост «дубликат не сохранён» показывать и
здесь, повторная генерация того же промпта поднимала бы попап каждый раз — поэтому
для подхвата дубль пишется в нижнюю строку ноды (`hintSticky`) и в F12
(`pickup: дубликат…`), без попапа. Тост оставлен только там, где запись ожидалась
явно (ручная кнопка, автосейв из `execute()`).

## 30.4. Ограничения (сознательные)

- В селекторе — только узлы ВЕРХНЕГО уровня графа (вложенные в subgraph не
  перечисляются; их id вида `"5:12"` всё равно матчатся по последнему сегменту).
- Если узел-источник не исполнился (кэш) и его виджет пуст — сохранять нечего
  (F12: `pickup: узел #N не отдал текст — запись не создана`).
- Если кэширована сама нода (`executed` не приходит) — подхвата нет. **Это и был
  главный дефект (v1.25–v1.27)**: у ноды нет проводов, поэтому ComfyUI кэшировал
  её по виджетам и со второго одинакового Queue `execute()` не вызывался — токена
  не было и запись молча не создавалась («то добавляет, то не добавляет»).
  С v1.28 при непустом `pickup` работает `IS_CHANGED` (§33): нода исполняется в
  каждом прогоне, а если токена всё равно нет (mute/bypass, interrupt) — причина
  показывается в нижней строке ноды, а не исчезает в тишине.
- Неактивные узлы не предлагаются: mute (`mode === 2`, LiteGraph `NEVER`) и bypass
  (`mode === 4`, `BYPASS`) — ровно та пара, по которой фронтенд сам считает узел
  неактивным (`app.ts: isMuted = mode === NEVER || mode === BYPASS`). До v1.27
  фильтровался только `mode === 4`, то есть mute-узел предлагался, хотя исполниться
  не мог (§32.4). Обложку даёт и видео-прогон — первый кадр (v1.26, §31).
- **Подхват — это ЗАПИСЬ, значит он подчиняется режиму (v1.38).** В «📤 Выдача»
  токена нет (`ui.pickup_blocked`), поэтому ни записи, ни обложки не появляется;
  причина видна в нижней строке ноды, а клиент не выдаёт ложное «нода не
  исполнялась (кэш)». Живой симптом до фикса: пользователь переключил режим на
  «📤 Выдача», а библиотека продолжала наполняться — 4 записи (03:31–03:43) с
  текстом узла-источника `#1622`, с превью и со снапшотом графа, где
  `widgets_values[0]` = «📤 Выдача». Для сохранения финального текста в выдающем
  прогоне есть «📤📥 Выдача + запись».
- Список узлов пересобирается при создании ноды, после загрузки воркфлоу
  (`onConfigure`) и перед открытием селектора — без наблюдателей и таймеров.

## 30.5. Диагностика (F12, без догалок)

`pickup saved: <id> (узел #N)` · `pickup: дубликат, запись не создана` ·
`pickup: узел #N не отдал текст — запись не создана` ·
`save_pickup failed: <status> {…}` · `pickup: узел не найден в графе: <id>`.

## 30.6. Правило

Обратную петлю в графе не замыкать проводом: значение, которое появляется ТОЛЬКО
в момент прогона, забирается из его событий (`executed.ui.*`) и отправляется на
сервер вместе с токеном. Всё, что нужно позже (воркфлоу, папка), живёт в токене,
а не в URL и не в памяти клиента.

---

# 31. Медиа-обложка: видео из прогона, ручное видео и замена превью (v1.26, 2026-09-19)

## 31.1. Три жалобы и один корень

1. «Не сохраняет видео превью» — прогон с видео-выходом оставался без обложки.
2. «Нет возможности добавить превью из видео для ручного промпта» — `📷 Прикрепить
   превью` принимал только `image/*`.
3. «Нельзя заменить превью у существующей записи» — обложка ставилась один раз
   (без `force` второй раз не перетиралась, а клиента для этого не было).

**Корень первой:** файл прогон отдаёт нормально — ядро кладёт видео в то же
`images` (`PreviewVideo.as_dict()` → `{"images": [...], "animated": (True,)}`), а
сторонние ноды (VHS и подобные) — в `video`/`gifs`. Падало на сервере:
`_load_image_file` — это PIL, а PIL медиаконтейнер не открывает. Ошибка уходила
в `400 media read failed`, нода писала `attach_preview failed` в F12 — снаружи
выглядело как «видео не сохраняется».

## 31.2. Что сделано

- **Первый кадр видео (Python).** `_load_media_frame()`: сначала `_load_image_file`
  (PIL), при ошибке — `_load_video_frame()` через **PyAV** (`av.open` +
  `decode(video=0)`, лениво: нужен только кадр [0]). PyAV — штатная зависимость
  ComfyUI (её же использует `SaveVideo`/`VideoInput`), новых пакетов нет.
- **Тип обложки.** `_is_video_file()` по расширению (`mp4/webm/mkv/mov/m4v/avi/
  wmv/mpg/mpeg/flv`); анимированные GIF/WEBP остаются картинками — PIL берёт
  первый кадр сам. `media` теперь описывает ТЕКУЩУЮ обложку и обновляется с ней.
- **Файл прогона: все пулы.** JS берёт первый файл из `output.images` →
  `output.video` → `output.gifs` (раньше читался только `images`).
- **Кадр из браузера (preview_data).** `/attach_preview` принимает либо ссылку на
  файл прогона, либо PNG-dataURL кадра от браузера. Так приходит видео с диска
  пользователя (сервер такого файла не видит): `st.readPreviewFile()` рисует первый
  кадр через `<video>`+canvas, без библиотек. Картинка идёт тем же путём, что и
  раньше (canvas-даунскейл 512px).
- **Замена обложки.** В панели книги — кнопка `🖼/🎬 Заменить превью`: она
  **скрыта в режиме просмотра** и появляется только после ✏️ Редактировать (рядом
  с 💾) — случайно нажать её при рассматривании записи нельзя, и она ведёт себя как
  действие изменения, а не просмотра. Файл с диска → `preview_data` + `media` +
  `force: true`. Локальная метка `st.previewStamp` добавляет `&r=` к URL превью —
  без неё браузер показал бы старую картинку из кэша (`t=created_at` не менялся).
- **Подтверждение перед перезаписью.** Обложка хранится ОДНИМ файлом
  `previews/{id}.png`, поэтому замена стирает прежний кадр безвозвратно. Перед
  POST — диалог (`app.extensionManager.dialog.confirm`, фолбэк `confirm()`), в
  котором названо имя записи и что именно будет записано; отказ — обложка не
  трогается, подпись кнопки возвращается к текущему типу (как у удаления записи,
  которое тоже идёт через `confirm`).
- **Режим редактирования не сбрасывается.** После замены обновляются только
  формуляр, подпись кнопки и карточка списка: поля `название/категория/текст`
  остаются редактируемыми, несохранённый текст не теряется (`st.fillMeta`).
- **✖ Отмена в панели книги.** В режиме правки рядом с 💾 есть ✖ — выход без
  сохранения: значения берутся из базы заново (`st.fillDetail`, одна точка правды,
  а не второй ручной сброс полей). Диалог спрашивают только когда правки реально
  есть («зашёл и передумал» — тихий возврат); отказ в диалоге оставляет пользователя
  в режиме правки с введённым текстом. Это закрывает ту же дыру, что и гейт у
  замены обложки: «случайно тронул запись» не должно ничего терять.
- **Ручной промпт** принимает и видео: `accept="image/*,video/*"`, миниатюра
  выбранного файла (`<img>` для картинки, `<video>` для видео), `/add` пишет `media`
  вместе с превью (по метке работают 🎬/📷-бейдж и фильтр «Тип»).

## 31.3. Совместимость и границы

- Старые записи без `media` читаются как раньше (`null`). Ручное фото раньше
  сохранялось без метки — теперь метка ставится, и такая запись видна в фильтре
  «📷 Фото» (осознанное изменение).
- `attach_preview` без `force` по-прежнему не перетирает готовую обложку — в том
  числе когда пришёл `preview_data`.
- Ручная замена у удалённой записи отдаёт `404`; авто-подхват — по-прежнему
  `no_entry` (там это норма: запись могли удалить между Queue и концом прогона).
- Прежняя обложка НЕ архивируется: файл перезаписывается на месте, восстановить
  его нельзя. Поэтому и режим правки, и подтверждение — не косметика, а условие
  того, что запись меняется осознанно.
- Видео декодируется ПЕРВЫМ кадром (как и кадр из провода, §5); выбор кадра
  (середина, обложка из файла) не делаем.
- Браузер не смог прочитать файл (экзотический кодек) — обложки нет, но молчания
  тоже: стойкая подсказка + F12, текст записи сохраняется.
- Файлы больше 8 МБ в `preview_data` не принимаются (тот же лимит, что у ручного
  превью): кадр — PNG 512px, он в лимит укладывается с запасом.

## 31.4. Диагностика (F12)

`media read failed: <причина>` (файл прогона не декодировался) ·
`video frame: PyAV недоступен` / `video frame failed: <причина>` ·
`replace preview failed: <status>` · `preview: не удалось прочитать файл <имя>` ·
`preview_data rejected` (кадр не распознан как картинка).

## 31.5. Проверено без ComfyUI

Синтетический mp4 (PyAV, 3 кадра) → `_load_video_frame` = (48,64,3) uint8 →
`_save_thumbnail` = `previews/*.png`. Тесты: 19 проверок Python (снифф расширений,
видео через PyAV, подсказка `media` важнее расширения, `preview_data`, `force`,
`404` на удалённой записи, воркфлоу в новом превью, `media` из `/add`) и 4 фазы
смоука (пулы `images`/`video`/`gifs`, кадр из файла, гейт замены обложки с подтверждением, отмена правок).

---

# 32. Аудит 2026-09-19 (v1.27): найденное и исправленное

## 32.1. Что проверялось

- Статика: `python -m py_compile`, `node --check`, валидность JSON локалей.
- Прогон: Python **225/225** (было 213), смоук **68 фаз** (было 65), аудит чист;
  рабочая копия в ComfyUI побайтово совпадает с исходником, в ней нет тестов.
- Код целиком: Python (роуты, база, `execute`, токен подхвата), JS (слушатели,
  layout, DOM, гонки, гигиена состояния) + git-состав репозитория.
- Сверка с ИСХОДНИКАМИ фронтенда (`comfyui_frontend_package/static/assets/*.map`
  → `sourcesContent`): `LGraphEventMode` (`globalEnums.ts`) и `app.ts` (`isMuted`).

## 32.2. `_DB_LOCK`: гонки read-modify-write закрыты (§25.3 п.2)

**Симптом.** `execute()` исполняется в потоке ComfyUI, роуты — в event loop;
два цикла «load → mutate → save» могли наложиться (Queue + параллельно нажатая
кнопка). Файл не портился (`os.replace`), но чужое изменение пропадало молча —
созданная вручную запись или удаление.

**Решение.** `_DB_LOCK = threading.RLock()`; `@_locked` для POST-роутов (тело
читается ДО замка, внутри — только sync-код, поэтому цикл непрерываем),
`@_locked_get` для `list`/`entry`/`search` (их `_load_db()` тоже пишет при
миграции формата), `execute()` → обёртка `execute → _execute`.

**Тест.** Владение замком проверяется по факту в момент `_save_db` (а не по
наличию декоратора) — для роутов и для `execute()`.

**Осознанно.** Под замком остаётся и декодирование кадра в `attach_preview`
(десятки мс): инвариант «вся работа с базой внутри одного замка» важнее
микрооптимизации для одного пользователя.

## 32.3. Guard на `id` в `attach_preview`

`entry_id` уходил прямо в имя файла (`previews/{id}.png`) без проверки —
единственная точка записи превью без guard (в `/preview`, `_upgrade_preview_to_png`
и `_remove_preview_file` он есть). Достижимо только с правленым вручную
`library.json`, но теперь: `re.fullmatch(r"[a-zA-Z0-9]+", entry_id)` → иначе
`400 bad id`.

## 32.4. Подхват: mute-узел предлагался

Было `if (n.mode === 4) continue; // mute: не исполнится`. Проверено по исходникам
фронтенда: `LGraphEventMode { ALWAYS:0, ON_EVENT:1, NEVER:2, ON_TRIGGER:3, BYPASS:4 }`,
а `app.ts` считает узел неактивным как `mode === NEVER || mode === BYPASS`.
То есть фильтровался bypass, а mute (2) — нет, хотя §30 обещал обратное.
Стало: `if (n.mode === 2 || n.mode === 4) continue;` + фазы смоука разделены
(mode 4 = bypass, mode 2 = mute).

## 32.5. Полнотекстовый поиск: `GET /prompt_library/search?q=`

`/list` отдаёт `head` ≤120 символов, поэтому слово из середины длинного промпта
не находилось, хотя §8.1 обещает «поиск по названию и тексту». Новый роут отдаёт
ТОЛЬКО `ids` (payload копеечный), клиент объединяет их со своим локальным
фильтром (`st.deepIds`). Без таймеров: устаревший ответ отбрасывается по номеру
запроса (last-wins). Роутов стало 15 (аудит сверяет оба списка).

## 32.6. Защита от двойного клика

`💾 Сохранить промпт`: второй клик стартовал второй POST до ответа первого
(в лучшем случае — ложное «уже есть», в худшем — гонка, см. §32.2).
Флаг `st._saving`; то же для `💾` панели книги (`st._savingEdit`).

## 32.7. Мелкая гигиена

- `plHookVueMode()` патчит `LiteGraph.vueNodesMode` только если это
  data-descriptor: если фронт сделает свойство доступором, обёртка перекрыла бы
  его сеттер (молча живём на событии настроек и сверке в `render()`).
- `st.previewStamp` чистится при удалении записи (карточка и массовое удаление)
  и в `onRemoved`; там же сбрасывается `st.deepIds`.
- `hookCanvasDrop` — слушатель один на страницу и держал замыкание ПЕРВОЙ ноды:
  открытие воркфлоу дропом теперь идёт через состояние живой ноды.

## 32.8. Проверено и ошибкой НЕ является

XSS (`innerHTML` только со статикой и `= ""`) · утечки слушателей (снимаются в
`onRemoved`) · `hidden` + `computeSize` для технических виджетов и `serialize`
свойством, а не опцией · `ui.*` только итерируемое · устойчивость `_load_db`
к битому файлу · traversal-guard'ы в `_resolve_output_file`/`_remove_preview_file`
/`_pl_preview` · EXIF (ручное превью идёт через canvas — браузер сам разворачивает
кадр) · локали только `ru` (норма для RU-узлов, как Prompt_Keeper).

Дополнительно в смоуке поднята точность заглушки DOM: `innerHTML = ""` теперь
реально чистит детей (раньше список «накапливал» карточки между рендерами,
и посчитать их было нельзя).

## 32.9. Осталось на решение

- §25.3 п.1 — ~~воркфлоу inline~~ ЗАКРЫТО в v1.40 (файлы `workflows/{id}.json`).
  Осталось (не срочно): старые 12 МБ inline уйдут только с самими записями,
  и база всё ещё парсится целиком на каждую операцию — потолок решит SQLite.
- §25.3 п.3 — сироты превью при обрезке `MAX_ENTRIES`.
- §25.3 п.5 — отмена confirm при смене режима в Vue (нужна живая проверка).
- Бюджет высоты панели книги: в v1.26b/c добавились две кнопки и ряд переносится
  (`flex-wrap`). Оценка по CSS даёт для режима правки ≈276px против `DETAIL_H = 280`
  — помещается, но нужна живая проверка (если нижняя строка подрежется — поднять
  `DETAIL_H`; читать размеры DOM в sizing по-прежнему нельзя).

## 32.10. Живая проверка v1.27

Рестарт ComfyUI + Ctrl+F5, маркер `1.27-audit`: (а) Queue в режиме «Запись» и
одновременно кнопка «Сохранить промпт» — обе записи на месте; (б) поиск слова из
СЕРЕДИНЫ длинного промпта; (в) mute-узел не виден в `📎 Текст в базу`;
(г) двойной клик по «Сохранить промпт» не двоит запись.

---

# 33. Почему подхват «то добавляет, то не добавляет»: кэш ComfyUI (v1.28, 2026-09-19)

## 33.1. Жалоба и данные

Рабочий процесс `Krea2_MY2`: нода `PromptLibrary #1925` в режиме «Запись»,
`pickup = 1622` («Итоговый Promt»), входы не подключены. Промпт каждый раз новый
(из LLM), но в базу попадает не каждый прогон — зависимости найти не удавалось.

Сопоставление PNG-снимков прогонов (`output/`, в каждом вшит воркфлоу) с записями
`library.json`: **сохранено 8 из 48** прогонов. Пропуски шли не подряд и не были
связаны ни с текстом, ни с временем.

## 33.2. Причина: `execute()` не вызывался

Токен подхвата выдаётся только из `execute()` (§30.2), и он **одноразовый**.
Но у ноды с подхватом **нет проводов**, поэтому ComfyUI кэширует её по значениям
входов: после первого же прогона с тем же набором (`mode` / `selected` /
`save_folder` / `pickup`) ключ кэша совпадает и `execute()` больше не вызывается.

Дальше ключевая деталь: для закэшированной ноды ComfyUI **всё равно пере-рассылает
`executed`** — но с сохранённым `ui` (`asset_enrichment.py: emit_cached_output`
→ `register_cached_outputs(cached.ui, …)`). То есть клиент получает **старый,
уже потраченный токен**, отправляет его в `save_pickup` и получает `400
unknown token`. Записи нет, и до v1.28 у этого не было **ни одного видимого
следа** (только `console.warn` в F12).

Отсюда и «нет зависимости» — цикл повторялся так:

| Прогон | Что происходило | Запись |
|---|---|---|
| 1-й после смены виджета | нода исполняется → свежий токен | ✅ создаётся |
| 2-й и далее (виджеты те же) | нода кэширована → `ui` из кэша → токен уже потрачен → 400 | ❌ тишина |
| после клика по карточке / правки графа / рестарта | кэш инвалидируется → цикл с начала | ✅ один раз |

Виджеты меняются не только руками: `selected` переписывается при выборе карточки
в самой ноде, поэтому «иногда работает» выглядело случайным.

Дубль при этом ни при чём: одинаковый текст действительно не пишется дважды
(§30.3), но в пропущенных прогонах тексты были новые, и записи не появлялись.

## 33.3. Решение

**`IS_CHANGED` (класс-метод)**: с непустым `pickup` возвращает `float("nan")`,
без подхвата — `None`.

```python
@classmethod
def IS_CHANGED(cls, **kwargs):
    if str(kwargs.get("pickup") or "").strip():
        return float("nan")
    return None
```

Почему это работает (проверено по исходникам этой сборки ComfyUI, Python 3.13):

1. `execution.py:83` — при наличии `IS_CHANGED` (`has_is_changed`) значение метода
   попадает в `node["is_changed"] = [NaN]`;
2. `comfy_execution/caching.py:117` — подпись кэша строится как
   `[class_type, is_changed]`;
3. `[NaN] == [NaN]` => **False**, а `hash(float("nan"))` у разных объектов разный
   (bpo-43475) → ключ кэша не совпадает никогда → нода исполняется в каждом Queue.

`None` без подхвата — поведение прежнее (кэш работает как обычно), так что у
нод без подхвата ничего не изменилось.

Побочные следствия (сознательные):

- При включённом подхвате перестаёт кэшироваться **вся ветка за нодой**: узел-
  источник и цепочка до него исполняются каждый Queue. Для этого воркфлоу это
  плюс — текст источника всегда свежий (иначе подхват мог бы унести старое
  значение из виджета); там, где за нодой стоит тяжёлый расчёт с фиксированным
  сидом, это будет видно как «кэш не работает» — цена включённого подхвата.
- Настройка ноды (`pickup`) проверяется по виджету, а не по графу: включил
  подхват — нода форсирует прогон, снял — кэшируется снова.

## 33.4. Диагностика (чтобы это больше не было молчаливым)

Новая строка `IS_CHANGED` убирает причину, а видимость даёт защита от остальных
случаев (нода была mute/bypass, прогон прерван, токен протух):

| Ситуация | Что видно |
|---|---|
| Подхват включён, `executed` не пришёл вообще | нижняя строка ноды: «нода не исполнялась в этом прогоне (кэш) — запись не создана» + `console.warn` |
| `ui` пришёл из кэша (токен потрачен) → 400 | нижняя строка: «сервер отклонил запись (400) — подробности в F12» + `console.warn` |
| Пустой текст у узла-источника | тост (как в §30.5) |
| Дубль | нижняя строка (без тоста, §30.3) |
| Консоль ComfyUI | `[PromptLibrary] save_pickup: token unknown or already used (node was cached? see SPEC section 33)` |

## 33.5. Живая проверка v1.28

Рестарт ComfyUI + Ctrl+F5, маркер `1.28-pickup`: (а) включённый подхват и три
Queue подряд без правок виджетов — все три новых текста в базе (раньше сохранялся
бы только первый); (б) в консоли сервера нет `unknown or expired token`;
(в) снять подхват — нода снова кэшируется.

## 33.6. Живой замер кэша (v1.41): `tests/_probe_live_cache.py`

Песочница видит только возвращаемое значение `IS_CHANGED`. Работает ли кэш на
самом деле, решает ДВИЖОК, поэтому это измеряется на живом ComfyUI — HTTP-пробой,
без браузера и websocket.

**Прибор (валидирован по исходникам этой сборки):**
- `execution.py:770` — движок шлёт сообщение `execution_cached` со списком id
  узлов, чьи выходы взяты из кэша; `execution.py:446` — если запись кэша найдена,
  нода НЕ исполняется (`execute()` выходит сразу);
- сообщения истории — пары `[тип, данные]` (проверено живым дампом); события
  `executing`/`executed` в `status.messages` НЕ попадают, поэтому «исполнена» =
  «отчёт о кэше есть, а нашего id в списке нет»; отчёта нет вовсе — замер помечен
  недействительным (это поломка прибора, а не дефект ноды);
- кэшируются и output-ноды: исключения для `OUTPUT_NODE` в `execute()` нет.

**Сценарий — 5 прогонов одного графа:** холодный (уникальная метка в `save_folder`,
иначе нода приедет закэшированной от прошлого запуска пробы) → повтор без правок
(ждём кэш) → смена `mode` (ждём перепрогон) → непустой `pickup` → его повтор
(ждём перепрогон). Прогоны 4–5 задают в том же замере «красный» случай, поэтому
прибор проверен в обе стороны: вернись проверка `mode` из v1.39 — прогон 2 попал
бы в «исполнена» и проба стала бы красной.

**Безопасность:** режим «📤 Выдача» + пустое `selected` — записи в базу нет
(`save_on = False`, `save_folder` в этом режиме не используется).

**Запуск:** `python tests/_probe_live_cache.py [--url ...] [--json]`; в `check.py`
не входит (нужен запущенный ComfyUI), как и `_probe_live_dom.py`.

**Результат 2026-09-21 (ComfyUI 0.36.0): ЗЕЛЁНОЕ** — «без подхвата закэширована»,
«смена режима переисполняет ноду», «с подхватом исполняется каждый Queue».

## 33.7. Живой аудит данных базы (v1.41): `tests/_probe_live_library.py`

Согласованность РЕАЛЬНЫХ данных (есть ли файл графа у записи, нет ли сирот в
`workflows/` и `previews/`, не врёт ли `has_workflow` в `/list`) головным тестом не
доказывается — там своя временная папка. Но аудит на живой базе сам способен
солгать: пока он читает, база может меняться (пользователь чистит карточки), и
расхождения между двумя чтениями выглядят как дефекты хранения. Аудит v1.40 ровно
на этом и попался.

Поэтому в пробе две защиты:

1. **Отпечаток базы** (хэш `library.json` + имена и размеры файлов `workflows/` и
   `previews/`) — снимается до и после проверок; изменился → вердикт
   «НЕДЕЙСТВИТЕЛЬНО» и код возврата 2, а не список «находок»;
2. **Подтверждение повторным чтением** — каждая провалившаяся проверка
   выполняется второй раз на свежем снимке. Не воспроизвелась — значит это было
   переходное состояние (файл писали в этот момент), и находка **снимается**.
   Две защиты дополняют друг друга: мелькнувший на миллисекунды файл не попадает
   в отпечаток, но снимается повторным чтением; а перезапись `library.json` ловит
   отпечаток.

Проверяет: разбор базы, дубли id, форму и наличие `workflow_file`, наличие
`preview`, сирот в обеих папках, согласованность флага `has_workflow` в `/list`
(та же дыра, что в самоаудите §26), справочно — нормализацию `folder` и
`pinned_folders` ⊆ `folders`. Нужен ли сервер — решает флаг `--no-api`; путь к
базе берётся из `sync.py` (или `--root`). Коды возврата: 0 — зелёное, 1 — есть
подтверждённые находки, 2 — замер недействителен.

**Исполненные фальсификаторы** (2026-09-21, на КОПИЯХ базы, не на живой):
убрали файл графа → 1/КРАСНОЕ; файл мелькает в `previews/` во время замера →
находка снята повторным чтением (0/ЗЕЛЁНОЕ); `library.json` переписывают во время
замера → 2/НЕДЕЙСТВИТЕЛЬНО. Плюс проба нашла дефект в самой себе (в `/list` нет
поля `has_preview` — оно только в ui-пакете `execute`), который был исправлен до
публикации выводов: живой ответ API проверен прежде, чем что-то объявлять.
---

# 34. Живой замер DOM: probe-инструменты (без бампа версии, 2026-09-19)

## 34.1. Зачем

Sizing-глюки в Nodes 2.0 (Vue) не видны ни в смоуке (там заглушки DOM), ни в
статическом аудите: причина живёт в цепочке CSS живого фронтенда, её надо
ИЗМЕРИТЬ, а не угадать. Правило §18.5 («не гадать, а читать исходники»)
расширяется: что не видно в исходниках — мерить в живом браузере.

## 34.2. Что добавлено

- **Стабильные классы-якоря** в `web/js/prompt_library.js` (3 строки, поведения
  ноль): `root → "pl-root/"`, `main → "pl-main/"`, `scrollArea → "pl-scroll/"`.
  CSS по ним не строится — только селекторы для замера. `PL_JS_VERSION` не
  поднимался (`1.28-pickup`): поведение не менялось, маркер не врёт.
- **`tests/_probe_live_dom.py`** — живой замер: поднимает headless Chrome с
  временным профилем, открывает работающий ComfyUI, создаёт ноду `PromptLibrary`
  (или грузит workflow из `user/default/workflows`), читает через CDP ширины
  всей цепочки (`.lg-node` → inner-wrapper → node-widgets → наш root) и состояние
  виджетов. Флаги: `--panel` (открыть панель свойств и замерить снова),
  `--workflow NAME`, `--canvas` (режим канваса вместо Vue), `--resize`
  (растянуть ноду мышью как за угол), `--select ID` (открыть панель книги),
  `--trap` (ловушка на `widget.width`; ⚠️ без выделения ноды бесполезна),
  `--audit-writer` (аудит писателя: выделяет ноду через `canvas.selectNodes`,
  снимает страж, ставит ловушку, открывает панель — §37.9).
  Зависимости — только те, что уже есть в ComfyUI (`websockets`, `aiohttp`).
  Требует запущенный ComfyUI и Chrome; в CI не гоняется.
  ⚠️ Интерпретатор — именно `D:/ComfyUI_windows_portable/python_embeded/python.exe`
  (в системном `python` нет `websockets` → `ModuleNotFoundError`, см. проверенные
  команды ниже); это была ошибка прибора, а не дефект ноды.
- **`tests/_probe_snippet.js`** — сниппет для F12 → Console на машине
  пользователя: читает ИМЕННО залипшую сессию (которую чистый старт не
  воспроизводит), ничего не меняет, только печатает JSON (цепочка родителей,
  CSS-переменные ноды, размеры виджета). Работает и на загруженной ранее версии
  расширения (зависит только от публичных полей LiteGraph, классы `.pl-*` не нужны).

## 34.3. Границы

- Probe-файлы — диагностика, а не тесты: в регулярный прогон
  (`_test_`/`_smoke_`/`_audit_`) не входят, `sync.py` их в рабочую копию не
  копирует (вся папка `tests/` игнорируется).
- Классы-якоря удалять нельзя, пока живы probe-инструменты: сниппет новой версии
  ищет `.pl-root` в первую очередь.

---

# 35. Название в ручном вводе (v1.29, 2026-09-19)

## 35.1. Задача

При ручном сохранении (`➕ Добавить промпт`) название всегда бралось из начала
текста — задать своё было негде (только потом через ✏️ в панели книги).

## 35.2. Что сделано

- В окне ручного ввода над textarea — однострочное поле `inputTitle`
  (плейсхолдер «Название (необязательно)»). Порядок в `inputArea`: название →
  текст → ряд прикрепления превью → `💾 Сохранить промпт`.
- `💾` отправляет `title` в POST `/prompt_library/add` всегда (пустая строка —
  штатно). Сервер: `title.strip() or _auto_title(prompt)` — пусто = начало
  текста, как раньше; заданное — стрип и как есть.
- После успешного сохранения очищаются ОБА поля (текст + название); при дубле —
  оба остаются (как раньше оставался текст: пользователь решает, что править).
- Бэкенд менять не пришлось: `title` в `/add` (`:1057`) и `_add_entry(title=)`
  (`:546`) принимали название с самого начала — дело было только за полем в окне.
- `st.inputTitle` выставлен в объект состояния (рядом с остальными DOM-ссылками) —
  нужно смоук-тесту и будущим фазам.
- Sizing: `INPUT_H = 170 → 202` (поле ~28px + ещё один gap 4px в `inputArea`);
  только boolean-стейт, замеров DOM нет — правило §22.3 соблюдено.

## 35.3. Проверено без ComfyUI

- Python 236/236: `/add` с `title` (стрип, сохранение), `/add` с пустым `title`
  (авто из начала текста).
- Смоук 71/71 (+фаза «v1.29: ручной ввод с названием»): поле есть в окне,
  `title` уходит в POST, после успеха оба поля пусты, пустое название уходит
  пустым (авто — на сервере).
- Аудит чист (новых роутов нет — тот же `/add`).

---

# 36. Закреп карточек вверху папки (v1.30, 2026-09-19)

## 36.1. Задача

Важные книги терялись в длинной папке — нужен закреп, чтобы висели вверху.
Только в папках: во «Всё» / «Избранном» / «Без категории» у записей нет «своего»
места, и всплытие там только запутало бы.

## 36.2. Что сделано

- Поле записи `pinned` (boolean): новые — `false`; старые без поля читаются как
  `false` (`setdefault`, файл зря не переписывается — как `media`).
- Роут POST `/prompt_library/pin` (`{id, pinned}`; без значения — тоггл):
  тот же шаблон, что `/favorite` — `_DB_LOCK`, `_save_db`, `_broadcast_refresh`.
  В список запись попадает как есть (`_strip_entry` режет только `workflow`),
  клиентская проекция `plMap` отдаёт `pinned`.
- Кнопка 📌 на карточке — **только в открытой папке** (там же, где закреп и
  действует): закреплённая — полная непрозрачность + «Открепить», обычная —
  приглушена + «Закрепить вверху папки». Оптимистичный флип → `render()` →
  POST `/pin` → откат при ошибке (как у ★).
- Заголовок закреплённой — с префиксом «📌 » (виден везде как информация, как
  🎬/📷-бейджи; на порядок вне папки не влияет).
- Порядок: в папке закреплённые всплывают поверх выбранного порядка сортировки,
  внутри групп порядок сохраняется (стабильное разбиение после обычной
  сортировки). Во «Всё»/«Избранном»/«Без категории» — обычный порядок.

## 36.3. Проверено без ComfyUI

- Python 244/244 (§23 + `/pin` в broadcast-цикле): `/add` → `pinned is False`,
  `/pin` ставит/снимает/тогглит + broadcast, неизвестный id — тихо и без
  broadcast, legacy-запись без поля читается как `False`.
- Смоук 72/72 (+фаза «v1.30»): в папке 2 кнопки 📌 и закреплённая первая, клик
  шлёт POST `/pin` с новым значением; во «Всё» кнопок нет и порядок обычный.
- Аудит чист (роут `/pin` есть с обеих сторон).

---

# 37. Панель свойств зажимала контент: панель писала в наш `widget.width` (v1.31 → v1.32)

## 37.1. Симптом (живьём, canvas-режим)

Открыть правую панель свойств → весь контент ноды сжимается в узкую колонку
слева. Закрыть панель — контент остаётся зажатым. Потянуть ноду шире — нода
растёт, контент остаётся зажатым. F5 — всё восстанавливается.

## 37.2. Механизм (по исходникам фронтенда из `.map`, подтверждён живьём)

Два факта, и вместе они дают симптом целиком:

1. **Оверлей читает `widget.width` ПРЕЖДЕ ширины ноды.** `DomWidgets.vue →
   updateWidgets()` (висит на `canvas.onDrawForeground`, т.е. пересчёт КАЖДЫЙ
   кадр; в локальном бандле 1.52.7 и в апстриме `main` одинаково):

   ```js
   widgetState.size = [(widget.width ?? posNode.width) - margin * 2, ...]
   ```

2. **Панель свойств пишет это `widget.width`.** Панель рендерит виджеты узла
   через `rightSidePanel/parameters/WidgetItem.vue`:
   `const component = getComponent(widget.type); return component || WidgetLegacy`.
   Для типа, которого нет в реестре компонентов, монтируется
   `WidgetLegacy.vue`, а в его `draw()` есть:

   ```js
   const width = canvasEl.value.parentElement.clientWidth  // ширина ПАНЕЛИ
   ...
   widgetInstance.width = width
   ```

   `widgetInstance` — не копия: это ЖИВОЙ объект виджета ноды
   (`resolveWidgetFromHostNode(hostNode, props.widget.name)`), так что ширина
   панели оседает на нашем `pl_browser`.

Итог: обёртка застывает на «ширине панели минус margin×2», живая ширина ноды в
формуле не участвует. Ресайз меняет `size`, формула читает `width` — контент
стоит; F5 создаёт свежий объект виджета без `width` — всё чинится.

Замеры (совпадение точное, погрешности нет):

| Где | `widget.width` | Обёртка `.dom-widget` |
|---|---|---|
| Живой аудит §37.9 (нода 1000) | 262 → 235 (панель) | **215** = 235 − 2×10 |
| Сниппет у пользователя (нода 1137) | 213 (панель) | **193** = 213 − 2×10 |

## 37.3. Почему наш виджет вообще попадал в панель

- Виджет создаётся как `addDOMWidget("pl_browser", "custom", root, …)` — тип
  **`custom`**. В реестре (`widgetRegistry.ts`, `coreWidgetDefinitions`: button,
  string, int, float, boolean, combo, color, textarea, chart, galleria, markdown,
  legacy, audio*, load3D, …) типа `custom` нет → `getComponent("custom") === null`
  → фолбэк на `WidgetLegacy`.
- Панель фильтрует виджеты по `w.options?.canvasOnly | hidden | hideInPanel |
  advanced` (`rightSidePanel/shared.ts → computedSectionDataList`, он же в
  `TabNormalInputs.vue`). У нашего виджета не было ни одного из них.
- Свойство `widget.hidden` (которым JS прячет технические поля) панель НЕ
  читает — она смотрит `options.hidden`. Поэтому панель показывала и наш
  DOM-виджет, и служебные `selected`/`save_folder`/`pickup` (в живом замере —
  5 строк виджетов вместо одной).

## 37.4. Лечение: не пускать виджеты ноды в панель (v1.32)

- `pl_browser.options.hideInPanel = true` — панель не рендерит наш DOM-виджет,
  `WidgetLegacy` для него не монтируется, писателя нет вообще (а не «пишет —
  сразу сносим»).
- Технические `selected`/`save_folder`/`pickup` — тот же `hideInPanel`: в панели
  они не нужны (ими управляют дерево и список), а лишний рендер виджета в
  панели — лишний шанс, что панель тронет сам виджет. В панели остаётся только
  «Режим».
- `hideInPanel` читают ТОЛЬКО панельные компоненты (`rightSidePanel/shared.ts`,
  `TabNormalInputs.vue`) — DOM-оверлей, рендер в Nodes 2.0 (`WidgetDOM`) и
  сериализация не затронуты. Проверено живьём в обоих режимах (§37.9).
- `canvasOnly` сознательно НЕ используем: его читает ещё и
  `shouldRenderAsVue` (`widgetRegistry.ts`) — в Nodes 2.0 виджет перестал бы
  рендериться как Vue-виджет (у соседней Custom_node_Images это уже ловили).
  `hideInPanel` точечный: только панель.
- Страж `st.unstickWidth` (v1.31) остаётся **страховкой**: во вкладке, открытой
  ДО этой правки, чужое `width` уже лежит на виджете (панель успела записать),
  и страж вычищает его в первый же кадр. Никаких `setDirtyCanvas`/layout он не
  делает — петель нет по построению, `width` не персистится.

## 37.5. Проверено без ComfyUI

- Смоук 74/74 (+фаза «v1.32»): у `pl_browser` и у всех трёх технических полей
  `options.hideInPanel === true`, свой `width` не выставляется; фаза «v1.31»
  (страж сносит чужое `width` в `onDrawForeground`/`onResize`) остаётся.
- Аудит: добавлены статические проверки «pl_browser скрыт из панели
  (hideInPanel)» и «страж unstickWidth на месте» — регресс удаления фикса
  ловится без ComfyUI.
- Python без изменений (244/244).

## 37.6. Живая проверка v1.32 (выполнена 2026-09-20, оба режима)

```bash
python tests/_probe_live_dom.py --audit-writer --canvas --window 1366,768
python tests/_probe_live_dom.py --audit-writer --window 1366,768
```

Ожидание: `hits: 0` (при СНЯТОМ страже — он в аудите отключается намеренно),
`panelWidgetRows: 1` (только «Режим»), `panelMirrors: 0`, обёртка = ширина ноды
(1000 − 2×10 = 980 в канвасе; 976 в Vue — паддинги ноды).

## 37.7. Почему v1.31 не была настоящим фиксом (разбор «костыль или нет», 2026-09-20)

**Вердикт: страж v1.31 — маска, а не фикс.** Он лечил симптом (контент больше
не зажат), но оставлял писателя работать: панель продолжала писать ширину в наш
виджет, а страж сносил её каждый кадр — вечная гонка вместо отсутствия бага.
Ошибочным был не код стража, а ДИАГНОЗ: «писателя в стеке нет, значение —
ископаемое».

Почему прежнее доказательство «писателя нет» не работало:

1. **Живой прогон был невалиден.** `--trap` «выделял» ноду кликом по
   `[data-node-id]`, а такие элементы есть только в Vue-режиме; запускался же он
   в канвас-режиме (как у пользователя). Выделения не происходило, панель
   оставалась без узла, наши виджеты в ней не рендерились — писатель не мог
   сработать физически. `hits: []` был артефактом стенда.
2. **Статический скан искал слишком узко.** Правильный контраргумент к старому
   прогону был рядом: в промежуточной верстке наш собственный `pl_browser` не
   терял `width` (в дампе были значения 262/235 при живой ноде), а в живой
   сессии у пользователя осело 213 при ноде 1137 — нода у пользователя в
   момент замера была ШИРЕ 1000, значит писатель есть. Искать надо было по
   `sourcesContent` .map и по любому имени переменной, а не только по
   `widget.width =` в минифицированном бандле:
   `WidgetLegacy.vue → widgetInstance.width = width`.
3. **«Панель — корреляция» было ошибкой.** Тогл панели на свежих нодах ничего
   не менял именно потому, что панель без выделенного узла (или в legacy-меню)
   просто не рендерит виджеты; стоило выделить ноду — и панель запиcывает ширину
   в первую же секунду (замер §37.9 — два раза: Vue-маунт и `ResizeObserver`).

Честная формула: связка «панель рендерит виджет → виджет пишется панелью →
формула оверлея предпочитает `widget.width` живой ширине ноды» — это и есть баг,
воспроизводимый по шагам, без «ископаемых».

## 37.8. След для проверки другим ИИ (как перепроверить всё с нуля)

### Хроника расследования (по порядку, с factual-опорами)

1. Жалоба + 3 скриншота: панель свойств зажимает контент; закрытие и ресайз не
   лечат; F5 лечит. Режим на скринах неизвестен (выяснилось позже: canvas).
2. Прочитан скилл `comfyui-frontend-sources`, поднят ComfyUI (200 на `:8188`).
3. Прогон `tests/_probe_live_dom.py --panel` (headless Chrome + CDP): панель
   НЕ открывалась (pinia-стор `rightSidePanel` не находился по старому имени).
   Починено: поиск любого стора с `togglePanel` + fallback на кнопку с иконкой
   `lucide--panel-right` + ретрай открытия. Всё это — в коммите v1.31.
4. Прогоны с реально открывающейся панелью (свежая нода 1000×1200; реальный
   воркфлоу `video_ltx2_5_MY`, нода 859×1076; зум 0.76/1.0): до/панель/закрыто
   совпадают до пикселя (976/976/976, 835/835/835). Вывод: панель сама по себе
   на свежих нодах ничего не ломает — состояние копится в живой сессии.
5. Прочитаны исходники фронтенда из `.map` (файлы ниже): найден цикл
   `DomWidgets.updateWidgets` ← `canvas.onDrawForeground` ← формула ширины.
6. Сниппет `tests/_probe_snippet.js` в F12 залипшей сессии пользователя, дважды:
   - замер 1: canvas, зум 0.63, нода `size=[920,1188]`, обёртка 193px;
   - замер 2 (файл расширен полями `overlay`/`wrapper`): `w.width=213`,
     `w.node` живой (`wNodeIsLive=true`, `wNodeWidth=1137.57`),
     `lowQuality=false`, инлайн обёртки `width: 193px` (= 213 − 2×10, margin=10).
7. Поиск писателя (первый проход): `LGraphNode.ts` (только 6 присваиваемых полей
   виджета, ширины нет), `WidgetItem.vue` (нет), ядро `web/extensions` (пусто),
   `custom_nodes/*/web` (пусто), git-история (`-S "browserWidget.width"` — пусто),
   греп по минифицированному бандлу (пусто). Вывод был «писателя нет» — неверный:
   запись лежит в `WidgetLegacy.vue` (`widgetInstance.width = width`), а Vue-сорцы
   доступны только через `sourcesContent` в `.map`.
8. Апстрим: формула есть в `main`; blame строки — февраль 2026 (коммит
   proxy-widget-v2). Локальный фронтенд 1.52.7 установлен 2026-09-13.
9. Страж `st.unstickWidth()` (v1.31) + тесты + синхронизация. Пользователь:
   «работает», затем: «не костыль ли?».
10. Прогон `--trap` (панель ×3, зумы, ресайз): `hits: []`. НО стенд был невалиден:
    в канвас-режиме `[data-node-id]` нет, нода не выделялась, панель наши виджеты
    не рендерила (§37.7 п.1). Ложный вывод «fossil» попал в SPEC — исправлено
    2026-09-20.
11. Повторный аудит (2026-09-20): флаг `--audit-writer` (§37.9) — выделение через
    `canvas.selectNodes`, страж отключён, ловушка на `width`. Результат:
    **hits: 2, значения 262 и 235, стек `WidgetLegacy.draw ← bindWidget` и
    `ResizeObserver.draw`** — писатель найден и активен. Фикс корня в v1.32:
    `options.hideInPanel = true` (панель не рендерит виджеты ноды). Проверено
    живьём в обоих режимах: `hits: 0`, панель показывает только «Режим».

### Ключевые исходники фронтенда (все — из `.map` локального бандла 1.52.7)

| Что | Где |
|---|---|
| Формула ширины обёртки | `src/components/graph/DomWidgets.vue → updateWidgets()` (cadence: `canvas.onDrawForeground`, chain) |
| Позиционер обёртки | `src/components/graph/widgets/DomWidget.vue` (`useAbsolutePosition`, `widget.element.classList.add('h-full','w-full')`) |
| Модель виджета (`width` лишь объявлен, не задаётся) | `src/scripts/domWidget.ts` (`BaseWidget.width: number` — декларация) |
| Присваиваемые полям виджета в ядре | `src/lib/litegraph/src/LGraphNode.ts` (`computedDisabled/computedHeight/label/last_y/value/y` — и всё) |
| Размер ноды Vue vs canvas | `LGraphNode.vue` (`--node-width`), `useNodeResize.ts` (`measureMinContentHeight`) |
| Склад DOM-виджетов | `src/stores/domWidgetStore.ts` (`registerWidget`: `pos/size=[0,0]`) |

### Как перепроверить (команды из папки проекта, ComfyUI запущен)

```bash
python tests/_test_prompt_library.py   # ожидание: ok: 244 | FAIL: 0
node tests/_smoke_prompt_library.mjs   # ожидание: phases ok: 74, SMOKE OK
node tests/_audit_prompt_library.mjs   # ожидание: аудит чист, 16 роутов
# живой аудит писателя (канвас и Vue):
D:/ComfyUI_windows_portable/python_embeded/python.exe tests/_probe_live_dom.py --audit-writer --canvas --window 1366,768
D:/ComfyUI_windows_portable/python_embeded/python.exe tests/_probe_live_dom.py --audit-writer --window 1366,768
# ожидание (v1.32): hits: 0 при СНЯТОМ страже, panelWidgetRows: 1, panelMirrors: 0,
#                   обёртка = ширина ноды − 20 (980 в канвасе)
# на КОДЕ ДО ФИКСА (v1.31): hits: 2, значения 262/235, стек WidgetLegacy
```

Логика проверки для верификатора:
1. Писатель: запустить `--audit-writer` и посмотреть `lastHits[].stack`. Стек
   `WidgetLegacy.draw ← bindWidget` или `ResizeObserver.draw` = писатель —
   панель. Если хитов нет, убедиться, что панель РЕАЛЬНО открылась и отрисовала
   виджеты (`panelWidgetRows`, `panelMirrors`): ноль хитов при закрытой панели
   ничего не доказывает (на этом провалился прогон v1.31).
2. Формула: `DomWidgets.vue` — ширина считается как
   `(widget.width ?? posNode.width) - margin*2` каждый кадр.
3. Фикс: `web/js/prompt_library.js` → `browserWidget.options.hideInPanel = true`
   (+ у трёх технических полей), версия `1.32-hide-in-panel`, смоук-фаза «v1.32»,
   аудит-проверка про hideInPanel.
4. Страж `st.unstickWidth` — на месте как страховка для сессий, заражённых до
   фикса (создание, `onResize`, `onDrawForeground`).

### Что опровергло бы нынешний вердикт (честные границы)

- Панель пишет `width` мимо `hideInPanel` (другая ветка рендера виджетов в
  панели): тогда `panelWidgetRows` после фикса останется 5, а `hits` вырастет —
  видно в `--audit-writer`.
- `hideInPanel` влияет на что-то кроме панели (сломает рендер в Nodes 2.0 или
  сериализацию): ловится живым замером в Vue-режиме (`mode: "vue"`,
  `hasElement: true`, `elementRect.w ≈ ширина ноды − паддинги`) и старым
  набором смоук-фаз про персистентность.
- Зажатие при `widget.width === undefined` — тогда причина не в ширине виджета:
  смотреть инлайн обёртки `.dom-widget` (поле `wrapper` сниппета v2) и цепочку
  ширин вверх (`plRootChain`).

---

# 37.9. Живой аудит писателя: флаг `--audit-writer` (2026-09-20)

Инструмент в `tests/_probe_live_dom.py` (headless Chrome + CDP), запуск из папки
проекта при работающем ComfyUI. Что делает:

1. Ставит канвас-режим (или Vue без `--canvas`) и создаёт ноду PromptLibrary
   1000×1200.
2. **Выделяет ноду штатным API канваса** — `app.canvas.selectNodes([node])`
   (а НЕ кликом по `[data-node-id]`: этих элементов в канвасе нет, на этом
   провалился прогон v1.31).
3. **Отключает страж** `n._pl.unstickWidth = () => {}` — иначе страж снесёт
   саму ловушку после первой записи и аудит ослепнет.
4. Ставит accessor-ловушку на `width` виджета `pl_browser` (каждая запись — в
   массив с `value`, `at` и стеком).
5. Открывает панель свойств НАДЁЖНО — через настройку `Comfy.RightSidePanel.IsOpen`
   (в legacy-меню стор её не открывал; в headless-профиле `Comfy.UseNewMenu`
   может быть `Disabled`), ставит `activeTab = 'parameters'`, крутит кадры.
6. Печатает ФАЗУ 1 (панель открыта): `hits`, `lastHits[].stack`, `widgetWidth`,
   `options`, `panelWidgetRows`, `panelMirrors`, `wrapperW`.
7. Ставит `w.options.hideInPanel = true` (или подтверждает уже стоящий),
   переоткрывает панель и печатает ФАЗУ 2 — прирост `hits` и `panelWidgetRows`
   показывают, что фикс работает. На коде до фикса это дало разные значения
   (было 5 строк виджетов → 1, зеркал 1 → 0, прирост хитов 0), на коде v1.32
   хитов нет вообще.

Читать вывод так: `hits: 0` И `panelMirrors: 0` И `panelWidgetRows: 1` — фикс
работает; `hits > 0` со стеком в `WidgetLegacy` — панель снова дорвалась до
виджета.

---
## 37.10. Закрепление папок в проводнике (v1.37, 2026-09-20)

Добавлен `POST /prompt_library/folder_pin` для закрепления/открепления
папок в дереве категорий. Закреплённые папки отображаются вверху дерева
(перед незакреплёнными).

**Схема:** `library.json` содержит поле `pinned_folders: [...]` — список
путей к закреплённым папкам. Хелперы `_load_pinned_folders()` / `_save_pinned_folders()`
работают с этим полем. `_save_db()` всегда сохраняет `pinned_folders`.

**Валидация:** `__` префикс запрещён, папка должна существовать в `folders`.

**Автоочистка:** при удалении (`folder_delete`, `folder_delete_many`),
переименовании (`folder_rename`) и перемещении (`move_many`) папок
`pinned_folders` автоматически обновляется — нет «призраков» закреплённых путей.

**Фронтенд:** кнопка 📌/📍 в `folderRow`, `st.pinnedFolders` Set,
сортировка в `renderTree`.

**Тесты:** Python 244/244, аудит 19 роутов.
n## 37.11. Выход `category_out` (v1.37, 2026-09-20)

Добавлен второй выход ноды — санитизированный путь выбранной категории.
`RETURN_TYPES = ("STRING", "STRING")`, `RETURN_NAMES = ("prompt_out", "category_out")`.

**Санитизация** (`_sanitize_folder_path`):
- Пробелы → сохраняются (v1.43; до этого → `_`), повторы сжимаются до одного
- Спецсимволы (`!@#$%` и т.д.) → `_`
- Кириллица и не-ASCII буквы → сохраняются
- Множественные `_` сжимаются до одного
- Служебные ветки (`__all`, `__fav`, `__root`) → `""`
- Бэкслэши → конвертируются в `/`

**Примеры:**
- `"Пейзажи"` → `"Пейзажи/"`
- `"Пейзажи/Аляска"` → `"Пейзажи/Аляска/"`
- `"Мои Пейзажи"` → `"Мои Пейзажи/"`
- `"Горы&Озёра"` → `"Горы_Озёра/"`
- `"Аляска   Западная"` → `"Аляска Западная/"`
- `"__fav"` → `""`

**Тесты:** 9 проверок обновлены на `res["result"][0]`.



# 39. Массовый экспорт на диск: категории и отмеченное (2026-09-20, v1.34)

## 39.1. Задача

Экспортировать из ноды **пачками**, а не по одной записи:
1. **Категория целиком** (с подкатегориями) — без меток.
2. **Отмеченное** — помеченные записи и содержимое помеченных категорий.

Обе возможности даёт **одна умная кнопка «📤 Экспорт» в шапке проводника**:
с метками (Ctrl/Shift) экспортирует отмеченное, без меток — текущую
выбранную категорию (или «Всё»/«Избранное»/«Без категории» — вся база).
Отдельных 📤 на строках дерева и в bulk-баре НЕТ (первоначально были —
шум, переработано по фидбеку).

## 39.2. Что сделано

Полностью на JS, новых Python-роутов/данных не нужно (список `list` + роуты
`entry`/`preview` уже есть).

Новые методы в `web/js/prompt_library.js` (после `st.exportEntry`):
- **`st.exportFolder(pathKey)`** — выбор папки (`showDirectoryPicker`,
  Chrome/Edge) и экспорт ветки:
  - `__all` — вся база, `__fav` — избранные, `__root` — записи без категории,
    иначе — папка + все вложенные (`e.folder === p || e.folder.startsWith(p + "/")`).
  - Пустая ветка → стойкая подсказка, диалог НЕ открывается.
  - Отмена диалога (`AbortError`) — тихий выход.
  - Нет API → стойкая подсказка «нужен Chrome или Edge».
  - Для реальной папки в выбранной директории создаётся **папка по её имени**
    (`getDirectoryHandle(sanitizeFileName(leaf), {create:true})`), внутрь пишется
    зеркало иерархии; для `__*` — прямо в выбранную директорию (подпапку категории
    не плодим).
  - Итог — стойкая строка: `Готово: N из M` / `Экспортировано: N записей`,
    при сбоях — имена до 5 записей.
- **`st.exportMarked()`** — экспорт записей из `st.markEntries` +
  содержимого папок из `st.markFolders` (как в `exportFolder`, вместе с подпапками);
  без помеченного — тихо, без диалога. Вызывается умной кнопкой в шапке
  (см. «Кнопки» ниже).
- **`st.writeEntryFlat(writeDir, full, used)`** — запись `.md` + обложки рядом
  (НЕ в подпапку `<title>/`, как у одиночного v1.33). Имя — `sanitizeFileName`,
  обложка — `full.preview` роутом `/preview`, расширение по `blob.type`
  (JPEG → `.jpg`), превью нет — только `.md`.
  **v1.39: имена уникальны в пределах папки экспорта** (`used` — набор занятых
  имён на каждую папку, `st.uniqueName` даёт второму и следующим хвост ` (2)`,
  ` (3)`…). До фикса два разных промпта с одинаковым авто-названием (первые 60
  символов) писались в ОДИН файл: `getFileHandle(..., {create:true})` молча
  перетирал первый. Замер на живой базе: 95 записей → 86 уникальных имён, 6 групп
  коллизий = **9 записей, которые «Экспорт всего» терял**. Проверка — фаза
  смоука «v1.39: три записи с одним названием = три файла» (с фальсификатором:
  снятый `uniqueName` → фаза краснеет).
  **v1.39: в `.md` пишется `№` записи** (`st._entryToMd`) — иначе по файлу
  на диске нельзя понять, какая карточка в него легла, и потерю нечем доказать.
- **`st.ensureDirPath(dirHandle, relPath)`** — последовательные
  `getDirectoryHandle(segment, {create:true})` для подпапок зеркала.
- **`st.loadFulls(ids, limit=6)`** — пул параллельных `fetch /entry`, возвращает
  `Map(id → полная запись)`. Нужен потому, что `plMap` из `/list` кладёт в
  `st.entries` только `head` (120 символов) и флаг `has_preview` — полного
  текста/пути превью в списке нет. Если полная запись не пришла — она **не
  пишется** (считается ошибкой, имя попадает в итог).
- **`st.relFolder(folder, pathKey)`** — хвост пути относительно экспортируемой
  папки (для `__*` — весь путь от корня).

Кнопки:
- «📤 Экспорт» в **заголовке проводника** (`treeHead`, рядом с «+ Категория») —
  **одна умная кнопка** на всё дерево: отдельных 📤 на строках и в bulk-баре
  НЕТ (первоначально были на строку и в bulk-баре — шум; переработано).
  Поведение (`onclick`):
  - **если активны метки** (Ctrl/Shift-выделение: `markEntries` + `markFolders`)
    → `st.exportMarked()` — экспорт именно отмеченного (записи + содержимое
    помеченных категорий);
  - **без меток** → `st.exportFolder(st.selFolder || "__all")` — текущая
    выбранная категория: «Всё» → вся база, «Избранное» → избранные,
    «Без категории» → записи без папки, реальная папка → категория
    с подкатегориями.
  Возвращает Promise (потому что `onclick`); тест ждёт его `await`.
- **Прогресс-бар массового экспорта:** в нижней строке (hintRow) полоса
  `progTrack`/`progFill` — скрыта (`display:none`) по умолчанию, показывается
  на время экспорта и заполняется с каждой записью (`st.setExportProgress(show,
  done, total)`); **резиновый** — `flex:1 1 0` в flex-строке, растягивается
  на всю свободную ширину вместе с нодой (как flex-дерево; размеров DOM не
  читает, feedback loop невозможен), высота 12px в фиксированной 22px-строке —
  высоту ноды не двигает. Во время показа подсказка `hint` скрывается
  (`display:none`), бар занимает всю строку; по завершении подсказка
  возвращается с итогом («Экспортировано: N записей» или «Готово: N из M,
  не удались: …»).
- Пойманная ошибка разработки: имя DOM-элемента `bulkExport` совпало с методом
  `st.bulkExport` — метод затирал элемент, `renderHint` падал. Метод переименован
  в **`st.exportMarked`** (сам элемент позже удалён — одну кнопку несёт умная
  кнопка в шапке).

Версия: `PL_JS_VERSION = "1.34-folder-export"`.

## 39.3. Проверено без ComfyUI

- `node --check web/js/prompt_library.js` — без ошибок.
- Смоук (новые фазы v1.34, всего 83 с фазами v1.39): экспорт категории «Фото» — зеркало
  иерархии (`Фото/…md`, `Фото/Портреты/…md`, `Фото/Портреты/Альбом/…md`),
  обложка РЯДОМ (не в подпапке `<title>/`), полный текст в `.md`; `__all`/`__fav`/
  `__root` (только нужные записи, дерево сохраняется); пустая категория — без
  диалога; отмена — тихо; нет API — подсказка; у строк дерева 📤 НЕТ, кнопка
  «📤 Экспорт» — одна, умная, в шапке проводника рядом с «+ Категория»
  (с метками → отмеченное, без меток → текущая категория; возвращает Promise);
  отдельной bulk-кнопки в нижней строке НЕТ; прогресс-бар скрыт до/после
  экспорта, заполняется во время (`setExportProgress`), во время показа
  подсказка скрыта (бар занимает строку) и возвращена после;
  `exportMarked` (записи + категории, неотмеченные не пишутся), экспорт
  без помеченного — без диалога.
- Ловушка тестов: фоновый `reload()` асинхронно перезаписывает `st.entries` из
  `/list` — стаб `fetch` должен отдавать те же записи (`installFetchStub`), а
  перед вызовом экспорта — `await st.reload()`, иначе `st.entries === []` и
  «нечего экспортировать». Хелпер `makeDirHandle` фейкает File System Access API.
  Умная кнопка возвращает Promise из `onclick` — тест вызывает
  `await st.exportBtn.onclick()` (иначе экспорт не дожидается).
- Python-песочница 244/244, аудит чист.

## 39.4. Как проверять живьём (после перезапуска ComfyUI)

1. Кликнуть категорию в дереве (она подсветится) → «📤 Экспорт» в шапке
   проводника рядом с «+ Категория» → выбрать папку → папка категории с `.md`
   (+ подпапки, при наличии) и обложками рядом; во время экспорта — прогресс-бар.
2. Выбрать «Всё»/«Избранное»/«Без категории» → «📤 Экспорт» в шапке — вся
   база / избранные / только без категории, без внешней папки категории.
3. Ctrl/Shift-выделить записи и категории → в нижней строке «📤 Экспорт» →
   отмеченное пишется на диск; без пометок кнопки с экспортом нет.
4. Хром/Edge обязателен — в другом браузере стойкая подсказка.

---

# 40. Мультивывод: несколько STRING-выходов из одной ноды (v1.44, дизайн 2026-09-22)

## 40.1. Задача

Одна нода должна отдавать **несколько** промптов по проводам (роли для Ollama:
system / prompt / style / negative и аналоги) — стандартного `prompt_out` мало.
Решение пользователя (из обсуждения): НЕ полоса выходов, а **категория в
проводнике** «📤 Выходы» (работает как «Избранное») + **первый провод остаётся
стандартным**. Новые выходы создаются ТОЛЬКО через добавление в категорию
«Выходы». Категорию вывода добавить в проводник ноды.

## 40.2. Механика

- Python: `RETURN_TYPES = ("STRING",) * 12`, `RETURN_NAMES` — первые два
  `("prompt_out", "category_out")` без изменений (совместимость со старыми
  графами), остальные `out_2`…`out_11` (или имена по карточке). Неиспользуемые
  слоты возвращают `""`.
- JS: сокеты по умолчанию скрыты (`node.outputs[i].hide = true`) — на узле видны
  только `prompt_out`, `category_out` и занятые слоты.
- Привязка слота → запись/папка живёт в скрытом JSON-виджете `slots_out`:
  ```
  [ {i: 2, kind: "card", id: "...", name: "…"},
    {i: 3, kind: "folder", path: "Пейзажи", active_id: "…"},
    ... ]
  ```
- Смена содержимого слота — перезапись `slots_out` (cache-key меняется) →
  нода переисполняется → Python достаёт тексты по id из базы и возвращает по
  слотам; удалённая запись → `"(запись удалена)"`.
- **Слоты 0–1 (`prompt_out`, `category_out`) — НЕ трогаем**, логика `execute()`
  и санитизация `_sanitize_folder_path` остаются.

## 40.3. Поведение (решения пользователя)

| Событие | Действие |
|---------|----------|
| Дроп папки/карточки в категорию «📤 Выходы» | Создаётся слот на ближайшем свободном индексе, сокет показывается, имя провода = название (обрезано) |
| Удаление из категории «Выходы» | Отвязка провода + сокет скрывается + запись уходит |
| Папка-слот | Выделение карточки-вывода **не слетает пока подключена**; свой цвет (не синий актив `#2c4a73`, не оранжевый «помечено») — предложен `#2e6b4f`; маркер 🔌 на карточке-выводе; клик по другой карточке внутри папки-слота → слот сразу выводит её (перезапись `active_id`) |
| Быстрое переключение | Клик по другой карточке в категории «Выходы» → слот сразу выводит её |
| Лимит | ~12 слотов суммарно (10 новых: индексы 2–11) |

## 40.4. Что НЕ делаем

- Полосу выходов (отклонено — категория покрывает всё).
- Динамическое добавление сокетов через `addOutput` + пересчёт
  `RETURN_TYPES` (Python требует фиксированной длины) — только 12 статичных,
  скрытых/показываемых сокетов.
- Изменение `prompt_out`/`category_out`.

## 40.5. Тесты

- Python: эммит слотов из `slots_out`, `""` для пустых, `(запись удалена)`.
- JS: дроп → сокет показан/имя; удаление → отвязка/скрытие; цвета; маркер.

## 40.6. Статус

Дизайн утверждён пользователем (2026-09-22, в этой сессии). Реализация —
следующим шагом, точка возврата — коммит v1.43 + дизайн (запись 36 §15).

---

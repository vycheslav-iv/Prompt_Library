# Память сессии — Prompt Library (2026-09-17, Vue-режим: растяжение + смена режима)

> Покажи этот файл агенту, чтобы продолжить работу.
> Всегда сверяйся с `AGENTS.md` и `SPECIFICATION.md`.

---

## 1. Что делали в этой сессии (кратко)

- Починили ноду: в незакоммиченных Vue-правках были **потеряны два блока** — `const st = {…}`
  и определение `dropAutoSockets` → `TypeError`/`ReferenceError` при добавлении ноды.
- По решению пользователя **откатились до `aa98777`** (копия отброшенных правок —
  `Custom_node_ComfyUI/Prompt_Library_js_workingtree_2026-09-17.bak`, вне репозитория).
- Сделали **растяжение контента в Nodes 2.0** (только Vue-ветка, canvas не трогали) —
  закоммичено и запушено: **`d917e2e`** (v1.11).
- По итогам живого теста закрыли три проблемы Vue (§22.10 SPEC): сжатие ноды
  «в ноль», смену режима без перезагрузки, и «сжатую» новую ноду.
  **Всё это в рабочей копии, ещё НЕ закоммичено.**
- Пол высоты в Vue поднят 280 → 480 (= канвасному, одна константа `PANES_MIN_H`).
- Глубокий аудит (§25 SPEC): починены баг устойчивости `_load_db` и вечный
  repaint канваса (`checkCycle` → `onDrawForeground`). Главный незакрытый риск —
  воркфлоу inline в `library.json` (2.9 МБ / 92 записи).
- Живое переключение canvas → Nodes 2.0 без F5 давало УЗКУЮ ноду
  (Vue монтирует ноду асинхронно → `applyNodeMinWidth` уходил в пустоту, нода
  оставалась на дефолтных 350px). Фикс — `settle()` в `onModeChange`
  (2 кадра rAF). См. §22.11 SPEC, маркер сборки `1.14-vue-switch-width`.

## 2. Итоговое состояние кода

- `web/js/prompt_library.js:27-50` — `plModeWatchers` + `plHookVueMode()` — перехват
  `window.LiteGraph.vueNodesMode` (обёртка accessor'ом один раз на страницу).
- `web/js/prompt_library.js:374` — `st.isVueNodes()` — сначала `LiteGraph.vueNodesMode`,
  затем `extensionManager.setting.get`, затем `ui.settings.getSettingValue`.
- `web/js/prompt_library.js:94` — `PANES_MIN_H = 480` — единый пол высоты панелей
  для ОБОИХ режимов (канвас: панели; Vue: `scrollArea`). Раньше в Vue было 280 →
  ноду можно было сжать заметно сильнее, чем в старых нодах.
- `web/js/prompt_library.js:411` — `st.applyPaneLayout(forceVue)` — **ключевое место**.
  Общее для обоих режимов: `root` `overflow:hidden` + `min-width:0` (страховка от
  разъезжания за ноду). Vue-ветка: `main`+`detail` в `scrollArea`, панели
  `flex:1 1 0`/`min-height:0`, а ПОЛ `min-height:480px` (`PANES_MIN_H`) — на самом `scrollArea`.
  Canvas-ветка: панели `flex:1 1 auto`/пол 480px, scrollArea не используется (§22.4).
- `web/js/prompt_library.js:483-501` — `st.onModeChange` + регистрация в `plModeWatchers`
  + подписка на событие настроек `Comfy.VueNodes.Enabled.change`; внутри `settle()`
  (2 кадра rAF) применяет `applyNodeMinWidth` + `enforceMinWidth` — фикс узкой
  ноды при живом переключении (§22.11).
- `web/js/prompt_library.js:889` — догоняющая сверка режима в конце `render()`.
- `web/js/prompt_library.js:1194` — `onRemoved` отписывает ноду от смены режима.
- `web/js/prompt_library.js:52` — `PL_JS_VERSION` (`1.15-vue-floor480`) + `console.log` маркер сборки
  (`st.version`) — чтобы точно знать, какая версия JS загружена браузером.
- `web/js/prompt_library.js:1067` / `:1080` — `st.minH()` и `computeLayoutSize()`
  (единый минимум `{minHeight: st.minH(), minWidth: MIN_W}`, `maxHeight` не задаём).
- `web/js/prompt_library.js:992` — `st.dropAutoSockets()`; `:273` — `scrollArea`;
  `:283` — `const st = {…}`.
- `_smoke_prompt_library.mjs` (в корне бандла, вне репозитория) — смоук-тест JS: 45 фаз,
  оба режима, живая смена режима, фолбэк без `window.LiteGraph`, отсутствие
  repaint-петли. **Гонять перед каждым синком.**
- `_test_prompt_library.py` — функциональный тест Python: **66 проверок**, песочница
  (`folder_paths` → temp, заглушки `server`/`aiohttp`, все 10 роутов вызываются
  напрямую). `_audit_prompt_library.mjs` — связность `st.*`, мёртвые остатки,
  сверка роутов JS ↔ Python, локали, порядок `widgets_values`. Оба — в корне бандла.

## 3. Проблемы, которые встречались (и как решали)

- `dropAutoSockets is not a function` + `st is not defined` — потерянные блоки в
  незакоммиченной правке; восстановили (целы в `aa98777`).
- «В Vue контент не тянется за нодой» — причина: Vue-ветка `applyPaneLayout` пинила
  `main flex:none` и панели на 480px. Цепочка высоты в Vue есть (факты §22.9).
- «В Vue ноду можно сжать в ноль» — `min-height:0` у панелей убирал пол; вернули пол 480px.
- «Смена режима не применяет раскладку» — сигналом было только событие настроек,
  а его на этой сборке ноде не хватало; добавили перехват `LiteGraph.vueNodesMode`.
- «В Vue новая нода открывается сжатой» — в Vue высоту задаёт минимальная высота
  контента (`measureMinContentHeight`; в канвасе ноду растит сам фронтенд:
  `!vueNodesMode && l > t && setSize`), а пол `min-height` на панелях в этот замер
  не попадал (панели внутри `overflow-y:auto`). Пол перенесли на `scrollArea`.
- Битый/чужой `library.json` (не-словарь в `entries`) ронял все запросы → отброшены
  битые записи при загрузке (§25.2).
- `checkCycle` звался из `onDrawForeground` и безусловно метил канвас грязным →
  вечный repaint, пока нода видна (§25.2).
- «После переключения режима без F5 нода узкая» — Vue монтирует ноду
  асинхронно: в момент смены `LiteGraph.vueNodesMode` элемента `[data-node-id]`
  ещё нет (`closest` → `null`), а в reload-пути то же делает rAF в `onNodeCreated`
  уже после монтирования. Решили `settle()` с повторным кадром.
- Ломало канвас в прежней (откаченной) попытке: `computeLayoutSize` возвращал
  `Math.max(this.size[1], …)`, `minH()` был сведён к BASE_H. Вывод: canvas-ветку не менять.

## 4. Что важно не сломать при продолжении работы

- **Canvas-ветку `applyPaneLayout` и `computeLayoutSize`** — проверены живьём (§22.4).
- Порядок INPUT_TYPES `[mode, selected, save_folder]`; `widget.serialize = false` свойством;
  ноль `setTimeout` в restore; никаких вечных таймеров/наблюдателей (сверка режима — по событию).
- Не возвращать `autoFitHeight`/`calibrateFloor`/`_vueFloor`: self-lock на первом замере
  (в Vue `root` растянут → `offsetHeight` = высота ноды, а не контент).
- Синк: `python sync.py Prompt_Library` → `D:\ComfyUI_windows_portable\...` → **Ctrl+F5**
  (JS-правки не требуют рестарта сервера).
- Исходники фронтенда читать так: `...\comfyui_frontend_package\static\assets\settingStore-*.js.map`
  → `sourcesContent` (там оригинальный TS/Vue, а не минифицированный код).

## 5. Следующие шаги (идеи, не сделано)

1. **Вынести воркфлоу из `library.json`** (главный риск §25.3): `workflows/{id}.json`
   или SQLite; сейчас 2.9 МБ / 92 записи (6 воркфлоу = 1.72 МБ), лимит — 500 записей.
2. Проверить живьём: `PANES_MIN_H = 480` и добавление новой ноды в Vue,
   переключение режима без F5, отмена confirm при смене режима в Nodes 2.0 (§25.3).
3. Закоммитить и запушить текущее (после проверки) + обновить память.
4. SPLIT на две ноды (Prompt Library + Prompt Saver); постраничность (~20 записей).

## 6. Связанные файлы

- `web/js/prompt_library.js` — нода (JS-расширение).
- `SPECIFICATION.md` — §22.9 (факты из фронтенда + растяжение), §22.10 (решённые
  проблемы Vue: пол 480px и смена режима), §22.11 (ширина ноды при живом
  переключении), §25 (глубокий аудит: инструменты, дефекты, открытые риски),
  §22.6 (упразднённое решение про 480px), §22.4 (рабочий canvas-путь).
- `.bak` в корне бандла — отброшенные Vue-правки (можно удалить).
